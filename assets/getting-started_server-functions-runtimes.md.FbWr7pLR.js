import{_ as i,c as e,o as t,aj as a}from"./chunks/framework.Id7s26SP.js";const c=JSON.parse('{"title":"Tasks and Requests","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/server-functions-runtimes.md","filePath":"getting-started/server-functions-runtimes.md"}'),n={name:"getting-started/server-functions-runtimes.md"};function h(r,s,o,l,d,p){return t(),e("div",null,[...s[0]||(s[0]=[a(`<h1 id="tasks-and-requests" tabindex="-1">Tasks and Requests <a class="header-anchor" href="#tasks-and-requests" aria-label="Permalink to “Tasks and Requests”">​</a></h1><p>Every <a href="./server-functions.html">server function</a> runs under one of two runtimes: the <strong>request</strong> runtime, which answers inside the call, or the <strong>task</strong> runtime, which starts a run that can sleep for hours or days. This page covers the two runtimes and how a task run behaves — steps, resets, budgets, run status and run error codes. How a caller picks one is on <a href="./server-functions-invoking.html">Invoking and Triggers</a>.</p><h2 id="runtimes" tabindex="-1">Runtimes <a class="header-anchor" href="#runtimes" aria-label="Permalink to “Runtimes”">​</a></h2><p>A function is a function. What changes is the <strong>runtime</strong> the platform runs it under, and nothing in its config says which — the caller picks at each call, and each door has its own answer:</p><table tabindex="0"><thead><tr><th>Door</th><th>Runtime</th></tr></thead><tbody><tr><td><code>functions.invoke</code> / <code>POST functions/{key}</code></td><td>request</td></tr><tr><td>A webhook delivery</td><td>request</td></tr><tr><td><code>functions.start</code> / <code>POST functions/{key}/start</code></td><td>task</td></tr><tr><td><code>ctx.functions.start</code> from inside a function</td><td>task</td></tr><tr><td>A cron fire</td><td>task</td></tr></tbody></table><p>Every cron fire starts a task run, and every webhook delivery runs under the request runtime. That is a property of the door, not of the function. A schedule is waiting for nothing, so there is nothing for a bounded invocation to answer; a provider is waiting inside the request for the function&#39;s answer, and a task run has none to give there — it returns a run id and finishes later. Neither is a dead end: a webhook-fired invocation can <a href="#starting-a-task-run-from-a-function">start a task run</a>, the same way any other caller can.</p><ul><li><strong>the request runtime</strong> runs the code inside the call and hands back its result. It has to finish inside a request.</li><li><strong>the task runtime</strong> starts a run, hands back a run id immediately, and lets the code sleep for hours or days in between.</li></ul><p>So the file below takes both verbs, and nothing in it says so — that is the point:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># primitive/dev/functions/order-sync.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order-sync&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/order-sync/index.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span></code></pre></div><h3 id="ctx-runtime" tabindex="-1"><code>ctx.runtime</code> <a class="header-anchor" href="#ctx-runtime" aria-label="Permalink to “ctx.runtime”">​</a></h3><p>Every invocation carries the runtime it is running under, beside the trigger that fired it:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // &quot;request&quot; | &quot;task&quot; — set by the platform from the runner in use.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ctx.runtime, ctx.trigger.kind);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><code>ctx.trigger.kind</code> says which <strong>door</strong> fired the invocation; <code>ctx.runtime</code> says whether the platform ran the code inside the request or as a task run. They are different questions: a <code>manual</code> invocation can be either, while every other door has the one runtime the table gives it.</p><h3 id="ctx-runid-ctx-sliceid-and-ctx-trigger-runkey" tabindex="-1"><code>ctx.runId</code>, <code>ctx.sliceId</code> and <code>ctx.trigger.runKey</code> <a class="header-anchor" href="#ctx-runid-ctx-sliceid-and-ctx-trigger-runkey" aria-label="Permalink to “ctx.runId, ctx.sliceId and ctx.trigger.runKey”">​</a></h3><p>A function can read which run it is, and which slice of that run it is running in:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // The run this invocation belongs to — \`null\` for an \`invoke\`, which writes</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // no run row at all.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ctx.runId;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // The slice of that run. A task run is a sequence of slices: it changes when</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // the run hibernates and wakes, so it is how you tell a resume from a first</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // pass. \`null\` under the request runtime — a request invocation is not a</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // slice.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ctx.sliceId;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><table tabindex="0"><thead><tr><th></th><th><code>ctx.runId</code></th><th><code>ctx.sliceId</code></th></tr></thead><tbody><tr><td><code>invoke</code> over HTTP</td><td><code>null</code></td><td><code>null</code></td></tr><tr><td>A webhook delivery</td><td>the fire&#39;s run</td><td><code>null</code></td></tr><tr><td><code>start</code>, a cron fire, a nested start</td><td>the run</td><td>the slice</td></tr></tbody></table><p><code>ctx.sliceId</code> is the same identifier <code>primitive functions runs</code> and the run status route&#39;s <code>slice.sliceId</code> report, so a log line carrying it lines up with what an operator sees.</p><p>When a start was coalesced by a run key — <code>start</code> with a <code>runKey</code>, <code>ctx.functions.start(key, input, { runKey })</code>, or an admin&#39;s system start — <code>ctx.trigger.runKey</code> carries it, on the <code>http</code>, <code>function</code> and <code>manual</code> arms. It is <code>null</code> when the start named none and on a request invocation, which coalesces nothing. A trigger root&#39;s run key is its own run id, which <code>ctx.runId</code> already says, so the <code>webhook</code> and <code>cron</code> arms do not carry one.</p><h3 id="assertruntime" tabindex="-1"><code>assertRuntime</code> <a class="header-anchor" href="#assertruntime" aria-label="Permalink to “assertRuntime”">​</a></h3><p>Some code only makes sense one way round. A function that sleeps for a day is not something anyone should be calling with <code>invoke</code> and waiting on. Say so in the code, and the platform refuses the call:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { assertRuntime, defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// At MODULE SCOPE: every invocation under the wrong runtime is refused before</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// a line of the handler runs.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertRuntime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;task&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">since</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sleep</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cooling-off&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;24 hours&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { sweptAt: Date.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">now</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>It works inside the handler too, and inside a <code>step.do</code> body, if only part of the code needs the guarantee:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (input.slow) </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertRuntime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;task&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // …</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>A mismatch throws a typed <code>FunctionRuntimeError</code> (<code>name</code> is <code>&quot;FUNCTION_RUNTIME_REFUSED&quot;</code>, with <code>runtime</code> and <code>required</code> on it) and the platform settles the invocation as <code>status: &quot;failed&quot;</code> with <code>errorCode: &quot;FUNCTION_RUNTIME_REFUSED&quot;</code> and a message naming both runtimes and the verb that takes the one it needs:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;failed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;errorCode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;FUNCTION_RUNTIME_REFUSED&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;error&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;FUNCTION_RUNTIME_REFUSED: this function is running under the request runtime and requires the task runtime. Start it as a task: functions.start(...) from a client, or POST .../functions/{key}/start.&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The same settlement on a task run: the run ends <code>failed</code>, its <code>errorCode</code> is <code>FUNCTION_RUNTIME_REFUSED</code>, and <code>status.error.message</code> leads with the code. Either way the invocation log records it.</p><p>There is <strong>no declaration in code or config</strong> beyond this. The assertion is the lock, and it is the only one: <code>mode</code> and <code>durable</code> are not keys of a function, and nothing in its TOML chooses a runtime.</p><h3 id="step-under-each-runtime" tabindex="-1"><code>step</code> under each runtime <a class="header-anchor" href="#step-under-each-runtime" aria-label="Permalink to “step under each runtime”">​</a></h3><p>The handler&#39;s third argument, <code>step</code>, is there under <strong>both</strong> runtimes, so one body runs either way. Where they genuinely differ, the request runtime says so rather than pretending:</p><table tabindex="0"><thead><tr><th></th><th>Task runtime</th><th>Request runtime</th></tr></thead><tbody><tr><td><code>step.do</code></td><td>Runs once and the result is <strong>memoized</strong>; a replayed slice does not re-run it.</td><td>Runs the body <strong>inline</strong>. Nothing is persisted and nothing is replayed.</td></tr><tr><td><code>step.sleep</code> / <code>step.sleepUntil</code></td><td><strong>Hibernates.</strong> Nothing runs and nothing is billed while it waits; hours and days are fine.</td><td>Really waits, when the remaining request <strong>budget</strong> covers it. When it does not, the invocation fails with <code>errorCode: &quot;FUNCTION_RUNTIME_REFUSED&quot;</code>, naming the step, the duration and the budget left.</td></tr><tr><td><code>step.waitForEvent</code></td><td>Waits for the event.</td><td>Fails with <code>FUNCTION_RUNTIME_REFUSED</code>: nothing can deliver an event to an invocation that has to return now.</td></tr></tbody></table><p>It is the same code an <code>assertRuntime</code> refusal settles with, and for the same reason: this runtime cannot host this code. The thrown error&#39;s <code>name</code> is <code>STEP_NOT_AVAILABLE</code> in every step refusal, so a body that catches it keeps catching it. A duration the platform cannot read (<code>&quot;3 fortnights&quot;</code>) is an ordinary throw naming the grammar — never a silent &quot;done&quot;.</p><p>This is the rule on <strong>every</strong> request-runtime door, not just the HTTP one: a webhook delivery answers <code>FUNCTION_RUNTIME_REFUSED</code> for a wait it cannot cover. The remedy is always the same — start the function as a task instead.</p><p>The handler with <code>step</code>:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">orderId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> charged</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;charge&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.api.documents.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ documentId: input.orderId });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Hibernate. Nothing is running — and nothing is billed — while it waits.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sleep</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cooling-off&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;24 hours&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { orderId: charged.documentId, confirmedAt: Date.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">now</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h2 id="task-runs" tabindex="-1">Task runs <a class="header-anchor" href="#task-runs" aria-label="Permalink to “Task runs”">​</a></h2><p>A task run is a sequence of slices: the engine runs the handler, hibernates it at a sleep, and wakes it again. The sections below cover starting one from code, keeping its steps safe to replay, what it may spend, and reading what it did.</p><h3 id="starting-a-task-run-from-a-function" tabindex="-1">Starting a task run from a function <a class="header-anchor" href="#starting-a-task-run-from-a-function" aria-label="Permalink to “Starting a task run from a function”">​</a></h3><p><code>ctx.functions.start</code> starts another function as a <strong>task run</strong> from inside a running one — from an invocation, from a task run&#39;s slice, and from a webhook- or cron-fired run alike:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> started</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;digest-page&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { userIds }, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  runKey: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`digest-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// { runId, runKey, instanceId, status: &quot;running&quot; }</span></span></code></pre></div><p>It answers the same start envelope an HTTP start answers, so the run id polls and terminates like any other. Repeating a <code>runKey</code> <strong>replays</strong>: you get <code>existing: true</code> and the run that already exists, which is how a run key becomes a singleton — including when that run <strong>failed</strong>. A run key says &quot;this run, once&quot;, not &quot;this run, until it succeeds&quot;, so a retry is a new key with the same input.</p><p>There is no nested <em>invoke</em>. Running a function inside your own invocation is ordinary code, and code composes by being imported — put the shared logic in a module both functions import. <code>ctx.functions.start</code> always starts a task run, whatever the callee is; a callee that must not run that way says so with <code>assertRuntime(&quot;request&quot;)</code>, and the refusal settles the child run <code>FUNCTION_RUNTIME_REFUSED</code>.</p><p><strong>The callee&#39;s <code>access</code> expression is not evaluated.</strong> Your own gate was the authorization, and your code chose the callee, so a function can start one the invoking member could not call themselves. Decide who may reach the tree in the <code>access</code> expression of the function at the <em>top</em> of it.</p><p><strong>Who the run belongs to.</strong> Every run in an invocation tree — the root and every nested start under it — is keyed by the <strong>original external initiator</strong>: the member who made the HTTP call, or the app&#39;s system principal when a trigger fired it. So the member who started a tree polls and terminates every run in it, another member gets the routes&#39; <code>404</code>, and a trigger-rooted tree is an admin&#39;s to inspect.</p><p><strong>Which document the run is keyed under.</strong> A task run is addressed by a context document plus its run key, so a nested run always has one, resolved in this order:</p><ol><li>the <code>contextDocId</code> you pass;</li><li>else the <strong>parent run&#39;s</strong> context — so a whole tree shares one;</li><li>else the <strong>root caller&#39;s root document</strong>, which is the default a member&#39;s own start uses;</li><li>else <code>fn:&lt;the root function&#39;s id&gt;</code> — a synthetic context, the same shape a cron-fired run&#39;s <code>cron:&lt;triggerId&gt;</code> has. This is what a trigger-rooted tree gets, and it is why a run key there is a singleton per root function across fires rather than per member.</li></ol><p>Rule 2 reads the parent&#39;s run row. If that row has expired or been deleted mid-tree, resolution falls through to rules 3 and 4 — a rule, not an accident. The value comes back on the run&#39;s status response, and you need it to terminate a trigger-rooted child by key (<code>?contextDocId=fn:&lt;id&gt;</code>).</p><p>Both <code>runKey</code> and <code>contextDocId</code> are bounded at 100 characters.</p><p><strong>Inside the child</strong>, <code>ctx.trigger.kind</code> is <code>&quot;function&quot;</code> and carries the parent function&#39;s key and its run id (<code>null</code> when the parent was a request invocation, which writes no run row). <code>ctx.user</code> names the tree&#39;s root caller, and <code>ctx.user</code> is null when a trigger rooted the tree — nobody called it.</p><p><strong>Nesting is bounded at 4 levels.</strong> A root is level 0, so four nested starts fit below it; the fifth answers <code>409 FUNCTION_NEST_DEPTH_EXCEEDED</code>. The depth travels on the platform&#39;s own invocation credential, so it is not something your code can raise, and the ceiling is what stops a function that starts itself from running away.</p><p>Each run is listed under its own function — <code>primitive functions runs digest-page</code> — with <code>FIRED BY function</code> and a <code>PARENT</code> column naming the function that started it.</p><h3 id="iterate-every-user" tabindex="-1">Iterate every user <a class="header-anchor" href="#iterate-every-user" aria-label="Permalink to “Iterate every user”">​</a></h3><p>App-wide iteration is a cursor plus a nested start: one page per slice, one child run per page. Three functions, and you can copy them as they are.</p><p><strong>The trigger.</strong> A function on a schedule (each cron fire is a short task run of its own), which starts the orchestrator with a run key derived from the date — so a second fire the same day replays rather than starting a second tree, and the cron&#39;s own <code>overlapPolicy</code> bounds this parent.</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># functions/nightly-digest.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nightly-digest&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/nightly-digest.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;false&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">triggers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cron</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nightly&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cron = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0 3 * * *&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">overlapPolicy = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;skip&quot;</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// functions/nightly-digest.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> date</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toISOString</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slice</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;digest-all-users&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { date }, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    runKey: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`digest-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>The orchestrator.</strong> A function, started as a task run, that takes one page per slice and hands each to a child. Both children declare <code>assertRuntime(&quot;task&quot;)</code> at module scope: neither is anything but a background run, and <code>ctx.functions.start</code> is the only way in. (The assertion is the authors&#39; choice, not a requirement — <code>ctx.functions.start</code> starts any callee as a task run.) Both declare <code>access = &quot;false&quot;</code>, because neither is ever invoked over HTTP: a nested start does not consult the callee&#39;s gate, so a closed gate costs the tree nothing and keeps the children off the member-callable surface.</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># functions/digest-all-users.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;digest-all-users&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/digest-all-users.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;false&quot;</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// functions/digest-all-users.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { assertRuntime } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// This orchestrator is only ever a background run.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertRuntime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;task&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cursor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> page </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> users </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> taken</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`page-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      ctx.users.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">list</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ limit: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, cursor })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    );</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    users </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> taken.items.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`start-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      ctx.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;digest-page&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        { userIds: taken.items.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">u</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> u.userId) },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        { runKey: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`digest-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      )</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    );</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    cursor </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> taken.nextCursor;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    page </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">taken.hasMore </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">||</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> !</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cursor) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">break</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sleep</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`next-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1 second&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { pages: page, users };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The pages are fetched <strong>inside <code>step.do</code>, with an explicit cursor</strong>, and that is not a style choice: a memoized step does not advance an iterator, so <code>ctx.users.iterate</code> would replay pages it had already taken the moment the run resumed after the sleep. Use <code>iterate</code> in an invocation or a single-slice loop; page by hand across slices.</p><p><strong>The page worker.</strong> One task run per page, so each page&#39;s budget is its own.</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># functions/digest-page.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;digest-page&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/digest-page.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;false&quot;</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// functions/digest-page.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { assertRuntime, pMap } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertRuntime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;task&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;digest&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pMap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(input.userIds, (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.users.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(userId, { digest: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }), {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      concurrency: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  );</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>The budget arithmetic.</strong> A task slice may make 10 000 subrequests (a request invocation 128) and 10 minutes of wall-clock budget per slice, refreshed by the platform as it goes so the clock is continuous for up to 12 hours. At one call per user, a page of 50 fits with room for the start and the cursor read — thousands of times over. Neither ceiling binds a page of sends: it is seconds of work, and a single <code>step.do</code> may run for up to a whole slice budget. What does bind is the yield at half the subrequest ceiling (see Budgets in a task run): a worker that has made 5 000 counted calls in one slice ends the slice at its next step boundary, so keep pages bounded and let the child runs do the fanning out. <code>pMap</code>&#39;s default concurrency of 8 bounds how many are in flight, not how many run in total.</p><p><strong>Inside the children</strong>, <code>ctx.user</code> is null and <code>ctx.trigger.kind</code> is <code>&quot;function&quot;</code> — nobody called this tree, the schedule fired it. Every run in it is keyed under <code>fn:&lt;nightly-digest&#39;s id&gt;</code>, which the status response reports and which you pass as <code>?contextDocId=</code> to terminate a child by key.</p><p><strong>Partial failure is per page.</strong> A page that throws is one failed run in <code>primitive functions runs digest-page</code>, beside its completed siblings; the other pages are unaffected.</p><p><strong>Retrying a page takes a NEW run key.</strong> A run key is a singleton, and that is all it is: starting <code>digest-page</code> again with the key the failed run already holds answers <code>200 {existing: true}</code> describing that failed run, and nothing runs a second time. Give the retry its own key and the same input:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;digest-page&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { userIds },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { runKey: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`digest-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}-retry-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">attempt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Same context document, so the retry is keyed under the same tree and shows up beside the run it replaces. A child that ends without anybody polling it settles its own row as it ends, like any other task run — see <a href="#when-a-run-s-status-settles">When a run&#39;s status settles</a>.</p><h3 id="reporting-back" tabindex="-1">Reporting back <a class="header-anchor" href="#reporting-back" aria-label="Permalink to “Reporting back”">​</a></h3><p>A run never broadcasts over the WebSocket, so decide up front how its result reaches whoever cares. Three channels, and they compose:</p><ul><li><p><strong>The run&#39;s <code>output</code>.</strong> Return a value and it is recorded on the run&#39;s own record by the same settlement that records its status, so it stays readable for the life of the run row — 45 days — whether or not the engine still holds the instance. <code>client.functions.getStatus(runId).output</code> and <code>waitFor(runId).output</code> answer it, and so do <code>primitive functions runs wait &lt;function-id&gt; &lt;run-id&gt;</code> and <code>primitive functions runs &lt;function-id&gt; --json</code>. The <code>runs</code> TABLE does not print it: an output can be a megabyte, so the table says where to find it.</p><p>Outputs over roughly 10 000 characters are stored by reference and answered whole; the split is invisible to a reader, except that a <code>runs</code> page holding many large outputs may be shorter than the limit you asked for and continue with a cursor. If the platform could not store a large output when the run settled, the record carries a preview marked <code>outputTruncated</code> and is completed from the engine on the next read while the engine still holds the run; once it has forgotten the instance, the preview is what is left.</p></li><li><p><strong>Server state.</strong> Write database rows, documents or notifications — on the app&#39;s authority, attributed to the caller — inside <code>step.do</code>. This is the natural shape for a job that only needs to leave results behind.</p></li><li><p><strong>A live push.</strong> <code>ctx.users.send</code> reaches a client that is connected right now and nobody else — pair it with state or a notification if the message has to survive the client being away.</p></li></ul><h3 id="step-discipline" tabindex="-1">Step discipline <a class="header-anchor" href="#step-discipline" aria-label="Permalink to “Step discipline”">​</a></h3><p>The engine re-executes your handler from the top on every resume and replays each completed <code>step.do</code> from its <strong>stored result</strong> instead of running it again. Two rules follow, and both matter:</p><ul><li><strong>Put every side effect inside <code>step.do</code>.</strong> A charge, an email, a write made outside a step runs again on every resume. Inside one it runs exactly once, and its return value is what later resumes see.</li><li><strong>Do not branch on anything nondeterministic between steps.</strong> <code>Date.now()</code>, <code>Math.random()</code> or a fresh read at the top level can take a different path after a resume, and the engine then looks for steps that are not there. Read such values inside a step, so the value is stored with it.</li></ul><h3 id="locks-from-a-function" tabindex="-1">Locks from a function <a class="header-anchor" href="#locks-from-a-function" aria-label="Permalink to “Locks from a function”">​</a></h3><p>A function takes a named <a href="./locks.html">lock</a> through the platform&#39;s own lock API, and a task run has one thing to get right: <strong>acquire live on every slice, outside <code>step.do</code></strong>.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">userId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> key</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> \`portfolio-import:\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">userId</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Live, on every slice. \`ctx.runId\` is the owner: presenting it again from a</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // later slice of the SAME run re-takes the lease with a fresh handle.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> got</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.api.locks.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tryAcquire</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    body: { key, ttlMs: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">120_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, owner: ctx.runId },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">got.acquired) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Somebody else&#39;s run holds it — \`got.owner\` says whose.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { skipped: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, heldBy: got.owner };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;import&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // …the work that must not overlap…</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">finally</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Live too: the handle belongs to this slice.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.api.locks.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">release</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      body: { key, handle: { handleId: got.handle.handleId } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>A memoized acquire — one inside <code>step.do</code> — would hand a resumed slice the handle a <em>previous</em> slice was given, and that handle no longer works. That is why both the acquire and the release are live.</p><p><code>owner: ctx.runId</code> is what makes the re-acquire succeed rather than collide with the run&#39;s own earlier slice. When several triggers are coalesced into one run by a run key, name the work that key stands for (<code>ctx.trigger.runKey ?? ctx.runId</code>) instead. Never a static string: two unrelated runs presenting one would re-enter each other&#39;s hold. See <a href="./locks.html#re-taking-your-own-lease">Re-taking Your Own Lease</a> for the full rule.</p><h3 id="what-resets-a-running-task" tabindex="-1">What resets a running task <a class="header-anchor" href="#what-resets-a-running-task" aria-label="Permalink to “What resets a running task”">​</a></h3><p>A long task run can be interrupted and started again by the platform, and it is worth knowing exactly what does that and what does not.</p><p><strong>A <code>config push</code> never resets a running task.</strong> A run is pinned to the <strong>content hash</strong> of the version that started it, and it reloads that same immutable bundle at every wake. Pushing a new version mid-run creates a new version and points the function at it for the <em>next</em> call; the run in flight goes on executing the code it started on. Nothing in <code>config push</code>, <code>config push --prune</code>, <code>functions archive</code> or <code>functions activate</code> reaches a run that is already going.</p><p><strong>A platform deploy does.</strong> The platform&#39;s own code — the <code>primitive-functions</code> SDK and the runtime around your handler — is <strong>not pinned</strong>: it is whatever is deployed at the moment your run wakes. Your handler executes inside the platform&#39;s own runtime, so when the platform is redeployed that runtime is reset under every slice that is <em>executing</em>, and the work in flight stops where it is. <strong>An isolate eviction does the same</strong> — the runtime may reset for memory, and a run sees the identical interruption. Neither is anything your code did.</p><p><strong>What the engine then does.</strong> The reset does not lose the run. About five minutes later the engine dispatches your handler again, and:</p><ul><li>your handler runs <strong>from the top</strong>;</li><li>every <code>step.do</code> that had already <strong>completed</strong> is replayed from its stored result — its body does not run again;</li><li>the step that was <strong>executing</strong> when the reset landed runs again, as a <strong>further attempt of that step</strong>.</li></ul><p>That last line is the one with a consequence. The re-run is charged to the <strong>step&#39;s own retry budget</strong> — the engine&#39;s default is 5 attempts per step — so a step that lowers <code>retries.limit</code> spends the difference on platform deploys it did not ask for, and a step whose budget the reset exhausts does <strong>not</strong> run again: the run fails with the engine&#39;s own internal-error message, which the platform records as <a href="#run-error-codes"><code>ENGINE_INTERNAL_ERROR</code></a>. If a step is short and idempotent, leave its retries alone; if you have lowered them, raise the limit by one for the deploys.</p><p><strong>What your code may SEE when it is an eviction.</strong> A deploy takes the runtime away without warning your handler — the slice simply stops. An eviction can arrive the other way round, and this is the shape worth recognising: the object your handler is running on goes away while it keeps executing, so every <code>step.do</code> it makes from then on — including the ones in its own error handling — <strong>rejects immediately</strong>, in no time at all, with the runtime&#39;s own sentence:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Connection closed: this Durable Object instance is no longer active.</span></span></code></pre></div><p>That rejection is <strong>not your step failing</strong>, and the run it belongs to is <strong>not over</strong>. The platform reads it as the teardown it is: the slice is recorded as reset, the row is left <code>running</code>, and the engine dispatches your handler again about five minutes later.</p><p>The consequence is for anything you wrote in a <code>catch</code>. Clean-up there — rolling something back, marking a job abandoned, writing a failure row of your own — runs <strong>against a run that will resume</strong>, so whatever it undoes, the replay will be doing again:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;import&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* … */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (err) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // This runs on an eviction too, and the run is not finished.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Anything recorded here is contradicted by the resumed run.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mark-abandoned&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* … */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  throw</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> err;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Two rules follow. Keep such clean-up <strong>idempotent</strong>, the same way step bodies have to be — the resumed run may reach it again. And do not treat it as the run&#39;s ending: what the run finally settled as is on the run&#39;s own row, and a run that resumes and completes reads <code>completed</code> with its output <strong>even if the platform had already recorded a failure for the teardown</strong>. The row records the real outcome, not the interrupted one.</p><p><strong>What it means for your code.</strong> Exactly what <a href="#step-discipline">step discipline</a> already asks, and a reset is the reason it is not optional: a side effect <strong>outside</strong> <code>step.do</code> runs again on a reset, and one inside a step that the reset interrupted may run twice — once in the attempt that was cut off, once in the attempt that replaces it. Make each step&#39;s body safe to repeat (an upsert rather than an insert, an idempotency key on a charge), and keep the value later steps read as the step&#39;s <strong>return value</strong> rather than something it wrote to a variable.</p><p><strong>How to see it.</strong> A run that was reset and then finished reads <code>completed</code> with no failure and no error code, so the count is where the history lives:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># the RESETS column: how many of this run&#39;s slices the platform tore down</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># which step was interrupted, and which ones replayed</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> steps</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># what the run printed, per slice</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-ke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --run</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p><code>client.functions.getStatus(runId).slice</code> carries the same three fields — <code>resets</code>, <code>lastResetAt</code> and <code>lastResetCause</code> (<code>code-updated</code> when the platform build changed underneath the run, <code>evicted</code> when the runtime said the isolate was reset, <code>unknown</code> when the platform saw a teardown it could not attribute). Treat the SDK as a stable interface rather than a frozen artifact, and treat a reset as ordinary: on a busy platform a run of several hours will meet one.</p><h3 id="budgets-in-a-task-run" tabindex="-1">Budgets in a task run <a class="header-anchor" href="#budgets-in-a-task-run" aria-label="Permalink to “Budgets in a task run”">​</a></h3><p>The ceilings below apply <strong>per engine invocation, not per run</strong>. A run is a series of <em>slices</em>: the engine runs your handler from the top until it returns or reaches a sleep that has not finished, then hibernates; the next wake is a new slice, with completed steps replayed from storage.</p><ul><li><strong>A slice&#39;s wall clock is continuous, up to 12 hours; a step never starts short.</strong> Each slice is minted with a 10-minute credential, and every step begins with at least 5 minutes of it. When a step finishes with less than 5 minutes left, or is about to start with less than 5 minutes left, the platform REFRESHES the credential through the gateway — the same road every <code>ctx.api</code> call travels — and the slice&#39;s clock rolls straight on with no pause. So a single step may run up to 10 minutes (a two-minute LLM call, a slow integration, a large export all fit inside one <code>step.do</code>), and a sequence of steps runs as long as it needs, up to a 12-hour ceiling from the slice&#39;s start. The guarantee holds at every <code>step.do</code> boundary — including a cleanup step that runs after your handler catches a failed step, which begins on a refreshed budget rather than what the failed step left. Code outside steps spends the clock it runs in, and the next <code>step.do</code> entry refreshes it. A handler that never calls <code>step.do</code> — one long await — gets no refresh and dies at the 10-minute slice deadline.</li><li><strong>When a refresh is refused, the slice falls back to a pause of a little over 5 minutes.</strong> A refresh is refused past the 12-hour ceiling, more than once a minute (one per 60-second window, counted on the platform&#39;s epoch-aligned clock, so two refreshes 59 s apart across a window boundary are both honoured), or during a rate-limiter or engine outage. The fallback is the old behaviour: the platform ends the slice with a hibernating sleep — the engine hibernates only for a sleep past its own five-minute grace period, so that is what the fallback sleeps — and the next wake mints a fresh 10-minute slice with a fresh 12-hour ceiling. So a refused refresh costs a little over five minutes, once; an ordinary run past a refusal simply continues on the new slice. Because the fallback is a hibernation, what the run printed before it is not recoverable — it is not on the log record (see <a href="./server-functions.html#debugging-a-failing-function">Debugging a failing function</a>). Steps running in parallel (<code>Promise.all</code> over two chains) that reach a fallback yield together: it waits for every step in flight to finish before it sleeps, because the engine hibernates only when nothing is running. On a local development server the engine never hibernates: a fallback there is a pause with no fresh credential, and a task run is bounded by one slice.</li><li><strong>Two budgets do NOT refresh.</strong> Subrequests yield instead: they are counted across the slice and do not reset while it refreshes, and when the count passes half the resolved subrequest limit (5 000 of the 10 000 a task slice gets, or half a lower configured value) the next <code>step.do</code> boundary takes the fallback yield rather than refreshing, and the fresh slice resets the count. CPU is the other: a task slice gets 5 minutes of active CPU, and a step that exhausts it is killed by the runtime. The run does <strong>not</strong> recover from that kill — it fails, with the kill as its error — and the ceiling is per slice, not per step: a refreshed slice keeps counting. So a step&#39;s own body must fit in 5 minutes of CPU, though its wall clock may be far longer, and a run whose steps together need more than 5 minutes of CPU must end the slice between them — a <code>step.sleep</code> past the engine&#39;s five-minute grace period hibernates it, and the wake starts a fresh CPU budget.</li><li><strong>The rate ceiling counts starts, not resumes.</strong></li><li><strong>The output ceiling and <code>outputSchema</code> apply once</strong>, to the final return value.</li></ul><p>So <code>step.sleep</code> between chunks is for waiting, not for budget: chunk a long job by steps, and sleep when there is something to wait for. A refresh does not count toward the engine&#39;s maximum number of steps per run, and neither does a fallback yield — <code>step.sleep</code> and <code>step.sleepUntil</code> are excluded from that allowance, and only your <code>step.do</code> calls count.</p><p>A task run&#39;s status answer says how its slice is doing: a <code>slice</code> block with when the slice started, its 12-hour <code>ceilingAt</code>, <code>refreshCount</code> and <code>lastRefreshAt</code>, and — once the slice ended — <code>settledAt</code> and <code>settledStatus</code>. <code>primitive functions runs</code> shows the same count in its <code>REFRESHES</code> column, so whether a long run is refreshing or falling back is visible at a glance.</p><h3 id="when-a-run-s-status-settles" tabindex="-1">When a run&#39;s status settles <a class="header-anchor" href="#when-a-run-s-status-settles" aria-label="Permalink to “When a run&#39;s status settles”">​</a></h3><p><strong>A task run settles its own row as it ends.</strong> The same settlement that writes the invocation record <code>primitive functions logs</code> prints writes the run&#39;s <code>status</code>, <code>endedAt</code> and <strong><code>output</code></strong>, so <code>functions runs</code>, <code>functions logs</code>, <code>getStatus</code> and <code>runs wait</code> agree about a run <strong>without being polled</strong> and without waiting for anything. You never need a staleness timeout of your own: a run that finished a second ago reads finished, and one that finished a day ago and was never polled reads the same — with the value it returned.</p><p><strong><code>running</code> means the run is live, or not yet known to have ended.</strong> A run executing, hibernating in <code>step.sleep</code> or waiting out a step retry reads <code>running</code>, and nothing that merely looks at it writes to it. That includes a run the engine is retrying after a <a href="#what-resets-a-running-task">platform deploy reset it</a>: the reset is not a failure and the row is never settled for one, so such a run reads <code>running</code> through the five minutes before the engine picks it up again, and <code>slice.resets</code> is what says it happened.</p><p><strong>The <code>slice</code> block is how you tell those two apart.</strong> <code>settledAt</code> is null while the slice is going and set, with <code>settledStatus</code>, once it ended. Read it one way only: a <strong>settled</strong> slice is proof the slice ended, and an <strong>unsettled slice is not proof that the run is live</strong> — the platform writes the invocation record before it settles the slice record, and a settlement it could not write down is logged rather than retried. So treat a <code>running</code> row with an unsettled slice as &quot;not known to have ended&quot;, never as &quot;definitely still going&quot;.</p><p><strong>A <code>running</code> row whose run really did end is repaired on read.</strong> If a run&#39;s row is ever left behind — a start that died between writing its row and creating its instance, a settlement the platform could not write — the next read of it repairs it: <code>primitive functions runs</code>, <code>getStatus</code>, <code>runs wait</code> and the hard-delete scan each reconcile the run once, asking the engine first and then the platform&#39;s own records (the slice record, then the run&#39;s <strong>invocation record</strong>, which lives seven days). A run that ended is settled from what was written down about it, never guessed at.</p><p>Two things are <strong>fallbacks</strong> and nothing more: polling a run, and the platform&#39;s own 60-second sweep. Both still work, and neither is what makes a finished run read finished.</p><p>There is one deliberate exception, and it is why a healthy start is never mistaken for a lost one: a run whose row was written in the last five minutes and about which nothing has been recorded yet is treated as <strong>still starting</strong>. It is answered exactly as stored, it is never written, and a hard delete of its function is still refused — the run is about to load that function&#39;s code.</p><h3 id="when-a-run-fails" tabindex="-1">When a run fails <a class="header-anchor" href="#when-a-run-fails" aria-label="Permalink to “When a run fails”">​</a></h3><p>A run that <strong>failed</strong> is an answer, not an error: <code>getStatus</code> and <code>waitFor</code> (<a href="./server-functions-invoking.html#starting-one-and-polling-it">Starting one and polling it</a>) resolve with <code>status: &quot;failed&quot;</code> rather than throwing, so branch on the status. Why it failed rides on the one structured <code>error</code>:</p><ul><li><code>error.message</code> — what went wrong, and <strong>where a platform refusal&#39;s code is</strong>: the message leads with it (<code>OUTPUT_SCHEMA_VIOLATION: …</code>, <code>OUTPUT_TOO_LARGE: …</code>), so match on the prefix. An <strong>engine</strong> failure is the exception: its message is the engine&#39;s own text, and its code is on <code>error.code</code> — see <a href="#run-error-codes">Run error codes</a>.</li><li><code>error.code</code> — the platform&#39;s classification of this failure, from the closed set in <a href="#run-error-codes">Run error codes</a>. The same value the run row carries as <code>run.errorCode</code>. Branch on this rather than on the message.</li><li><code>error.name</code> — the error&#39;s own name, <em>not</em> the platform&#39;s code. Your thrown error&#39;s class name (<code>Error</code> for a plain <code>throw new Error(…)</code>) where the engine preserved it, and <strong>absent for a failure read off the persisted row</strong> — which is the ordinary case once a run has settled, because the platform keeps only the message there. Treat it as a hint.</li><li><code>error.details</code> — anything else the platform sent, when it sent any.</li></ul><p><code>error</code> is absent on a run that has not failed.</p><h3 id="run-error-codes" tabindex="-1">Run error codes <a class="header-anchor" href="#run-error-codes" aria-label="Permalink to “Run error codes”">​</a></h3><p>A failed run carries a <strong>code</strong> beside its message: <code>run.errorCode</code> on the run row, and <code>error.code</code> on the function run status both clients read. It is a closed set the platform owns, so you can branch on it rather than matching prose.</p><p>Two of them are about your handler, five are about the platform&#39;s own limits, and six — the <code>ENGINE_*</code> family — are about the engine that ran your code. <strong>Nothing in your code caused an <code>ENGINE_*</code> failure.</strong> You can branch on the whole family with a prefix check.</p><table tabindex="0"><thead><tr><th>Code</th><th>What happened</th><th>What to do</th></tr></thead><tbody><tr><td><code>FUNCTION_THREW</code></td><td>Your handler threw.</td><td>Read <code>error.message</code> and the run&#39;s logs (<code>primitive functions logs &lt;key&gt; --run &lt;runId&gt;</code>).</td></tr><tr><td><code>FUNCTION_NO_HANDLER</code></td><td>The entry file exported no callable default.</td><td>Export a default function from the file <code>entry</code> names.</td></tr><tr><td><code>FUNCTION_TIMEOUT</code></td><td>The invocation did not finish inside its budget.</td><td>Shorten the work, or run it as a task (<code>functions start</code>) where the budget is per slice.</td></tr><tr><td><code>FUNCTION_INPUT_INVALID</code></td><td>The input did not match the declared <code>inputSchema</code>.</td><td>Fix the caller&#39;s input, or the schema.</td></tr><tr><td><code>FUNCTION_OUTPUT_INVALID</code></td><td>A webhook delivery&#39;s run returned output that did not match <code>outputSchema</code>. (An HTTP invoke refusing its output answers <code>OUTPUT_SCHEMA_VIOLATION</code> in the envelope instead.)</td><td>Fix the returned value, or the schema.</td></tr><tr><td><code>OUTPUT_SCHEMA_VIOLATION</code></td><td>A task run&#39;s output did not match <code>outputSchema</code>.</td><td>Fix the returned value, or the schema. The message names the failing paths.</td></tr><tr><td><code>OUTPUT_NOT_SERIALIZABLE</code></td><td>The returned value is not JSON.</td><td>Return plain data — no functions, no cycles, no <code>undefined</code> where a value is required.</td></tr><tr><td><code>OUTPUT_TOO_LARGE</code></td><td>The output exceeds the 1 MiB platform ceiling.</td><td>Page the result, or write it to a database and return a handle.</td></tr><tr><td><code>FUNCTION_DELETED</code></td><td>The function was hard-deleted while the run was starting.</td><td>Nothing to retry: the stored bundle the run pinned is gone.</td></tr><tr><td><code>FUNCTION_RUNTIME_REFUSED</code></td><td>The code refused the runtime it was handed (<code>assertRuntime</code>), or a request invocation&#39;s <code>step</code> was asked for a wait its budget cannot cover.</td><td>Call it the other way — <code>invoke</code> for a request, <code>start</code> for a task.</td></tr><tr><td><code>ENGINE_ISOLATE_EVICTED</code></td><td>A platform component the invocation depended on went away mid-call, or the isolate was reset for memory.</td><td>Nothing in your code caused it. To run the work again, start a <strong>new</strong> run with a new <code>runKey</code> and the same input — <a href="#starting-a-task-run-from-a-function">a repeated key replays the failed run</a> and executes nothing — and make any side effect the failed attempt already performed idempotent in your own code. Report a repeat to the platform.</td></tr><tr><td><code>ENGINE_CODE_UPDATED</code></td><td>A platform deploy reset the engine while the attempt ran.</td><td>Nothing in your code caused it. Start a <strong>new</strong> run with a new <code>runKey</code> and the same input; <a href="#starting-a-task-run-from-a-function">a repeated key replays</a> and executes nothing, and side effects the failed attempt performed need your own idempotency.</td></tr><tr><td><code>ENGINE_STORAGE_ERROR</code></td><td>The engine&#39;s storage timed out or failed and the object was reset.</td><td>Nothing in your code caused it. Start a <strong>new</strong> run with a new <code>runKey</code> and the same input (<a href="#starting-a-task-run-from-a-function">a repeated key replays</a>), with your own idempotency over side effects already performed. Report a repeat to the platform.</td></tr><tr><td><code>ENGINE_INTERNAL_ERROR</code></td><td>The engine reported an internal error on the attempt.</td><td>Nothing in your code caused it. Start a <strong>new</strong> run with a new <code>runKey</code> and the same input (<a href="#starting-a-task-run-from-a-function">a repeated key replays</a>), with your own idempotency over side effects already performed. Report a repeat to the platform.</td></tr><tr><td><code>ENGINE_SLICE_DEADLINE</code></td><td>A platform call was refused because the slice&#39;s token deadline had passed.</td><td>The slice outlived its token. Look for a step that runs longer than the slice ceiling and split it.</td></tr><tr><td><code>ENGINE_INSTANCE_LOST</code></td><td>The engine no longer reports the instance, and nothing recorded how the run ended.</td><td><code>primitive functions logs &lt;key&gt; --run &lt;runId&gt;</code> shows what the platform did record. Start a new run with a new <code>runKey</code> if the work still needs doing.</td></tr></tbody></table><p>An engine failure&#39;s <code>errorMessage</code> is the <strong>engine&#39;s own text</strong>, not a code-leading sentence: for these the code is on <code>error.code</code> and <code>run.errorCode</code>, never in the message&#39;s prefix.</p><p>An attempt whose engine died before the platform could write its invocation record leaves no record at all — the writer died with it. For such an attempt the run row&#39;s code is the whole statement; there is nothing in <code>functions logs</code> to read.</p><h3 id="reading-a-task-run-step-by-step" tabindex="-1">Reading a task run step by step <a class="header-anchor" href="#reading-a-task-run-step-by-step" aria-label="Permalink to “Reading a task run step by step”">​</a></h3><p><code>functions runs</code> answers at the level of the run: status, the runtime it used, start, end, the failure code. For a task run that is rarely enough — the handler&#39;s work is in its named steps, and what you want to know is which one it is on, which ones already finished, and where the time went.</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> steps</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>Each step comes back with its name, its kind (<code>function.step</code> for a <code>step.do</code>, <code>function.sleep</code> for a <code>step.sleep</code> or <code>step.sleepUntil</code>, <code>function.event</code> for a <code>step.waitForEvent</code>), its status, the idle GAP before it, and its duration. A step that is <strong>still executing</strong> reads <code>running</code> with the time it has been running so far — including a run that is asleep, whose <code>function.sleep</code> step stays <code>running</code> until it wakes. A step that threw reads <code>failed</code> with its message.</p><p>The rows are written as the run goes, so they are readable while it is still in flight, and they survive the hibernation of a <code>step.sleep</code>. A resumed run replays its completed steps from the engine&#39;s memo rather than re-running them: each step keeps <strong>one</strong> row, with the timing of the slice that actually executed it. A step that a <a href="#what-resets-a-running-task">platform deploy reset</a> keeps that same <strong>one</strong> row too — it stays <code>running</code>, not <code>failed</code>, until the replay finishes it, and its duration then spans the reset rather than stopping at it. <code>--json</code> emits the same rows as JSON.</p><h3 id="ending-a-run-that-will-not-settle" tabindex="-1">Ending a run that will not settle <a class="header-anchor" href="#ending-a-run-that-will-not-settle" aria-label="Permalink to “Ending a run that will not settle”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> terminate</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>Stops the run where it is and settles its row <code>terminated</code>. Both halves happen, and in that order: stopping the instance without settling the row would leave every reader still reporting <code>running</code>, and settling the row without stopping the instance would leave your code executing — and the next status read reconciles the row against the engine and rolls it forward again.</p><p>A run whose instance is already <strong>gone</strong> — aged out of the engine&#39;s retention, or discarded with the document it was working on — still settles, because that is the run you came here to end. A stop that fails for any other reason is an <strong>error</strong>, and the run is left exactly as it was: the engine is the only thing that knows whether your code is still executing, and a row settled over a live run would both mislead you and remove the retry, since a settled run is reported rather than stopped. Run the command again.</p><p>A run that has <strong>already finished</strong> is reported, not overwritten: the command prints the status it settled at and terminates nothing. Its outcome is the only record of what it actually did — and that is asked of the engine, not only of the run row. A task run settles its own row as it ends, but a settlement that could not be written leaves the row stored <code>running</code> after the run finished; terminating such a run records what it really did instead of burying it under <code>terminated</code>.</p><p>Any step still open when the run is terminated is closed with it, however the run was stopped — this command or <code>client.functions.terminate</code> — so the trace of a run stopped mid-<code>step.sleep</code> does not go on reporting that sleep as <code>running</code>. Running the command against a run that is already over closes anything its trace still has open, so a trace left behind by an older stop is repaired by asking again.</p><h2 id="ceilings" tabindex="-1">Ceilings <a class="header-anchor" href="#ceilings" aria-label="Permalink to “Ceilings”">​</a></h2><p>Every function runs under fixed platform ceilings. <code>[function.limits]</code> may <strong>lower</strong> any of them and can never raise one — a config asking for more simply gets the platform value.</p><p>The two runtimes differ in what they are allowed to spend, which is most of why the runtime matters at all:</p><table tabindex="0"><thead><tr><th>Limit</th><th>Request runtime</th><th>Task runtime</th><th>Key</th></tr></thead><tbody><tr><td>CPU</td><td>5 000 ms per invocation</td><td>5 minutes per slice</td><td><code>cpuMs</code> (see below)</td></tr><tr><td>Outbound subrequests</td><td>128</td><td>10 000 per slice</td><td><code>subRequests</code></td></tr><tr><td>Wall clock</td><td>5 000 ms default, 30 000 ms ceiling</td><td>continuous up to 12 hours; every step begins with at least 5 minutes</td><td><code>timeoutMs</code> on the request (request only); see Budgets in a task run</td></tr><tr><td>— a <code>step.sleep</code> has to fit the budget</td><td>past it, the invocation fails <code>FUNCTION_RUNTIME_REFUSED</code></td><td>hibernates; hours and days are fine</td><td>— (start the function as a task)</td></tr></tbody></table><p>And the ones that are the same under both:</p><table tabindex="0"><thead><tr><th>Limit</th><th>Platform value</th><th>Key</th></tr></thead><tbody><tr><td>Invocations per minute, per function</td><td>1 200</td><td><code>ratePerMinute</code></td></tr><tr><td>Response size</td><td>1 MiB, kept on the run row</td><td>—</td></tr><tr><td>Values per <code>$in</code> / <code>$nin</code> list</td><td>1 000</td><td>—</td></tr></tbody></table><p>A task run&#39;s output is held to the same 1 MiB and is <strong>kept on the run row</strong> for its life, so it outlives the engine&#39;s instance. Over roughly 10 000 characters it is stored by reference and still answered whole; a <code>runs</code> page holding many large outputs may be shorter than the limit you asked for and continue with a cursor.</p><p>A burst past the rate ceiling answers <code>429</code> with <code>errorCode: &quot;FUNCTION_RATE_LIMITED&quot;</code> and a <code>Retry-After</code> header. Buckets are per app and per function, so one function&#39;s traffic never consumes another&#39;s. A slot is taken only once a call is going to run: a caller your <code>access</code> rule rejects, or a request your <code>inputSchema</code> refuses, consumes none of the budget. When the limiter itself cannot be reached the call is refused with <code>503</code><code>FUNCTION_RATE_UNAVAILABLE</code> rather than run unmetered.</p><p><code>cpuMs</code> is the one ceiling the platform does not hold to the number you write. It is enforced — a function past its budget is stopped, and the invocation answers <code>failed</code> — but the runtime adds an allowance of its own before it stops anything, measured at roughly <strong>two seconds</strong>. So <code>cpuMs = 50</code> and <code>cpuMs = 200</code> buy the same thing in practice, and neither stops your code in fifty or two hundred milliseconds. Lowering <code>cpuMs</code> still lowers the ceiling where the value is large enough to matter; below the allowance it is a declaration of intent rather than a cap you can rely on. If you need a function bounded tightly, bound it with <code>timeoutMs</code> on the request, which is wall clock and is held exactly.</p><p>The response-size ceiling is the platform&#39;s, not your sandbox&#39;s: the platform reads your response with a byte cap and measures the parsed <code>output</code> (and <code>error</code>) with its own encoder. An output over 1 MiB is <code>{&quot;status&quot;:&quot;failed&quot;, &quot;errorCode&quot;:&quot;OUTPUT_TOO_LARGE&quot;}</code> whatever the code inside the sandbox did — page your results or write them to a database and return a handle.</p><p>The wall-clock budget starts when the platform accepts the invocation, so loading a function&#39;s code the first time comes out of it — and the credential the sandbox carries expires on that same clock, never later than the answer the caller sees.</p><div class="warning custom-block"><p class="custom-block-title">What a timeout does, and does not, stop</p><p>When the budget runs out the caller gets <code>{&quot;status&quot;:&quot;timeout&quot;}</code> and the sandbox&#39;s request is aborted. The platform cannot <strong>kill</strong> a running sandbox — the runtime offers no such control — so a function that ignores the abort may keep executing until its <code>cpuMs</code> ceiling stops it.</p><p>What it can no longer do is act. The invocation&#39;s credential carries an absolute deadline, checked independently on every platform call, so a function that outlives its own invocation is holding something that has already stopped working: its late calls are denied and its side effects never land. Write functions that finish inside their budget; do not rely on work continuing after a timeout, and do not fear it either.</p><p>The same deadline bounds what your function starts outside the platform. An <code>ctx.integrations.call</code> is cut off at whichever comes first, the integration&#39;s own timeout or what is left of the invocation — including while its response body is still arriving, not merely while it waits for a reply — and answers <code>UPSTREAM_TIMEOUT</code>. A <code>ctx.prompts.run</code> whose provider call passes the deadline answers <code>PROMPT_UPSTREAM_TIMEOUT</code>, which is worth catching separately from an ordinary model failure: it means the budget ran out, not that the model did.</p></div>`,142)])])}const g=i(n,[["render",h]]);export{c as __pageData,g as default};
