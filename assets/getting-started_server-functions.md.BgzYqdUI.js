import{_ as i,c as e,o as t,aj as a}from"./chunks/framework.Id7s26SP.js";const k=JSON.parse('{"title":"Server Functions","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/server-functions.md","filePath":"getting-started/server-functions.md"}'),n={name:"getting-started/server-functions.md"};function h(o,s,r,l,d,p){return t(),e("div",null,[...s[0]||(s[0]=[a(`<h1 id="server-functions" tabindex="-1">Server Functions <a class="header-anchor" href="#server-functions" aria-label="Permalink to “Server Functions”">​</a></h1><p>A <strong>server function</strong> is TypeScript you author in your config tree and push to the platform. It lives beside your prompts and integrations as config-as-code: a <code>functions/&lt;key&gt;.toml</code> file states the gate and the entry point, the code sits next to it, and <code>primitive config push</code> builds and ships both.</p><p>This page covers writing, pushing, monitoring and operating a function. Three more pages cover the rest:</p><ul><li><a href="./server-functions-invoking.html">Invoking and Triggers</a> — every way to call a function: a client invoke, a task start, a webhook and a cron schedule.</li><li><a href="./server-functions-runtimes.html">Tasks and Requests</a> — the two runtimes a function runs under, and how a task run behaves: steps, resets, budgets, run status and run error codes.</li><li><a href="./server-functions-context.html">The Function Context</a> — the platform API a function calls through <code>ctx</code>: records, documents, prompts, secrets, channels, email and outside services.</li></ul><h2 id="authoring-a-function" tabindex="-1">Authoring a function <a class="header-anchor" href="#authoring-a-function" aria-label="Permalink to “Authoring a function”">​</a></h2><p>Scaffold the file, then write the code its <code>entry</code> names:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> create</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> function</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> greet</span></span></code></pre></div><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># primitive/dev/functions/greet.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;greet&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">description = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Says hello&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># The TypeScript entry point, relative to the config tree root.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/greet/index.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># CEL gate. Required before code can be pushed — a function with no gate fails</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># closed at invoke. &quot;true&quot; allows any authenticated caller. The gate is the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># authorization: inside the function, the code acts as the system.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># cpuMs, subRequests, ratePerMinute — each may lower a platform ceiling, never</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># raise one. The wall-clock budget is a request field, not a config key.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">limits</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cpuMs = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2000</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// primitive/dev/functions/greet/index.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { salute } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;./greeting.js&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { message: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">salute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(input.name </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;world&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Run <code>primitive config fields function</code> for the full key list with types and defaults.</p><h2 id="pushing" tabindex="-1">Pushing <a class="header-anchor" href="#pushing" aria-label="Permalink to “Pushing”">​</a></h2><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span></span></code></pre></div><p><code>config push</code> builds the bundle with esbuild, inlines your npm dependencies, and stores the built bundle <strong>together with</strong> the authored TOML bytes and every source file it read. <code>primitive-functions</code> is the one import left external: the platform supplies it at invoke time, so your function always runs against the SDK the platform is running.</p><p>Every push that changes anything creates a new <strong>immutable version</strong> and repoints the function at it. &quot;Anything&quot; means the whole envelope, not just the compiled output: a comment-only or line-ending-only edit to a source file makes a new version too, so <code>primitive config pull</code> on a fresh clone always hands back the bytes you wrote — comments, key order and all. Pointing a function back at an earlier version is covered under <a href="#versions-and-rollback">Versions and rollback</a>.</p><p>The same push also typechecks your sources against the declarations it generates, and refuses a function that does not compile — see <a href="#typechecked-before-it-ships">Typechecked before it ships</a>.</p><h3 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to “Imports”">​</a></h3><ul><li><strong>Relative and absolute imports must stay inside the config tree.</strong> An entry or an import that resolves outside it — through a symlink included — is refused at push, so a push can never upload a file your tree does not contain.</li><li><strong>npm packages are imported by name and inlined into the bundle.</strong> They resolve through Node like any other import, and <code>node_modules</code> content is never stored as your source or written back by <code>config pull</code>.</li></ul><p>A missing entry file or a build error names the file and the message, and nothing is pushed for that function — the rest of your config tree still applies.</p><h3 id="what-a-function-touches-is-documented-not-declared" tabindex="-1">What a function touches is documented, not declared <a class="header-anchor" href="#what-a-function-touches-is-documented-not-declared" aria-label="Permalink to “What a function touches is documented, not declared”">​</a></h3><p><code>config push</code> collects a <strong>manifest</strong> with every version: the queries it registered, the models its code names — through the typed handle, the direct records API and the document records family alike — and the families of <code>ctx.api</code> and the ctx helpers it reaches. <code>primitive functions get</code> shows it under MANIFEST, and a model name the scan could not resolve (a computed <code>modelName</code>) is marked &quot;resolved at run time&quot; rather than silently dropped.</p><p>It is <strong>documentation, never authorization</strong>: a reviewer reads it to see what a change reaches, and nothing on the platform decides anything from it.</p><h3 id="what-config-push-runs" tabindex="-1">What <code>config push</code> runs <a class="header-anchor" href="#what-config-push-runs" aria-label="Permalink to “What config push runs”">​</a></h3><p>Collecting the manifest means <strong>running your tree&#39;s module scope</strong>: <code>config push</code> builds the bundle a second time with <code>primitive-functions</code> replaced by a recording stub, and executes it in a separate Node process with a scrubbed environment, your config tree as its working directory, and a hard timeout. It is the same trust you extend by running the project&#39;s own build — but it is worth knowing before you push a project you did not write. The models and families the manifest lists come from a static scan of the built bundle beside that run, so a literal <code>modelName</code> in a direct records call is recorded with no registration in sight.</p><p>A push whose collection FAILS is refused, naming the error: the manifest is part of the version&#39;s identity, so there is no &quot;push now, collect later&quot;.</p><h2 id="the-gate-runs-on-every-call" tabindex="-1">The gate runs on every call <a class="header-anchor" href="#the-gate-runs-on-every-call" aria-label="Permalink to “The gate runs on every call”">​</a></h2><p><code>access</code> is evaluated on <strong>every HTTP call</strong>, and it fails closed:</p><ul><li>a caller the expression denies gets <code>403</code> with <code>errorCode: &quot;FUNCTION_ACCESS_DENIED&quot;</code>;</li><li>a function with <strong>no</strong> <code>access</code> expression denies everyone (which is why push refuses to ship code for one);</li><li>app owners and admins bypass it;</li><li>a <a href="./server-functions-invoking.html#triggers">trigger</a> fire and a <a href="./server-functions-runtimes.html#starting-a-task-run-from-a-function">nested start</a> do not consult it — the gate governs the HTTP route. A function that only its triggers or another function should run declares <code>access = &quot;false&quot;</code>, a gate no member passes, which closes that route.</li></ul><p>The gate is checked <strong>before</strong> any answer about the function&#39;s operational state, so a caller it rejects cannot tell a disabled function from a never-pushed one from a live one — and cannot map your app&#39;s functions by probing keys.</p><p><strong>The gate is the authorization.</strong> Once a caller is through it, the code runs with the app&#39;s own authority (see <a href="./server-functions-context.html#calling-the-platform-from-a-function">Calling the platform from a function</a>): who may invoke is decided here, once, by the expression you reviewed, and nothing inside the function asks the question again. Write the gate for what the function can do, not for who happens to be the caller of the first line.</p><h2 id="typed-from-the-declaration" tabindex="-1">Typed from the declaration <a class="header-anchor" href="#typed-from-the-declaration" aria-label="Permalink to “Typed from the declaration”">​</a></h2><p><code>config push</code> writes the declarations that type the database handle, the document handle and the SDK into your config tree&#39;s <code>functions/</code> directory, beside your sources, along with a <code>functions/tsconfig.json</code> that includes them. There is nothing to wire up: open the folder in your editor and the types are there. <code>config diff</code> tells you when a schema change has left them stale, and the next <code>config push</code> refreshes them. None of the generated files is part of what gets pushed.</p><p>Push does not just write them: it <strong>holds your code to them</strong>, so a model, a schema or an SDK type that moved under a function is a refused push rather than a run-time surprise (see <a href="#typechecked-before-it-ships">Typechecked before it ships</a>).</p><p>In CI, ask the question directly — it exits non-zero when the generated files are out of date or the tsconfig no longer loads them:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> codegen</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --check</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # or without --check to regenerate</span></span></code></pre></div><p>The <code>tsconfig.json</code> is yours to adjust — push writes it once and then leaves your settings alone. The two things it keeps are the ones that make the types load at all: the <code>primitive-functions</code> path mapping and the generated declarations being in the program. If either goes missing, <code>config diff</code> reports the file and the next push puts them back, alongside everything else you have written in it.</p><p>A function&#39;s <code>inputSchema</code> and <code>outputSchema</code> are enforced at every door — an HTTP invoke, a webhook or cron fire — and since the file declares them, the code and the caller can be typed from them too. On every push (and with <code>primitive functions codegen</code>), the CLI renders more files from <code>functions/&lt;key&gt;.toml</code>: the typed client invokers your app calls, covered under <a href="./server-functions-invoking.html#typed-client-invokers">Typed client invokers</a>, and the declaration that types the function&#39;s own code:</p><ul><li><p><strong><code>functions/primitive-function-types.d.ts</code></strong> — <code>&lt;Key&gt;Input</code> and <code>&lt;Key&gt;Output</code> for every function, from its declared schemas, plus a <code>FunctionSchemas</code> augmentation keyed by function key. This is what types the <strong>keyed</strong> <code>defineFunction</code>:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// primitive/dev/functions/greet/index.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// \`input\` is GreetInput; the return is checked against GreetOutput; a key</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// this tree does not declare is a compile error, and a key that names</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// another file&#39;s function is refused at push.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;greet&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { greeting: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`hello \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The unkeyed <code>defineFunction(async (input: MyInput, ctx) =&gt; …)</code> still works, with the types you write. In a tree that has not pushed yet the keyed form compiles with <code>any</code>, so nothing is blocked on the first push.</p></li></ul><p>The same push also types <code>ctx.prompts.run</code> from your prompts&#39; declared output schemas — see <a href="./server-functions-context.html#typed-parsed-output">Typed, parsed output</a>.</p><h3 id="typechecked-before-it-ships" tabindex="-1">Typechecked before it ships <a class="header-anchor" href="#typechecked-before-it-ships" aria-label="Permalink to “Typechecked before it ships”">​</a></h3><p>The same push that writes the declarations above <strong>typechecks your sources against them</strong>, and refuses the function with the compiler&#39;s own diagnostics when they do not hold:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>✗ 1 change(s) could not be applied:</span></span>
<span class="line"><span>  FAILED function: greet</span></span>
<span class="line"><span>    function &#39;greet&#39; does not typecheck against the declarations \`config push\` generates:</span></span>
<span class="line"><span>      greet/index.ts(4,9): error TS2322: Type &#39;string&#39; is not assignable to type &#39;number&#39;.</span></span>
<span class="line"><span>      Reproduce with \`tsc -p functions/tsconfig.json --noEmit\`; push with --no-typecheck to ship it unchecked.</span></span></code></pre></div><p>esbuild erases types, so a bundle that builds says nothing about whether the code still holds: a function that dereferences <code>ctx.user</code> without checking for <code>null</code> builds cleanly and throws on the request path. The check is the compiler your editor is already running — the tree&#39;s own <code>functions/tsconfig.json</code> decides the options, and reproducing a refusal by hand is <code>tsc -p functions/tsconfig.json --noEmit</code>.</p><p>It is per function: a broken one is refused by name and everything else in the tree still lands. <code>--dry-run</code> reports the same diagnostics and ships nothing. <code>primitive config push --no-typecheck</code> skips the check, for the push you need out before the types are fixed.</p><h2 id="recording" tabindex="-1">Recording <a class="header-anchor" href="#recording" aria-label="Permalink to “Recording”">​</a></h2><p>Every HTTP invocation writes one <code>function.invoke</code> analytics event, attributed to the <strong>caller</strong> — the signed-in user who made the request, never the app itself, though the code ran as the app. It carries the function key, the version that ran, the terminal status and the duration. A trigger fire has no caller and writes no per-user event; its record is the run row.</p><p>The event is a COUNT — it carries no message and no error — so it answers &quot;how often&quot; and never &quot;why&quot;. For why, read the invocation records below. A failed invocation&#39;s event does report <code>outcome: &quot;error&quot;</code> through the shared inspection contract, so failures are countable.</p><h2 id="debugging-a-failing-function" tabindex="-1">Debugging a failing function <a class="header-anchor" href="#debugging-a-failing-function" aria-label="Permalink to “Debugging a failing function”">​</a></h2><p>Every invocation the platform dispatched toward the sandbox leaves a record, and <code>console.log</code> is where you read it back:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              # newest first</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --json</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # the shared inspection items</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --follow</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # tail as invocations happen</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --limit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 50</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --cursor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">curso</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">r</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>Reading one record by the <code>invocationId</code> an invoke answered with (<code>--invocation</code>), and one task run&#39;s records (<code>--run</code>), is covered in <a href="./inspecting-and-debugging.html#triaging-a-failing-function">Triaging a failing function</a>. <code>--invocation</code> names one record, so it cannot be combined with <code>--run</code>, <code>--follow</code>, <code>--cursor</code> or <code>--limit</code>, which all shape a listing. <code>--run</code> narrows to one run&#39;s records and cannot be combined with <code>--follow</code>, because a run&#39;s records are a bounded set and a tail walks the whole index.</p><p>How <code>--follow</code> tails — its polling, its NDJSON output, and how it still shows a slow invocation that settled after a faster one — is covered in <a href="./inspecting-and-debugging.html#watching-live-follow">Watching live</a>.</p><p>An id that names nothing, one belonging to another function, and one whose seven days are up all answer the same not-found: the platform does not say whether a record it will not show you ever existed.</p><p>A record carries what the invocation printed (<code>console.log</code>/<code>info</code>/<code>debug</code>/ <code>trace</code> as stdout, <code>warn</code>/<code>error</code> as stderr), the thrown error with its code and a bounded stack, the runtime it ran under (<code>request</code> or <code>task</code>), and the correlation keys: the app, the function, the config version that ran, the run id for a trigger fire or a task run, and what triggered it. <strong>Records are kept for seven days.</strong></p><h3 id="output-from-inside-a-step" tabindex="-1">Output from inside a step <a class="header-anchor" href="#output-from-inside-a-step" aria-label="Permalink to “Output from inside a step”">​</a></h3><p>A line printed <strong>inside a <code>step.do</code> body</strong> is captured like any other, and it carries which step wrote it: the step&#39;s name, which call of that name it was (counting from 0 across every named step call in the slice — <code>do</code>, <code>sleep</code>, <code>sleepUntil</code> and <code>waitForEvent</code> alike, so a label lines up with the row of the same name in <code>primitive functions runs steps</code>), and which attempt of that body it was. A line printed <strong>between</strong> steps carries none of the three, which is how you tell them apart.</p><p>If a body <strong>throws</strong>, the platform records the throw under that step as an <code>err</code> line — <code>step &quot;&lt;name&gt;&quot; attempt &lt;n&gt; threw: &lt;message&gt;</code> — so a failed run shows where it failed as well as what it printed on the way. The throw itself is unchanged: the engine still sees it, still retries it if you configured retries, and still fails the run if it runs out.</p><p>An attempt counts the times that body actually ran, from 1, and resets for the next call of the name. A retried body&#39;s lines therefore sit under one name and one occurrence with ascending attempts, and a step the engine answers from its memo on a replay runs no body and records nothing new.</p><p><code>--run</code> prints the run&#39;s step trace and then, per record oldest first, the record&#39;s summary and its lines under step headers:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fn_01ABC</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 01M2M8...</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>STEP    STATUS     DURATION</span></span>
<span class="line"><span>load    completed  12ms</span></span>
<span class="line"><span>charge  completed  84ms</span></span>
<span class="line"><span>ship    failed     9ms</span></span>
<span class="line"><span></span></span>
<span class="line"><span>04:39:45  failed  http  cfg_01XYZ  01M2M8...  FUNCTION_THREW  the depot is closed</span></span>
<span class="line"><span>  +0ms  out  between: starting</span></span>
<span class="line"><span>  step load #0 attempt 1</span></span>
<span class="line"><span>    +4ms  out  loading the order</span></span>
<span class="line"><span>  step charge #0 attempt 1</span></span>
<span class="line"><span>    +19ms  out  charging the card</span></span>
<span class="line"><span>  step ship #0 attempt 1</span></span>
<span class="line"><span>    +103ms  out  about to ship</span></span>
<span class="line"><span>  ! +104ms  err  step &quot;ship&quot; attempt 1 threw: the depot is closed</span></span></code></pre></div><p>Lines print in the order they were <strong>printed</strong>, with stdout and stderr interleaved as they happened — a <code>console.error</code> immediately followed by a <code>console.log</code> comes back in that order even though the record stores the two streams separately. A record whose output hit a cap ends with a visible marker, <code>… truncated: the platform&#39;s per-record cap dropped later lines</code>, so a shortened record never reads as a complete one.</p><p>Every door writes one — an HTTP invocation, a webhook delivery, a cron fire, and a task run&#39;s settling slice. A timed-out invocation writes one too, carrying what the function printed <strong>before</strong> it hung, which is usually the whole story.</p><p>A <strong>gate refusal</strong> writes none. A 403 from the access rule, a 404 for an unknown key, a 409 for a version that was never pushed, a 400 from the input schema and a 429 from the rate ceiling are all answered in the HTTP response and debugged from there; they never reached your code, and rows a caller can make on demand are rows a caller controls.</p><p>A function can read its own records too — <code>ctx.api.functions.logs({ functionKey, limit })</code> — and so can the admin API. Both are <strong>owner and admin only</strong>.</p><div class="warning custom-block"><p class="custom-block-title">Secrets are redacted, best-effort</p><p>A value <code>ctx.secret()</code> returned is replaced with <code>[REDACTED:&lt;NAME&gt;]</code> in the captured lines, in the lines forwarded to the live console, and in the error message and stack, on both sides of the sandbox boundary. It is best-effort by nature: a secret your code transformed before printing — sliced, encoded, interpolated into a signature — is not detectable. Do not print credentials.</p></div><p><strong>Task runs have a narrower guarantee, and it is about SLICES, not about steps.</strong> A task run&#39;s record is written when the slice that SETTLES it finishes, and it carries everything that slice printed — between steps and inside step bodies alike. What it cannot carry is what an EARLIER slice printed: a slice that hibernated at a <code>step.sleep</code> never settles, so it writes no record, and a <code>step.do</code> body that ran in it does not re-execute on replay. Output from before a hibernation is <strong>not recoverable</strong>. The same applies to a fallback yield the platform takes between steps when a slice runs low (see <a href="./server-functions-runtimes.html#budgets-in-a-task-run">Budgets in a task run</a>), which is a hibernation like any other sleep.</p><p>So a run that sleeps leaves one record per slice that settled, and a run that never sleeps leaves one record with everything in it. If you need progress from across a long run, print it in the slice that settles or write it as data — and note that a local development server never hibernates, so a task run there is one slice and one record however many times it sleeps.</p><p>Three markers are worth knowing. <code>truncated</code> says a line or the whole channel hit its cap (2 KiB per line, 16 KiB and 256 entries per invocation); the invocation itself is never failed for logging too much. <code>logsUnavailable</code> says the platform could not retrieve the buffer at all — an evicted isolate, or a dispatch refused before any code ran — which is different from a function that printed nothing. An attempt that ended in the engine before the handler printed anything carries <code>endedIn: &quot;engine&quot;</code> on its record, and <code>functions logs --run</code> prints it as <code>ended in the engine — no handler output</code> (or <code>logs unavailable</code> / <code>output suppressed</code> when a marker applies). <code>contentSuppressed</code> says the platform could not load <strong>every</strong> secret the version declares when it came to write the record — the store did not answer, or a declared <code>secret:&lt;NAME&gt;</code> has no value in this environment — so it kept the correlation and the status and dropped the console and the error fields rather than publish them unredacted. A declared secret that is not provisioned suppresses every record of that version, including the <code>FUNCTION_SECRET_NOT_FOUND</code> it would otherwise show you: provision the secret, or drop the declaration, and invoke again.</p><p>Records <strong>outlive the function</strong>. Archiving a function does not remove its records, and both read surfaces keep answering for it until the seven days are up — which is the point, since a function is usually archived because it was failing.</p><h2 id="operating-a-function" tabindex="-1">Operating a function <a class="header-anchor" href="#operating-a-function" aria-label="Permalink to “Operating a function”">​</a></h2><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # keys, status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # …plus the active version, its capabilities, its manifest and its triggers</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> configs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # every version this function&#39;s pushes made, newest first</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> activate</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">config-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # point it at one of them (rollback); makes no version</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # what its webhook and schedules fired</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> steps</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # one task run, step by step</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> terminate</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # end a run that will not settle</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # what it printed, and what it threw</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> disable</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> enable</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> archive</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>Reading a task run step by step, and ending one that will not settle, are covered on Tasks and Requests — see <a href="./server-functions-runtimes.html#reading-a-task-run-step-by-step">Reading a task run step by step</a> and <a href="./server-functions-runtimes.html#ending-a-run-that-will-not-settle">Ending a run that will not settle</a>.</p><h3 id="running-one" tabindex="-1">Running one <a class="header-anchor" href="#running-one" aria-label="Permalink to “Running one”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> invoke</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;n&quot;:21}&#39;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # request runtime: run it inside the request, print the result</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;n&quot;:21}&#39;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # task runtime: start a run, print its id</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --wait</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # …and wait for it</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wait</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # wait for a run already started</span></span></code></pre></div><p><code>invoke</code> and <code>start</code> take the <strong>key</strong> — what you wrote in <code>functions/&lt;key&gt;.toml</code> — because that is what the public route takes. <code>runs</code>, <code>runs wait</code>, <code>runs steps</code>, <code>runs terminate</code> and <code>logs</code> take the <strong>function id</strong>, and every verb prints the ids it produced with the command that takes each one underneath, so you never have to look one up:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Status  completed</span></span>
<span class="line"><span>Output</span></span>
<span class="line"><span>{</span></span>
<span class="line"><span>  &quot;doubled&quot;: 42</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>Ran as 01K3XV… (owner)</span></span>
<span class="line"><span>Function ID 01K3XW…</span></span>
<span class="line"><span>  → primitive functions logs 01K3XW…</span></span>
<span class="line"><span>Invocation ID 01K4ANRRG0Q7VZ3M8T6XW2H5NB</span></span>
<span class="line"><span>  → primitive functions logs 01K3XW… --invocation 01K4ANRRG0Q7VZ3M8T6XW2H5NB</span></span></code></pre></div><p><strong>Both verbs work on every function.</strong> <code>invoke</code> posts to the invoke route and runs the function inside the request; <code>start</code> posts to <code>/start</code> and runs it as a task. There is no wrong verb to be refused — unless the function&#39;s own code says so with <a href="./server-functions-runtimes.html#assertruntime"><code>assertRuntime</code></a>, and then the refusal comes back as a <code>failed</code> invocation with <code>errorCode: &quot;FUNCTION_RUNTIME_REFUSED&quot;</code> rather than as a mistake the CLI could have caught.</p><p><strong>Exit codes</strong>, so a script can branch on them:</p><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th></tr></thead><tbody><tr><td><code>0</code></td><td>The invocation completed, or the run you waited for completed.</td></tr><tr><td><code>1</code></td><td>It failed, timed out, was terminated, or the platform refused the call.</td></tr><tr><td><code>124</code></td><td><code>runs wait</code> spent its budget with the run still going. It prints the command that resumes the wait.</td></tr><tr><td><code>130</code></td><td>Ctrl-C. Same resume line; a second Ctrl-C exits at once.</td></tr></tbody></table><p><code>--timeout &lt;seconds&gt;</code> is the request budget for <code>invoke</code> (the platform clamps at 30 s) and the WAIT budget for <code>runs wait</code> and <code>start --wait</code>, which defaults to 900.</p><h3 id="who-it-runs-as" tabindex="-1">Who it runs as <a class="header-anchor" href="#who-it-runs-as" aria-label="Permalink to “Who it runs as”">​</a></h3><p>By default, <strong>your own app user</strong> — the one the platform provisions for an admin on first use. The output says so (<code>Ran as &lt;user-id&gt; (&lt;role&gt;)</code>), and the role matters: owners and admins <strong>bypass a function&#39;s <code>access</code> expression</strong>, so your own invocation does not exercise the gate. Use <code>--user</code> when you want it exercised.</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> invoke</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --user</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">user-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # as that app user</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> invoke</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --as</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> system</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # with no caller at all</span></span></code></pre></div><p><code>--user</code> mints a ten-minute API token for that user, makes the call with it, and revokes it afterwards — on every path, including Ctrl-C. The token&#39;s value is never printed. If the revoke itself fails you get a warning naming the token and <code>primitive tokens revoke &lt;id&gt;</code>; the exit code is still the invocation&#39;s. Owner and admin only, enforced by the server.</p><p><code>--as system</code> runs the function the way a <strong>cron or webhook fire</strong> does: <code>ctx.user</code> is <code>null</code>, <code>ctx.trigger</code> is <code>{ kind: &quot;manual&quot;, userId: &lt;you&gt; }</code>, and the code carries the app&#39;s system authority. It is how you exercise a trigger-fired code path without faking the trigger. Owner and admin only, and a task started this way is keyed to the system exactly as a trigger-fired root is — <code>FIRED BY manual</code>, with you recorded as the initiator.</p><p>The two flags are mutually exclusive, and <code>--as</code> accepts only <code>system</code>.</p><div class="tip custom-block"><p class="custom-block-title">A task start mints your root document</p><p><code>functions start</code> needs a context document, and the app user the platform provisions for an admin has none until something creates one. The CLI asks for it — through the same idempotent route sign-in uses — so <code>start</code> works on a fresh app without you doing anything first. Pass <code>--context-doc-id</code> to name one yourself. <code>--as system</code> uses the synthetic <code>fn:&lt;function-id&gt;</code> context that every trigger-rooted run uses, and asks for nothing.</p></div><h3 id="versions-and-rollback" tabindex="-1">Versions and rollback <a class="header-anchor" href="#versions-and-rollback" aria-label="Permalink to “Versions and rollback”">​</a></h3><p>Every push whose code differs from <strong>every</strong> version this function already has makes a new version and points the function at it. List them newest first:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> configs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>CONFIG ID                   PUSHED            ACTIVE  CONTENT       ENVELOPE      TRIGGERS  BY</span></span>
<span class="line"><span>01K4C2MJ8Q1V7ZP3RN6TWXH5DA  2026-09-15 14:02  *       9f31ab7c2d04  4e08bb61a9f7  cron×2    01K3XV…</span></span>
<span class="line"><span>01K4BZ0PR5KC9WQ2HM4YD8GTNE  2026-09-15 11:47          71c4de05ba18  2a9f7c30e58d  cron×2    01K3XV…</span></span></code></pre></div><p>To go back to an earlier one, point the function at it:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> activate</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">config-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>That is a <strong>repoint, not a new version</strong>. Nothing is created, the version&#39;s own bytes are what they always were, and the function&#39;s version list is the same length afterwards.</p><p>What changes at the next call, and what does not:</p><ul><li><strong>Request calls and trigger fires use the activated version from the next call.</strong> They resolve the active version per call, so there is no window and nothing to restart.</li><li><strong>The activated version&#39;s own capabilities, limits and triggers apply.</strong> A version IS its declaration: roll back to one that declared a cron entry a later version dropped and that schedule is live again; roll back past a <code>secret:</code> capability and the function stops being able to read that secret.</li><li><strong>A task run already in flight finishes on the version it started on.</strong> Its code is pinned by content hash and reloaded from the same immutable bundle at every wake, so a rollback never changes what a running task is executing.</li></ul><p>Because a version IS its declaration, a rollback asks for that version&#39;s triggers back — and a trigger is admitted against the app, not against the version. If a trigger that version declared can no longer be admitted against the app, the activation is refused with the reason and <strong>nothing moves</strong>: the function keeps running whatever it was running. Resolve the refusal and activate again.</p><p>Two things follow from versions being content-addressed per function:</p><ul><li><strong>Pushing a tree that matches a version you already have re-activates that version and makes none.</strong> So after a rollback, pushing the unchanged tree puts the newest version back — which is how you undo the rollback without editing anything.</li><li><strong>Pushing an edited tree makes a new version and activates it</strong>, as always.</li></ul><p><code>primitive config pull</code> writes the <strong>active</strong> version&#39;s bytes, because the tree should describe what runs. On a function you have rolled back it says so, naming the newer version it did not write:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Wrote functions/greet.toml from the active version 01K4BZ0P…; a newer version</span></span>
<span class="line"><span>01K4C2MJ… (pushed 2026-09-15 14:02) exists — \`primitive functions activate\`</span></span>
<span class="line"><span>switches, \`config push\` makes the tree the newest</span></span></code></pre></div><p><code>primitive config diff</code> names the same situation rather than reporting an edit you did not make: <em>the tree matches version <code>&lt;newest&gt;</code>, which is not the active one (<code>&lt;active&gt;</code>)</em>.</p><p>The version each run and each invocation executed shows on <code>primitive functions runs</code>, <code>primitive functions logs</code> and the invocation log record, and the active version&#39;s id and content hash show on <code>primitive functions get</code> — beside the key, and with who activated it and when.</p><h3 id="disabling-and-archiving-a-function" tabindex="-1">Disabling and archiving a function <a class="header-anchor" href="#disabling-and-archiving-a-function" aria-label="Permalink to “Disabling and archiving a function”">​</a></h3><p><code>primitive functions disable</code> takes a function out of service — a call a gate admits answers <code>403 FUNCTION_DISABLED</code> — and <code>enable</code> puts it back. Availability is <strong>server-owned</strong>: <code>status</code> is not a TOML key, <code>config pull</code> never writes it, and <code>config push</code> never sends it. That cuts both ways deliberately — a <code>config push</code> to a <strong>disabled</strong> function updates its code and leaves it out of service, so shipping a fix never puts an endpoint back in service on its own.</p><p><code>archive</code> takes a function out of service without destroying it. The archived row goes on holding its key, pushing code to it is refused, and there is no un-archive. Reclaiming the key means a hard delete: run <code>primitive config pull</code> (which removes the tombstone&#39;s local files), then a confirmed <code>primitive config push --prune</code>, which destroys the function, every version and their stored bundles.</p><h3 id="deleting-a-function-with-live-runs" tabindex="-1">Deleting a function with live runs <a class="header-anchor" href="#deleting-a-function-with-live-runs" aria-label="Permalink to “Deleting a function with live runs”">​</a></h3><p>A hard delete destroys the stored bundles a sleeping run reloads when it wakes, so it is refused while any of a function&#39;s runs has not settled — including through <code>primitive config push --prune</code>. The refusal names the live runs. Wait for them, or terminate them, and retry. To stop new runs meanwhile, <strong>archive</strong> the function: an archived function keeps its bundles, so a run already in flight finishes on the version it pinned.</p><p>While a hard delete that got past that check is running, the function stops accepting work: an invoke answers <code>404</code> and a task start that was already in flight answers <code>409</code> with <code>errorCode: &quot;FUNCTION_DELETE_IN_PROGRESS&quot;</code> rather than beginning a run whose code is about to be destroyed.</p><h2 id="one-key-namespace-per-app" tabindex="-1">One key namespace per app <a class="header-anchor" href="#one-key-namespace-per-app" aria-label="Permalink to “One key namespace per app”">​</a></h2><p>A function&#39;s key is unique per app, case-insensitively. Pushing a function whose key another object in the app already holds fails naming the holder. Keys must be URL-path-safe, because a function key doubles as the path segment that addresses it.</p>`,111)])])}const g=i(n,[["render",h]]);export{k as __pageData,g as default};
