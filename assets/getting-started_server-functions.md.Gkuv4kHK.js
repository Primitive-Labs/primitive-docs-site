import{_ as i,c as a,o as e,aj as t}from"./chunks/framework.BlveriQe.js";const c=JSON.parse('{"title":"Server Functions","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/server-functions.md","filePath":"getting-started/server-functions.md"}'),n={name:"getting-started/server-functions.md"};function h(l,s,r,p,o,k){return e(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="server-functions" tabindex="-1">Server Functions <a class="header-anchor" href="#server-functions" aria-label="Permalink to “Server Functions”">​</a></h1><p>A <strong>server function</strong> is TypeScript you author in your config tree and push to the platform. It lives beside your prompts and integrations as config-as-code: a <code>functions/&lt;key&gt;.toml</code> file states the gate and the entry point, the code sits next to it, and <code>primitive config push</code> builds and ships both.</p><h2 id="authoring-a-function" tabindex="-1">Authoring a function <a class="header-anchor" href="#authoring-a-function" aria-label="Permalink to “Authoring a function”">​</a></h2><p>Scaffold the file, then write the code its <code>entry</code> names:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> create</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> function</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> greet</span></span></code></pre></div><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/functions/greet.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;greet&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">description = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Says hello&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># The TypeScript entry point, relative to the config tree root.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/greet/index.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># CEL gate. Required before code can be pushed — a function with no gate fails</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># closed at invoke. &quot;true&quot; allows any authenticated caller. The gate is the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># authorization: inside the function, the code acts as the system.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># cpuMs, subRequests, ratePerMinute — each may lower a platform ceiling, never</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># raise one. The wall-clock budget is a request field, not a config key.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">limits</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cpuMs = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2000</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// config/functions/greet/index.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { salute } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;./greeting.js&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { message: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">salute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(input.name </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;world&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Run <code>primitive config fields function</code> for the full key list with types and defaults.</p><h2 id="pushing" tabindex="-1">Pushing <a class="header-anchor" href="#pushing" aria-label="Permalink to “Pushing”">​</a></h2><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span></span></code></pre></div><p><code>config push</code> builds the bundle with esbuild, inlines your npm dependencies, and stores the built bundle <strong>together with</strong> the authored TOML bytes and every source file it read. <code>primitive-functions</code> is the one import left external: the platform supplies it at invoke time, so your function always runs against the SDK the platform is running.</p><p>Every push that changes anything creates a new <strong>immutable version</strong> and repoints the function at it. &quot;Anything&quot; means the whole envelope, not just the compiled output: a comment-only or line-ending-only edit to a source file makes a new version too, so <code>primitive config pull</code> on a fresh clone always hands back the bytes you wrote — comments, key order and all.</p><h3 id="typechecked-before-it-ships" tabindex="-1">Typechecked before it ships <a class="header-anchor" href="#typechecked-before-it-ships" aria-label="Permalink to “Typechecked before it ships”">​</a></h3><p>The same push that writes the declarations below <strong>typechecks your sources against them</strong>, and refuses the function with the compiler&#39;s own diagnostics when they do not hold:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>✗ 1 change(s) could not be applied:</span></span>
<span class="line"><span>  FAILED function: greet</span></span>
<span class="line"><span>    function &#39;greet&#39; does not typecheck against the declarations \`config push\` generates:</span></span>
<span class="line"><span>      greet/index.ts(4,9): error TS2322: Type &#39;string&#39; is not assignable to type &#39;number&#39;.</span></span>
<span class="line"><span>      Reproduce with \`tsc -p functions/tsconfig.json --noEmit\`; push with --no-typecheck to ship it unchecked.</span></span></code></pre></div><p>esbuild erases types, so a bundle that builds says nothing about whether the code still holds: a function that dereferences <code>ctx.user</code> without checking it — written before that field became nullable — builds cleanly and throws on the request path. The check is the compiler your editor is already running — the tree&#39;s own <code>functions/tsconfig.json</code> decides the options, and reproducing a refusal by hand is <code>tsc -p functions/tsconfig.json --noEmit</code>.</p><p>It is per function: a broken one is refused by name and everything else in the tree still lands. <code>--dry-run</code> reports the same diagnostics and ships nothing. <code>primitive config push --no-typecheck</code> skips the check, for the push you need out before the types are fixed.</p><h3 id="imports" tabindex="-1">Imports <a class="header-anchor" href="#imports" aria-label="Permalink to “Imports”">​</a></h3><ul><li><strong>Relative and absolute imports must stay inside the config tree.</strong> An entry or an import that resolves outside it — through a symlink included — is refused at push, so a push can never upload a file your tree does not contain.</li><li><strong>npm packages are imported by name and inlined into the bundle.</strong> They resolve through Node like any other import, and <code>node_modules</code> content is never stored as your source or written back by <code>config pull</code>.</li></ul><p>A missing entry file or a build error names the file and the message, and nothing is pushed for that function — the rest of your config tree still applies.</p><h2 id="invoking" tabindex="-1">Invoking <a class="header-anchor" href="#invoking" aria-label="Permalink to “Invoking”">​</a></h2><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>POST /app/{appId}/api/functions/{key}</span></span></code></pre></div><p>Any signed-in member of the app may call it — subject to the function&#39;s own <code>access</code> gate. From a client, <code>client.functions.invoke</code> takes the key and the input and answers the envelope below; the JS and Swift calls are the same shape:</p><div class="vp-code-group"><div class="tabs"><input type="radio" name="group-66" id="tab-67" checked><label data-title="JavaScript" for="tab-67">JavaScript</label><input type="radio" name="group-66" id="tab-68"><label data-title="Swift" for="tab-68">Swift</label></div><div class="blocks"><div class="language-ts active"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">invoke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">message</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }&gt;(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;greet&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  input: { name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Ada&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// travels as rootInput</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  timeoutMs: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// default 5 000, ceiling 30 000</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (result.status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;completed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(result.output?.message); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &quot;hello Ada&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // &quot;failed&quot; — the handler threw or its output failed outputSchema — or &quot;timeout&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(result.status, result.error);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><div class="language-swift"><button title="Copy Code" class="copy"></button><span class="lang">swift</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Greeting</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Decodable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Sendable </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> message: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result: FunctionResult&lt;Greeting&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> try</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">invoke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;greet&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;name&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Ada&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// travels as rootInput</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  timeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               // seconds; default 5, ceiling 30</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result.status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;completed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(result.output</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.message </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &quot;hello Ada&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // &quot;failed&quot; — the handler threw or its output failed outputSchema — or &quot;timeout&quot;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(result.status, result.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">error</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ??</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div></div></div><h3 id="the-response-envelope" tabindex="-1">The response envelope <a class="header-anchor" href="#the-response-envelope" aria-label="Permalink to “The response envelope”">​</a></h3><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;completed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;output&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello Ada&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span></code></pre></div><p><code>status</code> is one of:</p><table tabindex="0"><thead><tr><th><code>status</code></th><th>What happened</th></tr></thead><tbody><tr><td><code>completed</code></td><td>The handler returned. <code>output</code> is its value, JSON-serialized.</td></tr><tr><td><code>failed</code></td><td>The handler threw, or its output did not match <code>outputSchema</code>. <code>error</code> says which.</td></tr><tr><td><code>timeout</code></td><td>The handler did not finish inside the request&#39;s budget. <code>error</code> says so and <code>errorCode</code> is <code>FUNCTION_TIMEOUT</code>.</td></tr></tbody></table><p>All three are <strong>HTTP 200</strong>: the call reached the function and the function has an answer. An HTTP error means the platform refused before your code ran — <code>403</code> for the gate or a disabled function, <code>404</code> for an unknown key, <code>409</code> for a function with no pushed code, <code>429</code> for the rate ceiling, and <code>503</code> with <code>errorCode: &quot;FUNCTION_RATE_UNAVAILABLE&quot;</code> when the platform cannot consult that ceiling at all. The last one is retryable: the platform refuses rather than run your function unmetered.</p><p>There is no <code>runId</code>, because the request path writes no run row. Nothing to poll, nothing to clean up.</p><p>Every envelope the platform recorded carries an <strong><code>invocationId</code></strong> — the id of the invocation-log record for that call, minted before your code ran so the answer and the record share it:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;failed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;error&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Cannot read properties of undefined&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;errorCode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;FUNCTION_THREW&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;invocationId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;01K4ANRRG0Q7VZ3M8T6XW2H5NB&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Hand it straight to the logs surface:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --invocation</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 01K4ANRRG0Q7VZ3M8T6XW2H5NB</span></span></code></pre></div><p>It is present exactly when the platform wrote a record, which is every outcome your function itself produced. A <strong>gate refusal</strong> — <code>403</code>, <code>404</code>, <code>409</code>, <code>400</code>, <code>429</code>, <code>503</code> — carries none, because it wrote none.</p><p>An envelope from a handler that ran to an answer — <code>completed</code>, or <code>failed</code> because it threw or its output failed <code>outputSchema</code> — also carries <code>limits</code>: the ceilings that invocation actually ran under, which is your <code>[function.limits]</code> clamped element-wise to the platform&#39;s own values:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;completed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;output&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hello Ada&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;limits&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;cpuMs&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;subRequests&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">128</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;ratePerMinute&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1200</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>It is there so you can tell what the platform resolved from what you declared, without guessing: a config asking for more than a ceiling gets the platform&#39;s value silently, and this is where you see that happen. A <code>timeout</code>, an output over the size ceiling, or a refusal that never reached your code — <code>403</code>, <code>404</code>, <code>409</code>, <code>429</code> — carries no <code>limits</code>.</p><h3 id="stating-which-runner-you-mean" tabindex="-1">Stating which runner you mean <a class="header-anchor" href="#stating-which-runner-you-mean" aria-label="Permalink to “Stating which runner you mean”">​</a></h3><p>The route is one route, and <strong><code>mode</code> in the body is how a caller picks the runner</strong>. On a version whose mode is <code>any</code> it decides: <code>&quot;request&quot;</code> runs the code in the call and answers its result, <code>&quot;task&quot;</code> starts a run and answers a run id. Omit it and you get the request runner, which is what <code>invoke</code> callers have always got.</p><p>On a <strong>locked</strong> version it is checked against the lock before either runner executes, so a caller who meant one and got the other is told rather than finding out afterwards with a run already started:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .../functions/reindex</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;mode&quot;:&quot;request&quot;}&#39;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 409 {&quot;error&quot;:&quot;Function &#39;reindex&#39; is a task function: this call asked for the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#      request runner. Use &#39;primitive functions start reindex&#39;.&quot;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#      &quot;errorCode&quot;:&quot;FUNCTION_MODE_MISMATCH&quot;,&quot;mode&quot;:&quot;task&quot;}</span></span></code></pre></div><p>Nothing ran: no run row, no invocation record, and the rate slot handed back. The CLI and the JS and Swift clients all send it on every call, so a version repointed underneath you is refused rather than run.</p><p><code>mode</code> names a <strong>runner</strong>, so <code>&quot;request&quot;</code> and <code>&quot;task&quot;</code> are the only two values a call may send. <code>{&quot;mode&quot;:&quot;any&quot;}</code> is refused <code>400 INVALID_MODE</code>: <code>any</code> is something a <em>version</em> may be — it says the version takes either — which is exactly why the caller is the one that picks.</p><h3 id="request-fields" tabindex="-1">Request fields <a class="header-anchor" href="#request-fields" aria-label="Permalink to “Request fields”">​</a></h3><p>Every field is optional:</p><table tabindex="0"><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody><tr><td><code>rootInput</code></td><td>The value your handler receives as <code>input</code>. Validated and coerced against <code>inputSchema</code> when the function declares one.</td></tr><tr><td><code>contextDocId</code></td><td>A document the invocation is about.</td></tr><tr><td><code>meta</code></td><td>Caller metadata, at most 1 KB encoded.</td></tr><tr><td><code>timeoutMs</code></td><td>Wall-clock budget. Defaults to 5 000 ms and clamps silently to the 30 000 ms ceiling.</td></tr><tr><td><code>runKey</code></td><td>Used by task starts (below); validated and otherwise <strong>ignored</strong> here — there is no run row to deduplicate against.</td></tr></tbody></table><h3 id="the-gate-runs-on-every-call" tabindex="-1">The gate runs on every call <a class="header-anchor" href="#the-gate-runs-on-every-call" aria-label="Permalink to “The gate runs on every call”">​</a></h3><p><code>access</code> is evaluated on <strong>every call</strong>, and it fails closed:</p><ul><li>a caller the expression denies gets <code>403</code> with <code>errorCode: &quot;FUNCTION_ACCESS_DENIED&quot;</code>;</li><li>a function with <strong>no</strong> <code>access</code> expression denies everyone (which is why push refuses to ship code for one);</li><li>app owners and admins bypass it.</li></ul><p>The gate is checked <strong>before</strong> any answer about the function&#39;s operational state, so a caller it rejects cannot tell a disabled function from a never-pushed one from a live one — and cannot map your app&#39;s functions by probing keys.</p><p><strong>The gate is the authorization.</strong> Once a caller is through it, the code runs with the app&#39;s own authority (below): who may invoke is decided here, once, by the expression you reviewed, and nothing inside the function asks the question again. Write the gate for what the function can do, not for who happens to be the caller of the first line.</p><h2 id="calling-the-platform-from-a-function" tabindex="-1">Calling the platform from a function <a class="header-anchor" href="#calling-the-platform-from-a-function" aria-label="Permalink to “Calling the platform from a function”">​</a></h2><p><code>ctx.api</code> is a typed client for the platform&#39;s own API, generated from its OpenAPI document. <strong>Function code acts as the system.</strong> Every call runs with the app&#39;s own authority — a function reads and writes what the app may, in every family the gateway exposes, whoever invoked it. The caller is attribution: <code>ctx.user</code> names them (or is <code>null</code> for a trigger fire), the rows the function writes are attributed to them, and the <code>function.invoke</code> event carries their id — but their role, their database permissions and their document grants decide nothing inside the function.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">documentId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Reads the document whoever owns it; the caller&#39;s own access does not enter.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> doc</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.api.documents.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ documentId: input.documentId });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { title: doc.title, askedBy: ctx.user?.userId </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>That is the point of a server call: the reason to move logic to a function is usually to reach what the caller may not, and the <code>access</code> gate is where you say who may ask for that. It is also why the gate fails closed and why push refuses a function without one.</p><p>A sandbox has <strong>no network access by default</strong>. Its only route out is the platform, and only for the operations it publishes to functions: every family of the API — <code>analytics</code>, <code>blobBuckets</code>, <code>channels</code>, <code>collections</code>, <code>configVars</code>, <code>connections</code>, <code>databases</code>, <code>documents</code>, <code>email</code>, <code>gemini</code>, <code>groups</code>, <code>integrations</code>, <code>llm</code>, <code>locks</code>, <code>notifications</code>, <code>prompts</code>, <code>resourceMetadata</code>, <code>secrets</code>, <code>users</code> — except <code>functions</code> (a function cannot invoke a function over HTTP), <code>iterations</code>, and the database-type catalogue. Within a family, the legacy member doors that duplicate a function door are shut — <code>integrations.proxy</code> (use <code>ctx.integrations.call</code>), <code>prompts.execute</code> (use <code>ctx.prompts.run</code>), <code>databases.executeOperation</code> (use <code>ctx.api.databases.runOperation</code>, which never consults the operation&#39;s access rule) and the deprecated <code>admin-data</code> aliases. Every one of them answers <code>FUNCTION_ROUTE_NOT_ALLOWED</code>.</p><h3 id="capabilities" tabindex="-1">Capabilities <a class="header-anchor" href="#capabilities" aria-label="Permalink to “Capabilities”">​</a></h3><p>Because the code already acts as the system, a capability is never about data. It is declared only where it <strong>configures</strong> something, and there are exactly three kinds:</p><table tabindex="0"><thead><tr><th>Kind</th><th>What it configures</th></tr></thead><tbody><tr><td><code>integration:&lt;key&gt;</code></td><td>The egress allowlist — which integrations <code>ctx.integrations.call</code> may reach.</td></tr><tr><td><code>secret:&lt;NAME&gt;</code></td><td>Which secret <strong>values</strong> may enter the sandbox through <code>ctx.secret</code>.</td></tr><tr><td>The high-blast opt-ins</td><td>Operations that destroy something, hand out persistent authority, or provision a resource.</td></tr></tbody></table><p>The high-blast list, 1:1 with the operation:</p><table tabindex="0"><thead><tr><th>Operation</th><th>Capability</th></tr></thead><tbody><tr><td><code>databases.create</code> / <code>databases.delete</code> / <code>databases.transferOwnership</code></td><td><code>databases:create</code> / <code>databases:delete</code> / <code>databases:transferOwnership</code></td></tr><tr><td><code>databases.addManager</code> / <code>databases.revokePermission</code></td><td><code>databases:addManager</code> / <code>databases:revokePermission</code></td></tr><tr><td><code>databases.grantGroupPermission</code> / <code>databases.revokeGroupPermission</code></td><td><code>databases:grantGroupPermission</code> / <code>databases:revokeGroupPermission</code></td></tr><tr><td><code>users.remove</code> / <code>users.setRole</code></td><td><code>users:remove</code> / <code>users:setRole</code></td></tr><tr><td><code>blobBuckets.createBucket</code> / <code>blobBuckets.deleteBucket</code></td><td><code>blobBuckets:createBucket</code> / <code>blobBuckets:deleteBucket</code></td></tr></tbody></table><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/functions/provision.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;provision&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/provision/index.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user.role == &#39;admin&#39;&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">capabilities = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;databases:create&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;integration:stripe&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;secret:PARTNER_TOKEN&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>An undeclared call in any of the three answers a structured permission error naming the exact string to add (<code>FUNCTION_HIGH_BLAST_GRANT_MISSING</code>, <code>FUNCTION_INTEGRATION_GRANT_MISSING</code>, <code>FUNCTION_SECRET_GRANT_MISSING</code>), and the declaration is read from the <strong>version that is running</strong> — a re-push that drops <code>databases:delete</code> refuses the same code on its next call. The capability is the function&#39;s, not the caller&#39;s: system is owner-equivalent, not super-admin, so a route&#39;s own rules still apply — <code>users.setRole</code> cannot touch the app owner from a function any more than from an owner&#39;s own request.</p><p>Nothing else is declared — a database model, a prompt, a config var, a channel, a send, an email, an analytics write are all reachable on the app&#39;s authority. A file carrying <code>database:…</code>, <code>prompt:…</code>, <code>var:…</code>, <code>channel:…</code>, <code>users:send</code>, <code>connections:send</code>, <code>email:send</code> or <code>analytics:writeForUser</code> is refused at push, naming the string and what stays; delete the line and push again. A <code>runAs</code> or <code>unscopedReads</code> key is refused on the same terms.</p><p>Anything else answers a structured error you can catch:</p><table tabindex="0"><thead><tr><th><code>errorCode</code></th><th>Meaning</th></tr></thead><tbody><tr><td><code>FUNCTION_EGRESS_DENIED</code></td><td><code>fetch</code> to a host other than the platform.</td></tr><tr><td><code>FUNCTION_ROUTE_NOT_ALLOWED</code></td><td>A platform operation not published to functions, or one of the shut legacy doors.</td></tr><tr><td><code>FUNCTION_INTEGRATION_GRANT_MISSING</code></td><td>An integration call the function&#39;s <code>capabilities</code> do not cover. The message names the string that would.</td></tr><tr><td><code>FUNCTION_SECRET_GRANT_MISSING</code></td><td>A secret read the function&#39;s <code>capabilities</code> do not cover.</td></tr><tr><td><code>FUNCTION_HIGH_BLAST_GRANT_MISSING</code></td><td>A high-blast operation the function&#39;s <code>capabilities</code> do not cover. The message names the exact string.</td></tr><tr><td><code>FUNCTION_SECRET_NOT_FOUND</code></td><td>The capability is declared; this environment has no secret of that name.</td></tr><tr><td><code>FUNCTION_VAR_NOT_FOUND</code></td><td>This environment has no config var of that name.</td></tr><tr><td><code>FUNCTION_SEND_TARGET_NOT_FOUND</code></td><td>The user is not a member of this app, or the connection is not one of this app&#39;s.</td></tr><tr><td><code>FUNCTION_SEND_PAYLOAD_TOO_LARGE</code></td><td>A send payload over the 64 KiB ceiling. Nothing was delivered.</td></tr><tr><td><code>FUNCTION_CHANNEL_GRANTEE_REQUIRED</code></td><td><code>ctx.channels.authorize</code> from a trigger fire with no <code>userId</code> — there is no caller to default to.</td></tr><tr><td><code>FUNCTION_CHANNEL_NAME_INVALID</code></td><td>A channel name outside the grammar.</td></tr><tr><td><code>FUNCTION_CHANNEL_GRANTEE_NOT_FOUND</code></td><td>The <code>userId</code> a grant names is not a member of this app — the same answer as a user that never existed.</td></tr><tr><td><code>QUERY_IN_LIST_TOO_LARGE</code></td><td>A single <code>$in</code> or <code>$nin</code> list over 1 000 values. The message names the field, the count and the cap. Split the list across queries or narrow the filter.</td></tr></tbody></table><h3 id="what-a-function-touches-is-documented-not-declared" tabindex="-1">What a function touches is documented, not declared <a class="header-anchor" href="#what-a-function-touches-is-documented-not-declared" aria-label="Permalink to “What a function touches is documented, not declared”">​</a></h3><p><code>config push</code> collects a <strong>manifest</strong> with every version: the queries it registered, the models its code names — through the typed handle, the direct records API and the document records family alike — and the families of <code>ctx.api</code> and the ctx helpers it reaches. <code>primitive functions get</code> shows it under MANIFEST, and a model name the scan could not resolve (a computed <code>modelName</code>) is marked &quot;resolved at run time&quot; rather than silently dropped.</p><p>It is <strong>documentation, never authorization</strong>: a reviewer reads it to see what a change reaches, and nothing on the platform decides anything from it.</p><h3 id="where-operations-are-going" tabindex="-1">Where operations are going <a class="header-anchor" href="#where-operations-are-going" aria-label="Permalink to “Where operations are going”">​</a></h3><p>Client-callable database operations are on their way out: the endpoints clients call retire in project phase 5, and the concept in phase 6. The succession is a function. An operation that filtered on a <code>$params.userId</code> fed from the user becomes a <code>defineQuery</code> with the <code>$caller</code> marker (below): the platform injects the invoking user, a call may not supply it, and the query runs on the app&#39;s authority under the function&#39;s <code>access</code> gate. <a href="#migrating-a-workflow-tree-to-functions">Migrating a workflow tree to functions</a> is the full account, including how a parent workflow keeps working while its leaves move.</p><h2 id="calling-an-outside-service" tabindex="-1">Calling an outside service <a class="header-anchor" href="#calling-an-outside-service" aria-label="Permalink to “Calling an outside service”">​</a></h2><p>A function cannot <code>fetch</code> the internet. What it can do is call an <strong>integration</strong> it has declared — the egress allowlist — and the platform makes that request on its behalf:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">orderId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> response</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.integrations.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stripe&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    method: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;POST&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    path: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/v1/refunds&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    body: { payment_intent: input.orderId },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { status: response.status, refund: response.body };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/functions/refund.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;refund&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/refund/index.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">capabilities = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;integration:stripe&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>The request is issued by the platform, not by your code, and that is the point: the integration&#39;s <code>{{secrets.*}}</code> and <code>{{vars.*}}</code> templates are resolved into it <strong>server-side</strong>, so the credential reaches the upstream service and never reaches the function. Your code sees the response; it never sees the key.</p><p>Everything the integration declares still applies, and applies to what the request really asks for rather than to the string you wrote:</p><ul><li><code>path</code> is relative to the integration&#39;s <code>baseUrl</code>, and the host is the integration&#39;s. The path is resolved and normalized before it is checked, so <code>//elsewhere/x</code> and <code>/allowed/../blocked</code> are refused rather than quietly sent somewhere else.</li><li><code>allowedMethods</code> and <code>allowedPaths</code> are enforced — <code>DISALLOWED_METHOD</code> and <code>DISALLOWED_PATH</code>.</li><li><strong>Redirects are re-checked before they are followed.</strong> A hop is followed only if it stays on the integration&#39;s exact origin (same scheme, host and port), lands on an allowed path, and uses an allowed method. Otherwise the <code>3xx</code> comes back to you unfollowed, so you can see what the upstream wanted. The chain is capped at five hops.</li><li>The upstream request is bounded by the integration&#39;s own <code>timeoutMs</code> or by <strong>your invocation&#39;s</strong> remaining time, whichever is smaller.</li></ul><p>The integration&#39;s <code>accessRule</code> is <strong>not</strong> consulted here. That rule says which signed-in members may call the integration from a client; on the function path the <code>integration:&lt;key&gt;</code> line in the reviewed TOML is the egress allowlist, and the function&#39;s <code>access</code> gate was the authorization. An integration that is disabled or deleted still refuses everyone, function or not (<code>INTEGRATION_INACTIVE</code>).</p><p>Every call is recorded on the integration&#39;s own log with the function&#39;s key, so <code>primitive integrations logs &lt;integration-id&gt;</code> answers &quot;what did this function send out, and when&quot;.</p><h2 id="running-a-prompt" tabindex="-1">Running a prompt <a class="header-anchor" href="#running-a-prompt" aria-label="Permalink to “Running a prompt”">​</a></h2><p><code>ctx.prompts.run</code> runs one of the app&#39;s saved prompts through the same execution path the member endpoint uses, with nothing to declare:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.prompts.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;summarize&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    variables: { text: input.text },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result.success) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(result.error </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;prompt failed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { summary: result.output };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>You get the same envelope the member endpoint answers with — <code>success</code>, <code>output</code>, <code>error</code>, <code>metrics</code>, <code>configId</code> — and <code>configId</code> tells you which version of the prompt ran. <code>configId</code> in the arguments pins a specific config of that prompt; anything else answers <code>PROMPT_NO_CONFIG</code>.</p><h3 id="typed-parsed-output" tabindex="-1">Typed, parsed output <a class="header-anchor" href="#typed-parsed-output" aria-label="Permalink to “Typed, parsed output”">​</a></h3><p>If the prompt answers JSON, say so once in its TOML and stop parsing it by hand. A <code>[prompt.outputSchema]</code> is the declaration the platform sends to the provider, validates the answer against, <strong>and</strong> types your call from:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/prompts/categorize.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">prompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;categorize&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">accessRule = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">prompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">outputSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;object&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">required = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;suggested_transactions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">prompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">outputSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">properties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">suggested_transactions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;array&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">prompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">outputSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">properties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">suggested_transactions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">items</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;object&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">prompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">outputSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">properties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">suggested_transactions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">items</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">properties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">amount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;number&quot;</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">payload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> answer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.prompts.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;categorize&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    variables: { payload: input.payload },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">answer.success) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(answer.error </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;categorize failed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // \`parsed\` is the JSON value of the model&#39;s answer, already validated</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // against the schema above and typed from it.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { suggested: answer.parsed.suggested_transactions };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><code>config push</code> renders every prompt&#39;s schema into <code>functions/primitive-prompt-types.d.ts</code>, so <code>answer.parsed</code> has the declared type, a field the schema does not declare is a compile error, and <code>ctx.prompts.run(&quot;no-such-prompt&quot;)</code> does not compile in a pushed tree.</p><p><code>parsed</code> appears on the <strong>success arm only</strong>, which is why the <code>success</code> check above is what unlocks it. Two things can go wrong with the answer itself, and both are <code>success: false</code> with an <code>errorCode</code> — the run happened and was billed, so <code>output</code>, <code>metrics</code> and <code>configId</code> are still there:</p><table tabindex="0"><thead><tr><th><code>errorCode</code></th><th>What happened</th></tr></thead><tbody><tr><td><code>PROMPT_OUTPUT_NOT_JSON</code></td><td>The prompt is declared to answer JSON and the model answered text that does not parse — or that parses to a number JSON cannot represent, such as one that overflows. <code>output</code> has the text.</td></tr><tr><td><code>PROMPT_OUTPUT_SCHEMA_VIOLATION</code></td><td>It parsed, and the declared <code>outputSchema</code> refuses it. <code>error</code> names the failing paths (<code>value.word must be string</code>).</td></tr></tbody></table><p>A <strong>provider</strong> failure — the model call itself failing — is unchanged: <code>success: false</code>, <code>error</code> set, and <strong>no</strong> <code>errorCode</code>. That is how you tell &quot;the model returned the wrong shape&quot; from &quot;the model failed&quot;.</p><p>A validation <code>error</code> names paths and the schema&#39;s own constraints and never quotes the model&#39;s answer; <code>output</code> is where the answer is, and that is deliberate, so a diagnostic in a log cannot carry generated content.</p><p>If a prompt declares no <code>outputSchema</code> but the config that ran declares <code>outputFormat = &quot;json&quot;</code>, you still get <code>parsed</code> when the text parses — but nothing declared its shape, so it is <code>unknown</code>. Declare the schema to get a type.</p><h3 id="what-the-prompt-s-own-rules-still-do" tabindex="-1">What the prompt&#39;s own rules still do <a class="header-anchor" href="#what-the-prompt-s-own-rules-still-do" aria-label="Permalink to “What the prompt&#39;s own rules still do”">​</a></h3><p>As with integrations, the prompt&#39;s <code>accessRule</code> is <strong>not</strong> consulted: it says which signed-in members may execute the prompt from a client, and the function&#39;s <code>access</code> gate is what authorized the function. Availability still applies — an inactive or archived prompt refuses everyone, and a prompt key the app does not have is not found.</p><p>The model call is bounded by <strong>your invocation&#39;s</strong> remaining time. A function that times out does not leave a model request running behind it.</p><h2 id="reading-secrets-and-config-vars" tabindex="-1">Reading secrets and config vars <a class="header-anchor" href="#reading-secrets-and-config-vars" aria-label="Permalink to “Reading secrets and config vars”">​</a></h2><p>A <code>secret:&lt;NAME&gt;</code> capability lets a function read one app secret by name; config vars need no declaration:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">_input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> region</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">configVar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;REGION&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> token</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">secret</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PARTNER_TOKEN&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { region, tokenLength: token.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">capabilities = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;secret:PARTNER_TOKEN&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>Two things worth knowing before you reach for <code>ctx.secret</code>:</p><ul><li><strong>Prefer an integration when the secret is a credential for one.</strong> An integration&#39;s <code>{{secrets.*}}</code> templates are resolved by the platform into the outbound request, so the value never enters your sandbox at all. <code>ctx.secret</code> is for the cases an integration cannot express.</li><li><strong>Secrets and config vars are provisioned per environment, out of band from your push.</strong> So a <code>secret:</code> capability is checked for spelling at push and for existence at call time: a declared name with no value in this environment answers <code>FUNCTION_SECRET_NOT_FOUND</code> (or <code>FUNCTION_VAR_NOT_FOUND</code> for a var), naming the key so you can set it.</li></ul><p>The two namespaces are separate. <code>ctx.secret(&quot;X&quot;)</code> never reads a config var named <code>X</code>, and <code>ctx.configVar(&quot;X&quot;)</code> never reads a secret.</p><p>Secret values are read through a short stale-while-revalidate cache, so a secret you have just rotated may serve its previous value for up to about a minute. That is the platform&#39;s read cache, not a property of your function — a retry a minute later sees the new value.</p><p>Config vars are cached harder, and deliberately: a var never changes within a deployment, so <code>ctx.configVar</code> reads each name <strong>once per config version</strong> and answers from the sandbox after that — no round trip on later invocations. A miss is not cached (provision the value and the next read sees it), and <code>ctx.secret</code> is never cached, so a rotated secret takes effect on the next read.</p><h2 id="sending-to-a-connected-client" tabindex="-1">Sending to a connected client <a class="header-anchor" href="#sending-to-a-connected-client" aria-label="Permalink to “Sending to a connected client”">​</a></h2><p><code>ctx.users.send</code> and <code>ctx.connections.send</code> push a message to a client that is connected right now, with nothing to declare:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">userId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.users.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(input.userId, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    kind: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order-ready&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    orderId: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;o-1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { delivered: result.connections };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The client receives a <code>direct.message</code> frame carrying your payload verbatim and the key of the function that sent it — <code>client.on(&quot;directMessage&quot;, …)</code> in JS, the <code>DirectMessageEvent</code> stream in Swift:</p><div class="vp-code-group"><div class="tabs"><input type="radio" name="group-657" id="tab-658" checked><label data-title="JavaScript" for="tab-658">JavaScript</label><input type="radio" name="group-657" id="tab-659"><label data-title="Swift" for="tab-659">Swift</label></div><div class="blocks"><div class="language-ts active"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;directMessage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">event</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // event.payload is what the function passed; event.functionKey is who sent it</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(event.functionKey, event.payload, event.sentAt);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><div class="language-swift"><button title="Copy Code" class="copy"></button><span class="lang">swift</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> event </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">stream</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: DirectMessageEvent.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // event.payload (JSONValue?) is what the function passed;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // event.functionKey is who sent it; event.sentAt is when.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(event.functionKey, event.sentAt)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> message </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> event.payload</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">stringValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(message)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div></div></div><p>What the API promises, and what it deliberately does not:</p><ul><li><strong>Presence is not guaranteed.</strong> A user with no connected client answers <code>{ connections: 0, truncated: false }</code> — a success, not an error. There is no durable record behind the frame: a client that was offline does not receive it later. Use a notification if the message has to survive being missed.</li><li><strong>One socket, one frame.</strong> A client with several documents open is one connection, counted once and delivered once.</li><li><strong>The fanout is bounded</strong> at 64 unique connections per user, and the lookup behind it is bounded too. Past either bound the send delivers what it found and answers <code>truncated: true</code> — treat that flag as &quot;there may be more&quot;, not as an error.</li><li><strong>Payloads are capped</strong> at 64 KiB serialized. Over it, the call answers <code>413</code> naming the bound and nothing is delivered.</li><li><code>ctx.connections.send(connectionId, payload)</code> addresses one connection. An id belonging to another app answers exactly as an id that never existed — connection ids are not a way to reach another tenant.</li></ul><h2 id="channels" tabindex="-1">Channels <a class="header-anchor" href="#channels" aria-label="Permalink to “Channels”">​</a></h2><p>A direct send addresses a user or a connection you already know. A <strong>channel</strong> addresses whoever is listening to a named topic — an order&#39;s status, a game table, a document&#39;s presence lane — without your function knowing who that is.</p><p>Two calls make one, and neither needs a declaration:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/functions/order-room.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order-room&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/order-room.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">orderId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 1. Authorize the caller for one channel. The grant names the CALLER, so it</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  //    is not a credential anybody else can use.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">grant</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">expiresAt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.channels.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">authorize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    \`orders:\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">orderId</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  );</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { grant, expiresAt };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The client presents that grant on the socket it already has with <code>subscribeToChannel</code>, and every publish to the channel then arrives as a <code>channelMessage</code> event (<code>client.on(&quot;channelMessage&quot;, …)</code> in JS, the <code>ChannelMessageEvent</code> stream in Swift):</p><div class="vp-code-group"><div class="tabs"><input type="radio" name="group-705" id="tab-706" checked><label data-title="JavaScript" for="tab-706">JavaScript</label><input type="radio" name="group-705" id="tab-707"><label data-title="Swift" for="tab-707">Swift</label></div><div class="blocks"><div class="language-ts active"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1. Ask the authorizing function for a grant; it names this caller and one channel.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">invoke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">grant</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">expiresAt</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }&gt;(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;order-room&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { input: { orderId } }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (result.status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;completed&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ||</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> !</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result.output) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 2. Present the grant on the socket the client already has.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> subscription</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">subscribeToChannel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  \`orders:\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">orderId</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  result.output.grant</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;channelMessage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">event</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // event.channel, event.payload, event.functionKey, event.sentAt</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(event.channel, event.payload);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// A grant that expired while the socket was down is refused on reconnect with</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// nothing waiting on it — invoke the function again and re-subscribe.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;channelSubscribeFailed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">event</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">warn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;renew&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, event.channel, event.message);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Later: leave. Idempotent — the same as client.unsubscribeFromChannel(channel).</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">subscription.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unsubscribe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><div class="language-swift"><button title="Copy Code" class="copy"></button><span class="lang">swift</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Grant</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Decodable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Sendable </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> grant: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; let expiresAt: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1. Ask the authorizing function for a grant; it names this caller and one channel.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result: FunctionResult&lt;Grant&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> try</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">invoke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;order-room&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;orderId&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orderId]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">guard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result.status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;completed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> output </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result.output </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 2. Present the grant on the socket the client already has.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> subscription </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> try</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">subscribeToChannel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;orders:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\\(orderId)</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  grant</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: output.grant</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Later: leave. Idempotent — the same as client.unsubscribeFromChannel(channel).</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">subscription.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unsubscribe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div></div></div><p>Any function can then publish to it — including one fired by a <a href="#database-change-triggers">database-change trigger</a>, which is the combination the realtime story is built for:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.channels.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">publish</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`orders:\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">orderId</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;shipped&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span></code></pre></div><p><code>publish</code> answers <code>{ connections, truncated }</code>: how many connections received the frame, and whether the fanout stopped at its ceiling of 500 connections per channel. Zero connections is a success, not an error — nobody was listening.</p><h3 id="channel-names" tabindex="-1">Channel names <a class="header-anchor" href="#channel-names" aria-label="Permalink to “Channel names”">​</a></h3><p>A channel name is one or more segments of <code>[A-Za-z0-9_-]</code> joined by <code>:</code>, at most 200 characters — <code>orders</code>, <code>orders:42</code>, <code>orders:42:chat</code>. A name outside the grammar is a <code>400</code> about the name (<code>FUNCTION_CHANNEL_NAME_INVALID</code>), decided before anything else.</p><h3 id="expiry-is-the-revocation" tabindex="-1">Expiry is the revocation <a class="header-anchor" href="#expiry-is-the-revocation" aria-label="Permalink to “Expiry is the revocation”">​</a></h3><p>A grant is a short-lived signed token, not a row, so there is nothing to delete: <strong>expiry is the only revocation</strong>, and it ends MEMBERSHIP, not just the join.</p><ul><li>The default TTL is 300 seconds and the ceiling is 900. Ask for more with <code>ctx.channels.authorize(channel, { ttlSeconds })</code> and you get the ceiling, not an error.</li><li><code>expiresAt</code> comes back with the grant so a client can <strong>renew ahead of time</strong>. Renewing is re-subscribing with a fresh grant: same channel, same socket, new expiry.</li><li>Past <code>expiresAt</code> the server stops delivering <strong>even though the socket is still open</strong>. A client that keeps its connection but lets its grant lapse simply stops hearing the channel until it re-subscribes.</li></ul><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Renew a minute before the grant lapses.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">grant</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">expiresAt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">invokeFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order-room&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  orderId,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setTimeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">subscribeToChannel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`orders:\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">orderId</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, grant),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  expiresAt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Date.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">now</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 60_000</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Everything else about a channel is a refusal you cannot distinguish from another: an expired grant, a tampered one, one minted for another app, one minted for another user and one naming a different channel all get the same error frame with the same message. The frame echoes the channel you asked for, so a client with several subscriptions in flight knows which one failed — but the REASON is uniform, because a different message for each would be an oracle.</p><p>On reconnect the client re-issues every subscribe it is holding with its stored grant. A grant that has expired in the meantime is refused, and only that channel&#39;s registration is dropped — the client reports it on the <code>channelSubscribeFailed</code> event (<code>{ channel, message }</code>), which is your cue to invoke the authorizing function again and re-subscribe.</p><div class="warning custom-block"><p class="custom-block-title">A grant is a bearer token for one user</p><p><code>ctx.channels.authorize</code> always names a grantee. An HTTP invocation defaults to its caller; a trigger fire has no caller and must pass <code>{ userId }</code> explicitly (<code>FUNCTION_CHANNEL_GRANTEE_REQUIRED</code> otherwise), and a grant is refused at subscribe time if the socket&#39;s user is not the one it names. There is no app-wide channel grant.</p></div><h2 id="sending-email" tabindex="-1">Sending email <a class="header-anchor" href="#sending-email" aria-label="Permalink to “Sending email”">​</a></h2><p><code>ctx.api.email.send</code> sends as the app, with nothing to declare — a cron-fired function sending a digest is the point of it. Template mode and inline mode are exclusive, as are <code>to</code> and <code>toUserId</code>; a <code>toUserId</code> must be a member of this app.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.api.email.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  body: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    toUserId: input.userId,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    subject: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Your receipt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    htmlBody: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`&lt;p&gt;Thanks, \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}.&lt;/p&gt;\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    textBody: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`Thanks, \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}.\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    variables: { appName: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Acme&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The route is <code>POST /app/{appId}/api/emails/send</code> and it is reachable ONLY from a function: a member, admin or owner calling it directly is refused, because sending as the app is what function code does and a role is not.</p><p>The app has ONE hourly email budget; passing it answers <code>429</code> with <code>errorCode: &quot;EMAIL_RATE_LIMITED&quot;</code>.</p><h2 id="writing-analytics-for-a-user" tabindex="-1">Writing analytics for a user <a class="header-anchor" href="#writing-analytics-for-a-user" aria-label="Permalink to “Writing analytics for a user”">​</a></h2><p><code>ctx.api.analytics.writeForUser</code> records an event attributed to a SUBJECT user rather than to whoever is running, with nothing to declare:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.api.analytics.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">writeForUser</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  body: { userId: input.userId, action: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order_placed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, feature: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;orders&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The route is <code>POST /app/{appId}/api/analytics/write-for-user</code>, function-only for the same reason.</p><p>The subject must be a member of this app — a user id from another app answers exactly what an id that never existed answers. <code>appId</code> and <code>userId</code> are reserved: a <code>context</code> object of your own cannot rewrite whose activity the event is.</p><h2 id="bounded-fan-out-ids-and-step-policies" tabindex="-1">Bounded fan-out, ids, and step policies <a class="header-anchor" href="#bounded-fan-out-ids-and-step-policies" aria-label="Permalink to “Bounded fan-out, ids, and step policies”">​</a></h2><p>Three helpers ship in <code>primitive-functions</code>:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { pMap, ulid, stepPolicy } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Bounded concurrency — an unbounded Promise.all over a page of rows spends</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// the sandbox&#39;s whole subrequest budget in one line.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> results</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> pMap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(rows, (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">row</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> handle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(row), { concurrency: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Generate ids INSIDE step.do. A durable run re-executes its handler on every</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// resume and memoizes only what a step returned, so an id minted at the top of</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// a handler is a different id after a sleep.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mint-id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ulid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// The platform&#39;s timeout and retry policy for non-idempotent calls.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// \`retries: 0\` on email and the AI families is deliberate: a retried timeout</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// would send the same email twice or bill the same generation twice.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;send-receipt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, stepPolicy.email, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ctx.api.email.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ body: { to, subject, htmlBody } })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p><code>stepPolicy</code> carries <code>email</code>, <code>llm</code>, <code>gemini</code> and <code>database</code>. It is a default you apply, not a policy the engine forces on your own <code>step.do</code> calls.</p><h2 id="databases-and-documents-on-the-app-s-authority" tabindex="-1">Databases and documents, on the app&#39;s authority <a class="header-anchor" href="#databases-and-documents-on-the-app-s-authority" aria-label="Permalink to “Databases and documents, on the app&#39;s authority”">​</a></h2><p>There is no database grant. A function reaches every model of every database of the app, and every model of every document, on the app&#39;s own authority — the <code>access</code> gate decided who may ask. The whole records surface is open: query, count, aggregate, save, batch, patch, delete, increment, the string-set verbs and the index routes, through the typed handle or through <code>ctx.api.databases.records.*</code>; and prepared operations through <code>ctx.api.databases.runOperation</code>, which never consults the operation&#39;s access rule.</p><p>The database&#39;s own tenant check still runs on every call: the platform hands the database a signed statement of <strong>whose</strong> database the call was minted for, and a database refuses a call minted for another tenant, whatever the sandbox sent. That is the boundary; the model list is not.</p><p>Documents are first-class beside databases. <code>ctx.api.documents.records.query</code> takes <code>filter</code> (an object, or its JSON string), <code>limit</code> and <code>cursor</code>, and answers the same <code>{ items, hasMore, nextCursor }</code> page the route and the CLI do; <code>count</code> takes <code>filter</code> too:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> page</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.api.documents.records.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  documentId: input.documentId,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  model: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Order&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filter: { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;open&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  limit: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rest</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.api.documents.records.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  documentId: input.documentId,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  model: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Order&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filter: { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;open&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  cursor: page.nextCursor,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h2 id="reading-and-writing-records" tabindex="-1">Reading and writing records <a class="header-anchor" href="#reading-and-writing-records" aria-label="Permalink to “Reading and writing records”">​</a></h2><p>A function reaches records two ways.</p><h3 id="the-typed-handle" tabindex="-1">The typed handle <a class="header-anchor" href="#the-typed-handle" aria-label="Permalink to “The typed handle”">​</a></h3><p><code>ctx.db(databaseId, &quot;&lt;type&gt;&quot;)</code> gives you the models that database type declares, typed from its schema:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">databaseId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> orders</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">db</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(input.databaseId, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;orders&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Order&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">save</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;o-1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, data: { label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;first&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, total: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> open</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ filter: { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;open&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> total</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Every row a read hands back carries its id, so a row you just queried is</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // a row you can change or remove.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> stale</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> open.items[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">patch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(stale.id, { data: { label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;renamed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">delete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(open.items[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">].id);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Several writes to the same model in one request; each carries the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // handle&#39;s model, so you never spell it again.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">batch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    operations: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      { op: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;save&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;o-2&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, data: { label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;second&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, total: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      { op: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;delete&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;o-3&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    atomic: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { open: open.items.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, total };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The second argument is the database <strong>type</strong> key, and it selects which models the handle knows. A model the type does not declare is a compile error, not a runtime surprise. It is a typing selector only: the platform reads the database&#39;s real type from its own row.</p><p><strong>Every row a read hands back carries <code>id: string</code></strong>, whatever its schema declares. The record identity belongs to the platform, not to your schema — a record saved without one is given a ULID, and <code>patch</code> and <code>delete</code> take nothing else — so it is on a queried row, on a saved record and on a patched one even when your <code>.toml</code> never mentions an <code>id</code> field. A row your code <em>builds</em> for a write still needs no id: <code>save</code> and the batch&#39;s <code>data</code> take a partial row.</p><p>Each model handle carries <code>query</code>, <code>count</code>, <code>aggregate</code>, <code>save</code>, <code>patch</code>, <code>delete</code> and <code>batch</code>. <code>patch</code> and <code>delete</code> take the record id first and the body second, so the id and the model are both the handle&#39;s business rather than something a body can quietly change. A batch that spans <strong>several</strong> models is written on the database handle instead — <code>ctx.db(id, &quot;orders&quot;).batch({ … })</code> — and names each model itself.</p><p><strong>The type&#39;s <code>timestamps</code> directive applies to these writes.</strong> A handle&#39;s <code>save</code> and <code>patch</code> are the records routes, so a type declaring <code>timestamps = { create = &quot;createdAt&quot;, update = &quot;modifiedAt&quot; }</code> stamps them — including each <code>save</code> and <code>patch</code> inside a batch, <code>upsertOn</code> included, on the create and on the update. You do not set those fields yourself, and an explicit value you do send wins. <code>delete</code>, <code>increment</code>, <code>addToSet</code> and <code>removeFromSet</code> carry no record body, so they never stamp. See <a href="./working-with-databases.html#server-stamped-fields">Server-Stamped Fields</a>.</p><h3 id="one-paged-envelope" tabindex="-1">One paged envelope <a class="header-anchor" href="#one-paged-envelope" aria-label="Permalink to “One paged envelope”">​</a></h3><p>Every paged read the SDK exposes answers the same shape — <code>{ items, hasMore, nextCursor? }</code>. The database handle, the document handle (<code>ctx.doc(id)</code>), <code>ctx.users.list</code> and the raw <code>ctx.api.databases.records.query</code> all use it, so paging code is written once and handed any of them. The raw operation adds a <code>prevCursor</code>, and it is the only surface that has one.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> page </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ options: { limit: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rows</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page.items];</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (page.hasMore) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  page </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ options: { limit: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, uniqueStartKey: page.nextCursor } });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  rows.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">push</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page.items);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">Migrating</p><p>The database handle used to answer <code>{ data, … }</code>. A function reading <code>.data</code> off <code>ctx.db(...).model(...).query()</code> or off <code>ctx.api.databases.records.query()</code> moves to <code>.items</code> — the generated declarations make the old key a compile error, rather than an <code>undefined</code> that pages to nothing. The REST route&#39;s own response is unchanged and still carries <code>data</code>, so nothing outside a function moves.</p></div><p><code>config push</code> writes the declarations that make this work into your config tree&#39;s <code>functions/</code> directory, beside your sources, along with a <code>functions/tsconfig.json</code> that includes them. There is nothing to wire up: open the folder in your editor and the types are there. <code>config diff</code> tells you when a schema change has left them stale, and the next <code>config push</code> refreshes them. None of the generated files is part of what gets pushed.</p><p>Push does not just write them: it <strong>holds your code to them</strong>, so a model, a schema or an SDK type that moved under a function is a refused push rather than a run-time surprise (see <a href="#typechecked-before-it-ships">Typechecked before it ships</a>).</p><p>In CI, ask the question directly — it exits non-zero when the generated files are out of date or the tsconfig no longer loads them:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> codegen</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --check</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # or without --check to regenerate</span></span></code></pre></div><p>The <code>tsconfig.json</code> is yours to adjust — push writes it once and then leaves your settings alone. The two things it keeps are the ones that make the types load at all: the <code>primitive-functions</code> path mapping and the generated declarations being in the program. If either goes missing, <code>config diff</code> reports the file and the next push puts them back, alongside everything else you have written in it.</p><p>Filters and query options are ordinary objects. <code>ctx.api.databases.records.*</code> is the untyped form of the same operations, and carries the rest of the surface (<code>increment</code>, the string-set and index routes).</p><p>A single <code>$in</code> or <code>$nin</code> list holds up to <strong>1 000 values</strong>, whatever the filter around it — a list on one field and a list on another are counted separately, so two 600-value lists in one filter are fine. Past the cap the call is refused with <code>QUERY_IN_LIST_TOO_LARGE</code>, naming the field and the count, rather than failing inside SQL. The list itself costs the statement one bound parameter, so a long list is no more expensive to page or sort than a short one, and reading a thousand keys is one query:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> page</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> positions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filter: { snapshotKey: { $in: keys } },   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// up to 1 000 keys</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  options: { sort: { snapshotKey: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, limit: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">200</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>For more than a thousand, how you split depends on the operator. Chunk an <code>$in</code> and merge the pages yourself — each chunk matches some of the rows you want, and the union is the answer. Never merge chunked <code>$nin</code> queries that way: a chunk excluding some of the values still returns the rows the other chunks exclude, so merging gives back nearly everything. Put <code>$nin</code> chunks in one filter instead, where <code>$and</code> intersects them rather than unioning them:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> chunks</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [];</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> keys.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) chunks.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">push</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(keys.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slice</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(i, i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Each chunk is its own capped list and its own bound parameter; \`$and\`</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// applies all of them, so this excludes every key in every chunk.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> page</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> positions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  filter: { $and: chunks.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">chunk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ snapshotKey: { $nin: chunk } })) },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h3 id="registered-queries" tabindex="-1">Registered queries <a class="header-anchor" href="#registered-queries" aria-label="Permalink to “Registered queries”">​</a></h3><p><code>defineQuery</code> names a read over the handle, so the same query can be called from several places, validated once, scoped to its caller, and — when the platform can verify what it touched — cached.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineQuery, defineMutation } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> myOrders</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;myOrders&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  models: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Order&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  params: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    owner: { caller: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// $caller: injected, never supplied</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    since: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;string&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, optional: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    limit: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;number&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, default: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">25</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  cache: { ttlMs: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // \`params\` is typed from the declaration above: owner is a string, since is</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // string | undefined, limit is a number — and \`params.nope\` is a compile error.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">db</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> answer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Order&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ limit: params.limit });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> answer.items.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">row</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> row.owner </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> params.owner);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// A list parameter — the shape behind every \`$in\` filter. \`items\` is the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// element type; \`params.ids\` is \`string[]\` inside \`run\`.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ordersByIds</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ordersByIds&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  models: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Order&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  params: { ids: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;array&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, items: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;string&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">db</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    db.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Order&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ filter: { id: { $in: params.ids } } }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> addOrder</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineMutation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;addOrder&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  models: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Order&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  params: { owner: { caller: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, label: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;string&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">db</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    db.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Order&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">save</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ data: { owner: params.owner, label: params.label } }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> db</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">db</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(input.databaseId, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;orders&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { orders: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> myOrders</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(db, { limit: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>Register at module scope.</strong> A registration made inside your handler still works for direct calls, but <code>config push</code> will not see it, so it is absent from the manifest <code>primitive functions get</code> shows.</p><ul><li><strong>Names are unique.</strong> Registering the same name twice throws.</li><li><strong>Parameters are validated and coerced</strong> against what you declared, with defaults applied; an error names the query and the parameter, and an undeclared parameter is refused rather than dropped. A parameter&#39;s <code>type</code> is <code>string</code>, <code>number</code>, <code>boolean</code>, <code>any</code>, or <code>array</code> with an <code>items</code> type from the same list (no <code>items</code> accepts any array); <code>caller</code> cannot be combined with <code>array</code>. A declared <code>default</code> is <strong>coerced to the declared type at registration</strong> (<code>&quot;25&quot;</code> becomes <code>25</code>, <code>[&quot;1&quot;]</code> becomes <code>[1]</code> for numeric items), so <code>run</code> receives what the declaration promises — and a default that cannot be coerced is refused at registration, which <code>config push</code> reports.</li><li><strong><code>$caller</code> is a convenience marker, and it is immutable.</strong> The platform injects the invoking user; a call that supplies that parameter is refused by name. It scopes a query to whoever asked without your handler threading <code>ctx.user</code> through — the succession for an operation that filtered on a <code>$params.userId</code>. In an invocation with no initiating user — a webhook, cron or database-change fire — a <code>$caller</code> query throws, because there is nobody to scope to.</li></ul><p><strong>Caching is off unless the run is verified.</strong> Declaring <code>cache.ttlMs</code> is not enough: the wrapper watches what <code>run</code> really did, and stores the answer only when every model it touched was declared and nothing was written. The TTL is capped at one minute.</p><p>A cache entry belongs to one query, one <strong><code>databaseId</code> and resolved type</strong>, and one set of parameters after injection — so two databases of the same type never share an answer, and two callers never see each other&#39;s rows. A write through the handle to a model drops the entries that read it, and the whole cache lives inside one warm sandbox instance: it bounds staleness, it does not replicate.</p><h3 id="the-typed-document-handle" tabindex="-1">The typed document handle <a class="header-anchor" href="#the-typed-document-handle" aria-label="Permalink to “The typed document handle”">​</a></h3><p>Documents get the same handle as databases. <code>ctx.doc(documentId)</code> addresses one document; <code>.model(&quot;&lt;Model&gt;&quot;)</code> binds one of its record models, and the six operations answer the record routes&#39; own shapes — <code>{ items, hasMore, nextCursor }</code>, <code>{ count }</code>, <code>{ record }</code>, <code>{ deleted }</code>:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">documentId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> orders</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">doc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(input.documentId).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Order&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> open</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ filter: { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;open&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, limit: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rest</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ filter: { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;open&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, cursor: open.nextCursor });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ filter: { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;closed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">save</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;o-1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, data: { label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;first&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;open&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">patch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;o-1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { data: { label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;renamed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">deleted</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">delete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;o-1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { open: open.items.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rest.items.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, count, record, deleted };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Its rows carry <code>id: string</code> for the same reason a database row does: the record identity is the platform&#39;s, so <code>query().items[0].id</code>, <code>save(...).record.id</code> and <code>patch(...).record.id</code> are all there to hand straight back to <code>patch</code> or <code>delete</code>, whatever the schema declares.</p><p>The rows are typed from your project&#39;s <code>models/models.toml</code> — the document schema <code>primitive init</code> wires (the web client&#39;s <code>src/models/models.toml</code> is the fallback). <code>config push</code> renders it into <code>functions/primitive-document-types.d.ts</code> beside the other declarations, so a model the schema does not declare is a compile error and a declared field has its declared type. A project with no schema file gets the open form: any model name, rows as open records. Like <code>ctx.db</code>, the handle confers nothing — every call is one of the <code>ctx.api.documents.records.*</code> operations on the app&#39;s own authority, and a document id from another app is the uniform not-found.</p><h3 id="what-config-push-runs" tabindex="-1">What <code>config push</code> runs <a class="header-anchor" href="#what-config-push-runs" aria-label="Permalink to “What config push runs”">​</a></h3><p>Collecting the manifest means <strong>running your tree&#39;s module scope</strong>: <code>config push</code> builds the bundle a second time with <code>primitive-functions</code> replaced by a recording stub, and executes it in a separate Node process with a scrubbed environment, your config tree as its working directory, and a hard timeout. It is the same trust you extend by running the project&#39;s own build — but it is worth knowing before you push a project you did not write. The models and families the manifest lists come from a static scan of the built bundle beside that run, so a literal <code>modelName</code> in a direct records call is recorded with no registration in sight.</p><p>A push whose collection FAILS is refused, naming the error: the manifest is part of the version&#39;s identity, so there is no &quot;push now, collect later&quot;.</p><h2 id="typed-from-the-declaration" tabindex="-1">Typed from the declaration <a class="header-anchor" href="#typed-from-the-declaration" aria-label="Permalink to “Typed from the declaration”">​</a></h2><p>A function&#39;s <code>inputSchema</code> and <code>outputSchema</code> are enforced at every door — an HTTP invoke, a webhook or cron fire, a database change — and since the file declares them, the code and the caller can be typed from them too. On every push (and with <code>primitive functions codegen</code>), the CLI renders two more files from <code>functions/&lt;key&gt;.toml</code>:</p><ul><li><p><strong><code>functions/primitive-function-types.d.ts</code></strong> — <code>&lt;Key&gt;Input</code> and <code>&lt;Key&gt;Output</code> for every function, from its declared schemas, plus a <code>FunctionSchemas</code> augmentation keyed by function key. This is what types the <strong>keyed</strong> <code>defineFunction</code>:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// config/functions/greet/index.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// \`input\` is GreetInput; the return is checked against GreetOutput; a key</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// this tree does not declare is a compile error, and a key that names</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// another file&#39;s function is refused at push.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;greet&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { greeting: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`hello \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The unkeyed <code>defineFunction(async (input: MyInput, ctx) =&gt; …)</code> still works, with the types you write. In a tree that has not pushed yet the keyed form compiles with <code>any</code>, so nothing is blocked on the first push.</p></li><li><p><strong><code>functions/generated/&lt;key&gt;.generated.ts</code></strong> — a typed client invoker for your app&#39;s code, importing only from <code>js-bao-wss-client</code>. Its <strong>mode is fixed at generation time</strong>: a request function&#39;s invoker has exactly <code>invoke</code>, a task function&#39;s exactly <code>start</code>, <code>getStatus</code>, <code>waitFor</code> and <code>terminate</code>, so calling the wrong verb is a compile error rather than a <code>FUNCTION_MODE_MISMATCH</code> at runtime. <code>input</code> is required exactly when the schema rejects <code>{}</code>, and the task <code>waitFor</code> result&#39;s <code>output</code> is <code>&lt;Key&gt;Output</code>.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { greet } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;../primitive/dev/functions/generated/greet.generated&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { orderSync } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;../primitive/dev/functions/generated/order-sync.generated&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> answered</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> greet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(client).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">invoke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ input: { name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Ada&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">answered.output?.greeting; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// string | undefined</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> run</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> orderSync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(client).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ input: { orderId } });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> settled</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> orderSync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(client).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">waitFor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(run.runId);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">settled.output?.confirmedAt; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// typed from order-sync&#39;s outputSchema</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// greet(client).start(...) — compile error: a request function has no start</span></span></code></pre></div></li><li><p><strong><code>functions/generated/&lt;key&gt;.generated.swift</code></strong> — the same invoker for a Swift app, emitted by <code>primitive functions codegen --lang swift</code>. It carries <code>&lt;Key&gt;Input</code> / <code>&lt;Key&gt;Output</code> as <code>Codable</code> types and a <code>&lt;Key&gt;Function</code> struct reached through a <code>&lt;key&gt;(client)</code> factory, bound over the generic <code>client.functions</code> overloads. The mode fixes its verbs the same way: a request function&#39;s invoker has exactly <code>invoke</code>, a task function&#39;s exactly <code>start</code>, <code>getStatus</code>, <code>waitFor</code> and <code>terminate</code>. A function with no declared schema gets <code>typealias &lt;Key&gt;Input = JSONValue</code> rather than an empty struct, and a key that is not a legal Swift identifier is mangled (<code>123-job</code> → <code>_123JobFunction</code>, <code>_123Job(client)</code>) while the call still passes the original key.</p><div class="language-swift"><button title="Copy Code" class="copy"></button><span class="lang">swift</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> answered </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> try</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> greet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(client).</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">invoke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">GreetInput</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Ada&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">answered.output</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.greeting  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// String?</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> run </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> try</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> orderSync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(client).</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">nil</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">contextDocId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: docId)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> settled </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> try</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> orderSync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(client).</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">waitFor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">runId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: run.runId)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">settled.output</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.confirmedAt  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// typed from order-sync&#39;s outputSchema</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// greet(client).start(...) — compile error: a request function has no start</span></span></code></pre></div><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> codegen</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --lang</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> swift</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Sources/App/Functions/Generated</span></span></code></pre></div><p>Swift mode writes no TypeScript artifact and no tsconfig wiring; <code>--check</code> names the stale files and its hint carries <code>--lang swift</code>. The two languages can share <code>functions/generated/</code>: each sweeps only the files carrying its own banner. The Swift app template runs this from <code>scripts/codegen.sh</code> on every build path, so the committed invokers never drift.</p><p>On the Swift client there is <strong>no untyped entry point</strong>: <code>client.functions</code> takes an <code>Encodable</code> input and decodes into a <code>Decodable</code> output. A caller with no per-key type names the witness — <code>client.functions.invoke(key, input: nil as JSONValue?)</code> bound <code>as FunctionResult&lt;JSONValue&gt;</code>.</p><p>The invokers land under <code>functions/generated/</code> by default; <code>primitive functions codegen -o &lt;dir&gt;</code> writes them somewhere else. The scaffolded <code>functions/tsconfig.json</code> excludes <code>generated/**</code> — they are for the client, not for the function&#39;s own program — and a function whose TOML is removed loses its invoker on the next push. Both files are regenerated, never pushed; <code>config diff</code> and <code>functions codegen --check</code> name them when a schema edit has left them stale.</p></li></ul><p>The same push renders one more declaration, from the prompts in your tree rather than the functions:</p><ul><li><p><strong><code>functions/primitive-prompt-types.d.ts</code></strong> — <code>&lt;Key&gt;PromptOutput</code> for every <code>prompts/&lt;key&gt;.toml</code> that declares a <code>[prompt.outputSchema]</code>, and a <code>PromptSchemas</code> augmentation keyed by prompt key. It is what types <code>ctx.prompts.run(&quot;&lt;key&gt;&quot;)</code>: <code>parsed</code> on the success arm has the declared type, and a key this tree does not declare is a compile error. A prompt that declares no schema is listed with an empty entry, so its <code>parsed</code> stays optional and <code>unknown</code> — which means <strong>removing</strong> a schema turns a handler reading <code>parsed.field</code> into a compile error on the next push rather than a runtime <code>undefined</code>. See <a href="#typed-parsed-output">Typed, parsed output</a>.</p><p>The prompt key is the one push deploys: the file&#39;s <code>[prompt] key</code> when it declares one, its file name otherwise. A file named <code>local.toml</code> carrying <code>key = &quot;categorize&quot;</code> is typed under <code>categorize</code>, because that is the prompt the platform answers on.</p></li></ul><h2 id="modes-and-runners" tabindex="-1">Modes and runners <a class="header-anchor" href="#modes-and-runners" aria-label="Permalink to “Modes and runners”">​</a></h2><p>A function is a function. What changes is the <strong>runner</strong> it runs on, and the caller picks it at call time:</p><ul><li><strong>the request runner</strong> — <code>functions.invoke</code> — runs the code inside the call and hands back its result. It has to finish inside a request.</li><li><strong>the task runner</strong> — <code>functions.start</code> — starts a run, hands back a run id immediately, and lets the code sleep for hours or days in between. It is the durable one.</li></ul><p><code>mode</code> in the file says which runners a version allows, and <strong><code>any</code> is the default</strong> — the value you get by writing no <code>mode</code> line at all:</p><table tabindex="0"><thead><tr><th><code>mode</code></th><th>What it allows</th></tr></thead><tbody><tr><td><code>any</code> (the default)</td><td>Either runner. <code>invoke</code> runs it in the call; <code>start</code> runs it as a background run.</td></tr><tr><td><code>request</code></td><td>A <strong>lock</strong>: only <code>invoke</code>. <code>start</code> is refused <code>409 FUNCTION_MODE_MISMATCH</code>.</td></tr><tr><td><code>task</code></td><td>A <strong>lock</strong>: only <code>start</code>. <code>invoke</code> is refused <code>409 FUNCTION_MODE_MISMATCH</code>.</td></tr></tbody></table><p>So the file below takes both verbs, and nothing in it says so — that is the point:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/functions/order-sync.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order-sync&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/order-sync/index.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span></code></pre></div><p>Lock it when the code only makes sense one way. A function that sleeps for a day is not something you want anyone calling with <code>invoke</code> and waiting on, so say so out loud:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/functions/order-sync.toml — a deliberate LOCK: \`invoke\` is refused</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order-sync&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/order-sync/index.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mode = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;task&quot;</span></span></code></pre></div><p>The key the first releases taught, <code>durable = true</code>, is accepted as an alias for <code>mode = &quot;task&quot;</code> (and <code>durable = false</code> for <code>mode = &quot;request&quot;</code>). It is a boolean, so it names one runner and cannot say <code>any</code>: <code>durable</code> beside <code>mode = &quot;any&quot;</code> is refused naming both keys, the same way two locks that disagree always were. <code>config pull</code> writes a file back exactly as it was authored, whichever spelling it uses, so a tree that says <code>durable = true</code> keeps saying it and diffs Synced.</p><p><strong>If you already have functions pushed:</strong> an explicit lock is kept. A version whose file said <code>mode = &quot;request&quot;</code> — or <code>durable = false</code> — stays request-only, and nothing about it changes. A version whose file said <em>neither</em> now reads <code>any</code>, so it takes <code>start</code> as well as <code>invoke</code> with no re-push; <code>invoke</code> on it answers exactly what it answered before. Nothing about a function&#39;s authorization moves with the runner: the <code>access</code> gate, the capabilities, the input schema and the rate ceiling are all properties of the function, checked before the runner is chosen.</p><h3 id="step-under-each-runner" tabindex="-1"><code>step</code> under each runner <a class="header-anchor" href="#step-under-each-runner" aria-label="Permalink to “step under each runner”">​</a></h3><p>The handler&#39;s third argument, <code>step</code>, is there under <strong>both</strong> runners, so one body runs either way. Where they genuinely differ, the request runner says so rather than pretending:</p><table tabindex="0"><thead><tr><th></th><th>Task runner (<code>start</code>)</th><th>Request runner (<code>invoke</code>)</th></tr></thead><tbody><tr><td><code>step.do</code></td><td>Runs once and the result is <strong>memoized</strong>; a replayed slice does not re-run it.</td><td>Runs the body <strong>inline</strong>. Nothing is persisted and nothing is replayed.</td></tr><tr><td><code>step.sleep</code> / <code>step.sleepUntil</code></td><td><strong>Hibernates.</strong> Nothing runs and nothing is billed while it waits; hours and days are fine.</td><td>Really waits, when the remaining request <strong>budget</strong> covers it. When it does not, the invocation fails with <code>errorCode: &quot;FUNCTION_STEP_NEEDS_TASK&quot;</code>, naming the step, the duration and the budget left.</td></tr><tr><td><code>step.waitForEvent</code></td><td>Waits for the event.</td><td>Fails with <code>FUNCTION_STEP_NEEDS_TASK</code>: nothing can deliver an event to an invocation that has to return now.</td></tr></tbody></table><p>The thrown error&#39;s <code>name</code> is <code>STEP_NOT_AVAILABLE</code> in every refusal, so a body that catches it keeps catching it. A duration the platform cannot read (<code>&quot;3 fortnights&quot;</code>) is an ordinary throw naming the grammar — never a silent &quot;done&quot;.</p><p>This is the rule on <strong>every</strong> request-runner door, not just the HTTP one: a webhook delivery, a database-change fire, a cron entry whose <code>mode</code> is <code>request</code>, and a DSL workflow&#39;s <code>workflow.call</code> step all run the request runner and all answer <code>FUNCTION_STEP_NEEDS_TASK</code> for a wait they cannot cover. The remedy is always the same — start the function as a task instead.</p><p>The handler with <code>step</code>:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">orderId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> charged</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;charge&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.api.documents.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ documentId: input.orderId });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Hibernate. Nothing is running — and nothing is billed — while it waits.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sleep</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cooling-off&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;24 hours&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { orderId: charged.documentId, confirmedAt: Date.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">now</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h3 id="starting-one-and-polling-it" tabindex="-1">Starting one and polling it <a class="header-anchor" href="#starting-one-and-polling-it" aria-label="Permalink to “Starting one and polling it”">​</a></h3><p>The same route, <code>POST /app/{appId}/api/functions/{key}</code>. Because the function is a task it answers <code>201</code> with a run id instead of a result:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;runId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;01J…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;runKey&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;01J…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;instanceId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;app-doc-01J…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;running&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>From a client, <code>client.functions.start</code> answers that envelope, and <code>client.functions.getStatus</code> / <code>client.functions.waitFor</code> poll the run by its id:</p><div class="vp-code-group"><div class="tabs"><input type="radio" name="group-1123" id="tab-1124" checked><label data-title="JavaScript" for="tab-1124">JavaScript</label><input type="radio" name="group-1123" id="tab-1125"><label data-title="Swift" for="tab-1125">Swift</label></div><div class="blocks"><div class="language-ts active"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> run</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order-sync&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  input: { orderId },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  runKey: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`order-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">orderId</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// a repeat replays the existing run (existing: true)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Poll once…</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> now</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(run.runId);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(now.status);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// …or wait for a terminal state.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> settled</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">waitFor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">confirmedAt</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }&gt;(run.runId, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  timeoutMs: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">60_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// throws WORKFLOW_WAIT_TIMEOUT past it</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(settled.status, settled.output?.confirmedAt);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// The function key goes where a workflow key would.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">terminate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ functionKey: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order-sync&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, runKey: run.runKey });</span></span></code></pre></div><div class="language-swift"><button title="Copy Code" class="copy"></button><span class="lang">swift</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Confirmation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Decodable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Sendable </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> confirmedAt: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> run </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> try</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;order-sync&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;orderId&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orderId],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  runKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order-</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\\(orderId)</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // a repeat replays the existing run (existing == true)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Poll once…</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> now </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> try</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">getStatus</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">runId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: run.runId)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(now.status)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// …or wait for a terminal state.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> settled </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> try</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">waitFor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  runId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: run.runId,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: Confirmation.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  options</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">WaitForWorkflowOptions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">timeout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">60</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// throws workflowWaitTimeout past it</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(settled.status, settled.output</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.confirmedAt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// The function key goes where a workflow key would.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> try</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.functions.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">terminate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  FunctionRunRef</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">functionKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order-sync&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">runKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: run.runKey)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div></div></div><p>If you are not using the client, poll <code>GET /app/{appId}/api/workflows/runs/{runId}/status</code> directly. <code>functions.terminate</code> takes the function key and the run key.</p><p><code>timeoutMs</code> is accepted and ignored on a task start: the durable engine owns how long a run may take, and a per-request budget is a promise this path cannot keep across a <code>step.sleep</code>.</p><p>On a <strong>locked</strong> function, calling the other verb is a stable error, <code>FUNCTION_MODE_MISMATCH</code> — <code>functions.invoke</code> on a task-locked function, or <code>functions.start</code> on a request-locked one. The server refuses it before either runner executes, and the JS and Swift clients surface it as the same error they always threw. On an <code>any</code> function neither verb is wrong: that is what <code>any</code> means.</p><p>Which triggers a function may declare is decided per kind. A <strong>cron</strong> entry takes either runner, and on an <code>any</code> function the entry says which one — <code>mode</code> on the entry is required there, because nothing else in the system answers it, and it is refused when it names the runner a lock forbids. A <strong>webhook</strong> and a <strong>database-change</strong> watch always use the <strong>request runner</strong>, because both are waiting inside a request for the function to answer; a task LOCK is refused beside either. Neither is a dead end: a request fire can start a task function, the same way any other function can. That is the next section.</p><h3 id="starting-a-task-function-from-a-function" tabindex="-1">Starting a task function from a function <a class="header-anchor" href="#starting-a-task-function-from-a-function" aria-label="Permalink to “Starting a task function from a function”">​</a></h3><p><code>ctx.functions.start</code> starts a <strong>task</strong> function from inside a running one — from a request function, from a task function&#39;s slice, and from a webhook- or cron-fired invocation alike:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> started</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;digest-page&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { userIds }, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  runKey: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`digest-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// { runId, runKey, instanceId, status: &quot;running&quot; }</span></span></code></pre></div><p>It answers the same start envelope an HTTP start answers, so the run id polls on <code>workflows/runs/{runId}/status</code> and terminates on the instance routes like any other. Repeating a <code>runKey</code> <strong>replays</strong>: you get <code>existing: true</code> and the run that already exists, which is how a run key becomes a singleton — including when that run <strong>failed</strong>. A run key says &quot;this run, once&quot;, not &quot;this run, until it succeeds&quot;, so a retry is a new key with the same input.</p><p>There is no nested <em>invoke</em>. A request function is ordinary code, and code composes by being imported — put the shared logic in a module both functions import. Calling <code>ctx.functions.start</code> on a request function answers <code>FUNCTION_MODE_MISMATCH</code>.</p><p><strong>The callee&#39;s <code>access</code> expression is not evaluated.</strong> Your own gate was the authorization, and your code chose the callee, so a function can start one the invoking member could not call themselves. Decide who may reach the tree in the <code>access</code> expression of the function at the <em>top</em> of it.</p><p><strong>Who the run belongs to.</strong> Every run in an invocation tree — the root and every nested start under it — is keyed by the <strong>original external initiator</strong>: the member who made the HTTP call, or the app&#39;s system principal when a trigger fired it. So the member who started a tree polls and terminates every run in it, another member gets the routes&#39; <code>404</code>, and a trigger-rooted tree is an admin&#39;s to inspect, exactly as a cron-fired workflow run is today.</p><p><strong>Which document the run is keyed under.</strong> A durable run is addressed by a context document plus its run key, so a nested run always has one, resolved in this order:</p><ol><li>the <code>contextDocId</code> you pass;</li><li>else the <strong>parent run&#39;s</strong> context — so a whole tree shares one;</li><li>else the <strong>root caller&#39;s root document</strong>, which is the default a member&#39;s own start uses;</li><li>else <code>fn:&lt;the root function&#39;s id&gt;</code> — a synthetic context, the same shape a cron-fired run&#39;s <code>cron:&lt;triggerId&gt;</code> has. This is what a trigger-rooted tree gets, and it is why a run key there is a singleton per root function across fires rather than per member.</li></ol><p>Rule 2 reads the parent&#39;s run row. If that row has expired or been deleted mid-tree, resolution falls through to rules 3 and 4 — a rule, not an accident. The value comes back on the run&#39;s status response, and you need it to terminate a trigger-rooted child by key (<code>?contextDocId=fn:&lt;id&gt;</code>).</p><p>Both <code>runKey</code> and <code>contextDocId</code> are bounded at 100 characters.</p><p><strong>Inside the child</strong>, <code>ctx.trigger.kind</code> is <code>&quot;function&quot;</code> and carries the parent function&#39;s key and its run id (<code>null</code> when the parent was a request invocation, which writes no run row). <code>ctx.user</code> names the tree&#39;s root caller, and <code>ctx.user</code> is null when a trigger rooted the tree — nobody called it.</p><p><strong>Nesting is bounded at 4 levels.</strong> A root is level 0, so four nested starts fit below it; the fifth answers <code>409 FUNCTION_NEST_DEPTH_EXCEEDED</code>. The depth travels on the platform&#39;s own invocation credential, so it is not something your code can raise, and the ceiling is what stops a function that starts itself from running away.</p><p>Each run is listed under its own function — <code>primitive functions runs digest-page</code> — with <code>FIRED BY function</code> and a <code>PARENT</code> column naming the function that started it.</p><h3 id="iterate-every-user" tabindex="-1">Iterate every user <a class="header-anchor" href="#iterate-every-user" aria-label="Permalink to “Iterate every user”">​</a></h3><p>App-wide iteration is a cursor plus a nested start: one page per slice, one child run per page. Three functions, and you can copy them as they are.</p><p><strong>The trigger.</strong> A request function on a schedule, which starts the orchestrator with a run key derived from the date — so a second fire the same day replays rather than starting a second tree, and the cron&#39;s own <code>overlapPolicy</code> bounds this parent.</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># functions/nightly-digest.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nightly-digest&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/nightly-digest.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;false&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">triggers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cron</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nightly&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cron = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0 3 * * *&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">overlapPolicy = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;skip&quot;</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// functions/nightly-digest.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> date</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toISOString</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slice</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;digest-all-users&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { date }, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    runKey: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`digest-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>The orchestrator.</strong> A task function that takes one page per slice and hands each to a child. Both children LOCK themselves with <code>mode = &quot;task&quot;</code>: neither is anything but a background run, and <code>ctx.functions.start</code> is the only way in. (The lock is the authors&#39; choice, not a requirement — <code>ctx.functions.start</code> takes any callee that is not locked to the request runner, and a request LOCK answers <code>FUNCTION_MODE_MISMATCH</code> telling you to import its module instead.) Both declare <code>access = &quot;false&quot;</code>, because neither is ever invoked over HTTP: a nested start does not consult the callee&#39;s gate, so a closed gate costs the tree nothing and keeps the children off the member-callable surface.</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># functions/digest-all-users.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;digest-all-users&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/digest-all-users.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;false&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mode = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;task&quot;</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// functions/digest-all-users.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cursor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> page </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> users </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> taken</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`page-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      ctx.users.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">list</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ limit: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, cursor })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    );</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    users </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> taken.items.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`start-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      ctx.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;digest-page&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        { userIds: taken.items.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">u</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> u.userId) },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        { runKey: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`digest-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      )</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    );</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    cursor </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> taken.nextCursor;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    page </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">taken.hasMore </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">||</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> !</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cursor) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">break</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sleep</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`next-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1 second&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { pages: page, users };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The pages are fetched <strong>inside <code>step.do</code>, with an explicit cursor</strong>, and that is not a style choice: a memoized step does not advance an iterator, so <code>ctx.users.iterate</code> would replay pages it had already taken the moment the run resumed after the sleep. Use <code>iterate</code> in a request function or a single-slice loop; page by hand across slices.</p><p><strong>The page worker.</strong> A task function per page, so each page&#39;s budget is its own.</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># functions/digest-page.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;digest-page&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/digest-page.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;false&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mode = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;task&quot;</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// functions/digest-page.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { pMap } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;digest&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pMap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(input.userIds, (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">userId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.users.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(userId, { digest: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }), {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      concurrency: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  );</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>The budget arithmetic.</strong> A task slice may make 10 000 subrequests (a request invocation 128) and 10 minutes of wall-clock budget per slice, refreshed by the platform as it goes so the clock is continuous for up to 12 hours. At one call per user, a page of 100 fits with room for the start and the cursor read — thousands of times over. Neither ceiling binds a page of sends: it is seconds of work, and a single <code>step.do</code> may run for up to a whole slice budget. What does bind is the yield at half the subrequest ceiling (see Budgets in a task run): a worker that has made 5 000 counted calls in one slice ends the slice at its next step boundary, so keep pages bounded and let the child runs do the fanning out. <code>pMap</code>&#39;s default concurrency of 8 bounds how many are in flight, not how many run in total.</p><p><strong>Inside the children</strong>, <code>ctx.user</code> is null and <code>ctx.trigger.kind</code> is <code>&quot;function&quot;</code> — nobody called this tree, the schedule fired it. Every run in it is keyed under <code>fn:&lt;nightly-digest&#39;s id&gt;</code>, which the status response reports and which you pass as <code>?contextDocId=</code> to terminate a child by key.</p><p><strong>Partial failure is per page.</strong> A page that throws is one failed run in <code>primitive functions runs digest-page</code>, beside its completed siblings; the other pages are unaffected.</p><p><strong>Retrying a page takes a NEW run key.</strong> A run key is a singleton, and that is all it is: starting <code>digest-page</code> again with the key the failed run already holds answers <code>200 {existing: true}</code> describing that failed run, and nothing runs a second time. Give the retry its own key and the same input:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;digest-page&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { userIds },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { runKey: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`digest-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">page</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}-retry-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">attempt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Same context document, so the retry is keyed under the same tree and shows up beside the run it replaces. A child that ends without anybody polling it is settled by the dispatch sweep like any other task run.</p><h3 id="reporting-back" tabindex="-1">Reporting back <a class="header-anchor" href="#reporting-back" aria-label="Permalink to “Reporting back”">​</a></h3><p>A run never broadcasts over the WebSocket, so decide up front how its result reaches whoever cares. Three channels, and they compose:</p><ul><li><strong>The run&#39;s <code>output</code>.</strong> Return a value and it lands on the run status once the run completes — poll the run id whenever you like.</li><li><strong>Server state.</strong> Write database rows, documents or notifications — on the app&#39;s authority, attributed to the caller — inside <code>step.do</code>. This is the natural shape for a job that only needs to leave results behind.</li><li><strong>A live push.</strong> <code>ctx.users.send</code> reaches a client that is connected right now and nobody else — pair it with state or a notification if the message has to survive the client being away.</li></ul><h3 id="step-discipline" tabindex="-1">Step discipline <a class="header-anchor" href="#step-discipline" aria-label="Permalink to “Step discipline”">​</a></h3><p>The engine re-executes your handler from the top on every resume and replays each completed <code>step.do</code> from its <strong>stored result</strong> instead of running it again. Two rules follow, and both matter:</p><ul><li><strong>Put every side effect inside <code>step.do</code>.</strong> A charge, an email, a write made outside a step runs again on every resume. Inside one it runs exactly once, and its return value is what later resumes see.</li><li><strong>Do not branch on anything nondeterministic between steps.</strong> <code>Date.now()</code>, <code>Math.random()</code> or a fresh read at the top level can take a different path after a resume, and the engine then looks for steps that are not there. Read such values inside a step, so the value is stored with it.</li></ul><div class="warning custom-block"><p class="custom-block-title">Your code is pinned; the platform&#39;s is not</p><p>A run is pinned to the <strong>content hash</strong> of the version that started it: pushing a new version mid-run does not change what a running run executes, and new runs get the new code.</p><p>The platform&#39;s own code — the <code>primitive-functions</code> SDK and the runtime around your handler — is <strong>not pinned</strong>. A resume executes whatever is deployed at that moment. Treat the SDK as a stable interface rather than a frozen artifact.</p></div><h3 id="budgets-in-a-task-run" tabindex="-1">Budgets in a task run <a class="header-anchor" href="#budgets-in-a-task-run" aria-label="Permalink to “Budgets in a task run”">​</a></h3><p>The ceilings below apply <strong>per engine invocation, not per run</strong>. A run is a series of <em>slices</em>: the engine runs your handler from the top until it returns or reaches a sleep that has not finished, then hibernates; the next wake is a new slice, with completed steps replayed from storage.</p><ul><li><strong>A slice&#39;s wall clock is continuous, up to 12 hours; a step never starts short.</strong> Each slice is minted with a 10-minute credential, and every step begins with at least 5 minutes of it. When a step finishes with less than 5 minutes left, or is about to start with less than 5 minutes left, the platform REFRESHES the credential through the gateway — the same road every <code>ctx.api</code> call travels — and the slice&#39;s clock rolls straight on with no pause. So a single step may run up to 10 minutes (a two-minute LLM call, a slow integration, a large export all fit inside one <code>step.do</code>), and a sequence of steps runs as long as it needs, up to a 12-hour ceiling from the slice&#39;s start. The guarantee holds at every <code>step.do</code> boundary — including a cleanup step that runs after your handler catches a failed step, which begins on a refreshed budget rather than what the failed step left. Code outside steps spends the clock it runs in, and the next <code>step.do</code> entry refreshes it. A handler that never calls <code>step.do</code> — one long await — gets no refresh and dies at the 10-minute slice deadline.</li><li><strong>When a refresh is refused, the slice falls back to a pause of a little over 5 minutes.</strong> A refresh is refused past the 12-hour ceiling, more than once a minute (one per 60-second window, counted on the platform&#39;s epoch-aligned clock, so two refreshes 59 s apart across a window boundary are both honoured), or during a rate-limiter or engine outage. The fallback is the old behaviour: the platform ends the slice with a hibernating sleep — the engine hibernates only for a sleep past its own five-minute grace period, so that is what the fallback sleeps — and the next wake mints a fresh 10-minute slice with a fresh 12-hour ceiling. So a refused refresh costs a little over five minutes, once; an ordinary run past a refusal simply continues on the new slice. Because the fallback is a hibernation, what the run printed before it is not recoverable — it is not on the log record (see Debugging a failing function). Steps running in parallel (<code>Promise.all</code> over two chains) that reach a fallback yield together: it waits for every step in flight to finish before it sleeps, because the engine hibernates only when nothing is running. On a local development server the engine never hibernates: a fallback there is a pause with no fresh credential, and a task run is bounded by one slice.</li><li><strong>Two budgets do NOT refresh.</strong> Subrequests yield instead: they are counted across the slice and do not reset while it refreshes, and when the count passes half the resolved subrequest limit (5 000 of the 10 000 a task slice gets, or half a lower configured value) the next <code>step.do</code> boundary takes the fallback yield rather than refreshing, and the fresh slice resets the count. CPU is the other: a task slice gets 5 minutes of active CPU, and a step that exhausts it is killed by the runtime. The run does <strong>not</strong> recover from that kill today — it fails, with the kill as its error — and the ceiling is per slice, not per step: a refreshed slice keeps counting. So a step&#39;s own body must fit in 5 minutes of CPU, though its wall clock may be far longer, and a run whose steps together need more than 5 minutes of CPU must end the slice between them — a <code>step.sleep</code> past the engine&#39;s five-minute grace period hibernates it, and the wake starts a fresh CPU budget.</li><li><strong>The rate ceiling counts starts, not resumes.</strong></li><li><strong>The output ceiling and <code>outputSchema</code> apply once</strong>, to the final return value.</li></ul><p>So <code>step.sleep</code> between chunks is for waiting, not for budget: chunk a long job by steps, and sleep when there is something to wait for. A refresh does not count toward the engine&#39;s maximum number of steps per run, and neither does a fallback yield — <code>step.sleep</code> and <code>step.sleepUntil</code> are excluded from that allowance, and only your <code>step.do</code> calls count.</p><p>A task run&#39;s status answer says how its slice is doing: a <code>slice</code> block with when the slice started, its 12-hour <code>ceilingAt</code>, <code>refreshCount</code> and <code>lastRefreshAt</code>, and — once the slice ended — <code>settledAt</code> and <code>settledStatus</code>. <code>primitive functions runs</code> shows the same count in its <code>REFRESHES</code> column, so whether a long run is refreshing or falling back is visible at a glance.</p><h3 id="deleting-a-function-with-live-runs" tabindex="-1">Deleting a function with live runs <a class="header-anchor" href="#deleting-a-function-with-live-runs" aria-label="Permalink to “Deleting a function with live runs”">​</a></h3><p>A hard delete destroys the stored bundles a sleeping run reloads when it wakes, so it is refused while any of a function&#39;s runs has not settled — including through <code>primitive config push --prune</code>. The refusal names the live runs. Wait for them, or terminate them, and retry. To stop new runs meanwhile, <strong>archive</strong> the function: an archived function keeps its bundles, so a run already in flight finishes on the version it pinned.</p><p>While a hard delete that got past that check is running, the function stops accepting work: an invoke answers <code>404</code> and a task start that was already in flight answers <code>409</code> with <code>errorCode: &quot;FUNCTION_DELETE_IN_PROGRESS&quot;</code> rather than beginning a run whose code is about to be destroyed.</p><h2 id="ceilings" tabindex="-1">Ceilings <a class="header-anchor" href="#ceilings" aria-label="Permalink to “Ceilings”">​</a></h2><p>Every function runs under fixed platform ceilings. <code>[function.limits]</code> may <strong>lower</strong> any of them and can never raise one — a config asking for more simply gets the platform value.</p><table tabindex="0"><thead><tr><th>Limit</th><th>Platform value</th><th>Key</th></tr></thead><tbody><tr><td>CPU per invocation</td><td>5 000 ms request; 5 minutes per task slice</td><td><code>cpuMs</code> (see below)</td></tr><tr><td>Outbound subrequests</td><td>128 request; 10 000 per task slice</td><td><code>subRequests</code></td></tr><tr><td>Invocations per minute, per function</td><td>1 200</td><td><code>ratePerMinute</code></td></tr><tr><td>Wall clock, request</td><td>5 000 ms default, 30 000 ms ceiling</td><td><code>timeoutMs</code> on the request</td></tr><tr><td>— a request-runner <code>step.sleep</code> has to fit inside it</td><td>past it, the invocation fails <code>FUNCTION_STEP_NEEDS_TASK</code></td><td>— (start the function as a task)</td></tr><tr><td>Wall clock, task slice</td><td>continuous up to 12 hours; every step begins with at least 5 minutes</td><td>— (see Budgets in a task run)</td></tr><tr><td>Response size</td><td>1 MiB</td><td>—</td></tr><tr><td>Values per <code>$in</code> / <code>$nin</code> list</td><td>1 000</td><td>—</td></tr></tbody></table><p>A burst past the rate ceiling answers <code>429</code> with <code>errorCode: &quot;FUNCTION_RATE_LIMITED&quot;</code> and a <code>Retry-After</code> header. Buckets are per app and per function, so one function&#39;s traffic never consumes another&#39;s. A slot is taken only once a call is going to run: a caller your <code>access</code> rule rejects, or a request your <code>inputSchema</code> refuses, consumes none of the budget. When the limiter itself cannot be reached the call is refused with <code>503</code><code>FUNCTION_RATE_UNAVAILABLE</code> rather than run unmetered.</p><p><code>cpuMs</code> is the one ceiling the platform does not hold to the number you write. It is enforced — a function past its budget is stopped, and the invocation answers <code>failed</code> — but the runtime adds an allowance of its own before it stops anything, measured at roughly <strong>two seconds</strong>. So <code>cpuMs = 50</code> and <code>cpuMs = 200</code> buy the same thing in practice, and neither stops your code in fifty or two hundred milliseconds. Lowering <code>cpuMs</code> still lowers the ceiling where the value is large enough to matter; below the allowance it is a declaration of intent rather than a cap you can rely on. If you need a function bounded tightly, bound it with <code>timeoutMs</code> on the request, which is wall clock and is held exactly.</p><p>The response-size ceiling is the platform&#39;s, not your sandbox&#39;s: the platform reads your response with a byte cap and measures the parsed <code>output</code> (and <code>error</code>) with its own encoder. An output over 1 MiB is <code>{&quot;status&quot;:&quot;failed&quot;, &quot;errorCode&quot;:&quot;OUTPUT_TOO_LARGE&quot;}</code> whatever the code inside the sandbox did — page your results or write them to a database and return a handle.</p><p>The wall-clock budget starts when the platform accepts the invocation, so loading a function&#39;s code the first time comes out of it — and the credential the sandbox carries expires on that same clock, never later than the answer the caller sees.</p><div class="warning custom-block"><p class="custom-block-title">What a timeout does, and does not, stop</p><p>When the budget runs out the caller gets <code>{&quot;status&quot;:&quot;timeout&quot;}</code> and the sandbox&#39;s request is aborted. The platform cannot <strong>kill</strong> a running sandbox — the runtime offers no such control — so a function that ignores the abort may keep executing until its <code>cpuMs</code> ceiling stops it.</p><p>What it can no longer do is act. The invocation&#39;s credential carries an absolute deadline, checked independently on every platform call, so a function that outlives its own invocation is holding something that has already stopped working: its late calls are denied and its side effects never land. Write functions that finish inside their budget; do not rely on work continuing after a timeout, and do not fear it either.</p><p>The same deadline bounds what your function starts outside the platform. An <code>ctx.integrations.call</code> is cut off at whichever comes first, the integration&#39;s own timeout or what is left of the invocation — including while its response body is still arriving, not merely while it waits for a reply — and answers <code>UPSTREAM_TIMEOUT</code>. A <code>ctx.prompts.run</code> whose provider call passes the deadline answers <code>PROMPT_UPSTREAM_TIMEOUT</code>, which is worth catching separately from an ordinary model failure: it means the budget ran out, not that the model did.</p></div><h2 id="triggers" tabindex="-1">Triggers <a class="header-anchor" href="#triggers" aria-label="Permalink to “Triggers”">​</a></h2><p>A function does not have to wait for an HTTP call. Its config block can declare an inbound <strong>webhook</strong>, any number of <strong>cron</strong> schedules, and any number of <strong>database-change</strong> watches, and the platform creates and operates whatever they need — there is no separate webhook or cron object to manage. A triggered which modes it takes depends on the kind. A <strong>cron</strong> entry runs on a request or a task function: on a task function each fire starts a run. A <strong>webhook</strong> and a <strong>database-change</strong> watch require request mode, because the delivery or the write is waiting for the function&#39;s answer inside the request — those hand long work to a task function with <code>ctx.functions.start</code>.</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/functions/stripe-events.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stripe-events&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/stripe-events/index.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># One webhook per function. It answers on the function&#39;s own key.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">triggers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">webhook</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">verificationScheme = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stripe&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">signingSecret = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;{{secrets.STRIPE_WEBHOOK_SECRET}}&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">triggers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cron</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nightly&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cron = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0 3 * * *&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">timezone = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;UTC&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">rootInput = { mode = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;full&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">triggers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cron</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hourly-sweep&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cron = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0 * * * *&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">overlapPolicy = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;skip&quot;</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// config/functions/stripe-events/index.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (ctx.trigger.kind </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;webhook&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // webhookKey (the function key), webhookId, externalEventId (the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // provider&#39;s X-Webhook-Event-Id, or null)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { received: input.type, event: ctx.trigger.externalEventId };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (ctx.trigger.kind </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;cron&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // name, triggerId, scheduledFor (ISO 8601) — and manual: true on a fire</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // from \`primitive cron-triggers test\`, absent otherwise</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { swept: ctx.trigger.name, at: ctx.trigger.scheduledFor };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { invokedBy: ctx.user?.userId </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><code>ctx.trigger</code> is a union narrowed on <code>kind</code>, and each arm&#39;s fields are typed — they are the fields the platform builds, held equal to the declaration by a test, so a field you read is one the fire really sets. <code>ctx.user</code> is <code>FunctionUser | null</code>: a function a trigger can fire reads it as <code>ctx.user?.userId</code>.</p><p><code>primitive config push</code> applies it, and <code>primitive functions get &lt;function-id&gt;</code> shows the result — the receiver URL to give the provider, the schedule the platform holds, the database types it watches, and when each last fired.</p><h3 id="the-webhook-trigger" tabindex="-1">The webhook trigger <a class="header-anchor" href="#the-webhook-trigger" aria-label="Permalink to “The webhook trigger”">​</a></h3><p>The receiver path carries the function&#39;s key: <code>POST /app/{appId}/webhook/{functionKey}</code>. Verification, replay protection and a delivery log are built in.</p><p><code>verificationScheme</code> is required and <code>&quot;none&quot;</code> is refused: the receiver is a public URL, so an unverified delivery would let anyone run the function. Accepted keys are <code>verificationScheme</code>, <code>signingSecret</code>, <code>toleranceSeconds</code>, <code>deduplicationEnabled</code>, <code>deduplicationWindowMs</code>, <code>maxBodyBytes</code>, <code>secretGracePeriodMs</code>, and a <code>[function.triggers.webhook.verification]</code> table for scheme-specific settings (a Discord public key, a JWT&#39;s JWKS, …).</p><p>A verified delivery runs the function inline, in request mode, and answers <code>200 {&quot;received&quot;: true}</code> <strong>whatever the function did</strong> — the delivery was accepted, and the run row records the outcome. A delivery that cannot run right now (the function is disabled, or the burst is past its rate ceiling) is answered <code>202</code> with the reason in the delivery log and no dedup key stored, so the provider&#39;s redelivery still runs once the condition clears.</p><p>To exercise the trigger end to end without a provider, take the <code>webhookId</code><code>primitive functions get &lt;key&gt;</code> prints and run <code>primitive webhooks test &lt;webhook-id&gt; --payload &#39;{…}&#39; --deliver</code>: it signs the payload and posts it to the receive endpoint above, so verification, dedup and dispatch all run for real, and it reports the run id to inspect with <code>primitive functions runs &lt;function-id&gt;</code>. That is a real delivery — it writes a delivery row and spends the body&#39;s dedup key — so see <a href="./inbound-webhooks.html#delivering-the-payload-for-real-with-deliver">Delivering the payload for real</a> before using it on a scheme whose suppression never expires.</p><h4 id="work-that-outlasts-the-delivery" tabindex="-1">Work that outlasts the delivery <a class="header-anchor" href="#work-that-outlasts-the-delivery" aria-label="Permalink to “Work that outlasts the delivery”">​</a></h4><p>A webhook delivery always runs the <strong>request runner</strong>, and push refuses <code>mode = &quot;task&quot;</code> beside <code>[function.triggers.webhook]</code>: a delivery needs its answer inside the request and a task run has none to give there — it returns a run id and finishes later. (An <code>any</code> function is fine; it is the LOCK to the task runner that cannot answer.) When the work a delivery kicks off is longer than one bounded invocation, the pattern is two functions: the one the provider calls verifies and acknowledges, and hands the work to a task function.</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/functions/provider-events.toml — the request function the provider calls</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;provider-events&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/provider-events.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mode = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;request&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">triggers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">webhook</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">verificationScheme = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;custom&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">signingSecret = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;{{secrets.PROVIDER_WEBHOOK_SECRET}}&quot;</span></span></code></pre></div><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/functions/process-batch.toml — the task function that does the work</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;process-batch&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/process-batch.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mode = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;task&quot;</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// functions/provider-events.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">delivery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Verification already happened: the receiver would not have run this.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.functions.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;process-batch&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { batchId: delivery.batchId },</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // The run key makes the start idempotent. Use the provider&#39;s own event id,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // or an id from the body when a redelivery should replay the same work:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // a second delivery with the same key answers with the FIRST run rather</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // than starting a second one.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { runKey: delivery.batchId }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  );</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The delivery is answered as soon as the run exists — usually in tens of milliseconds — and the task function keeps going for as long as it needs. <code>primitive functions runs &lt;process-batch-id&gt;</code> lists each run with the webhook function as its parent.</p><h3 id="cron-triggers" tabindex="-1">Cron triggers <a class="header-anchor" href="#cron-triggers" aria-label="Permalink to “Cron triggers”">​</a></h3><p>Each entry needs a <code>name</code>; it is half of the trigger&#39;s identity and appears on every run. <code>cron</code> is a standard five-field expression, <code>timezone</code> is IANA (default UTC), <code>overlapPolicy</code> is <code>skip</code> (default) or <code>allow</code>, and <code>rootInput</code> is the object the function receives. A function may declare up to ten.</p><p>Each entry also says <strong>which runner its fires use</strong>. Under <code>mode = &quot;request&quot;</code> a fire is one bounded invocation; under <code>mode = &quot;task&quot;</code> each fire starts a run, with the same slice budgets, the same run routes and the same <code>primitive functions runs</code> listing as a run somebody started by hand.</p><p>On a function whose mode is <code>any</code>, <code>mode</code> on the entry is <strong>required</strong>: nothing else in the system says whether a given schedule means &quot;run it now&quot; or &quot;start a run&quot;, and guessing would be the platform choosing for you. On a LOCKED function it is optional and may not name the other runner — an entry saying <code>mode = &quot;task&quot;</code> on a request-locked function is refused at push, naming both. A declaration written before the key existed keeps firing under the request runner, exactly as it did.</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/functions/categorise-sweep.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;categorise-sweep&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/categorise-sweep.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">triggers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cron</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;nightly&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cron = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0 3 * * *&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mode = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;task&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">overlapPolicy = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;skip&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># The same function, swept lightly every hour — inside the fire, not as a run.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">triggers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cron</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hourly-touch&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cron = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0 * * * *&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mode = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;request&quot;</span></span></code></pre></div><p><code>primitive functions get &lt;key&gt;</code> prints each entry&#39;s runner in its RUNNER column, so an operator can see which schedule does which without reading the file.</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// functions/categorise-sweep.ts — ctx.user is null; nobody called this.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> batch </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; batch </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; batch</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> step.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`batch-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">batch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      /* one batch of the sweep; the platform yields between steps when a slice runs low */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>With <code>overlapPolicy = &quot;skip&quot;</code>, a scheduled fire that arrives while the previous run is still going is counted as a skip instead of starting a second one. For a task function that question is put to the <strong>engine</strong>, not to the run row: nothing marks the row finished until somebody polls it, so a schedule that read the row would stall behind a run that is already over. A <strong>sleeping</strong> run counts as live — a run part-way through a <code>step.sleep</code> is still going — and so does a run the platform cannot be asked about, because a probe that cannot be answered must not become a second copy of a job that may still be running. Such a cycle is counted as a skip and the next one asks again.</p><p><code>overlapPolicy = &quot;allow&quot;</code> starts one run per fire.</p><p>Prefer starting the task from a <strong>request</strong> function with <code>ctx.functions.start</code> when you want something a cron entry cannot express: a run key, so a fire that arrives twice in a period replays the first run instead of starting a second (<code>{ runKey: \\</code>digest-\${today}\` }<code>), or an input computed at fire time rather than the fixed </code>rootInput\` — the &quot;Iterate every user&quot; example above is exactly that shape.</p><h3 id="database-change-triggers" tabindex="-1">Database-change triggers <a class="header-anchor" href="#database-change-triggers" aria-label="Permalink to “Database-change triggers”">​</a></h3><p>A <code>[[function.triggers.database]]</code> block watches a <strong>database type</strong>. Every write request that commits something to any database of that type runs the function once, with the committed changes:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/functions/order-watcher.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;order-watcher&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/order-watcher.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">triggers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">database</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;orders&quot;</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// config/functions/order-watcher.ts</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Watches \`orders\`; writes \`shipments\` — a type it does NOT watch — and</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// publishes. A watcher that wrote back to \`orders\` would fire itself once</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// more and then be refused (see Loops below), so write to a different type,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// or make the handler idempotent on \`change.data\` if it must write its own.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> SHIPMENTS_DB</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// the shipments database&#39;s id</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (ctx.trigger.kind </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;database&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ctx.trigger.databaseId / ctx.trigger.databaseType say which database.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> shipments</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">db</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">SHIPMENTS_DB</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;shipments&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Shipment&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> change</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> input.changes) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // change.op, change.modelName, change.id, change.data and</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // change.previousData — on every change.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (change.op </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;save&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> change.previousData </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> shipments.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">save</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ data: { orderId: change.id } });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.channels.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">publish</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`orders:\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">change</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">id</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        op: change.op,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        label: change.data?.label </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { seen: input.totalChanges };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>input</code> is <code>{ databaseId, databaseType, changes, totalChanges, truncated }</code>.</p><h4 id="the-delivery-contract" tabindex="-1">The delivery contract <a class="header-anchor" href="#the-delivery-contract" aria-label="Permalink to “The delivery contract”">​</a></h4><p>Delivery is best-effort, post-commit, and bounded. Each clause is deliberate:</p><ul><li><strong>After the write&#39;s answer, from committed operations only.</strong> The fire is dispatched off the response path once the write&#39;s own answer has been decided. A database batch is partial-success inside an HTTP <code>200</code>, and only the operations that actually committed reach <code>changes</code>. A write that failed never becomes a change your function acts on, and the write&#39;s caller never waits for the function or sees its outcome.</li><li><strong>Once per write request, not once per row.</strong> A batch of fifty saves is one invocation carrying fifty changes. Inside one fire, <code>changes</code> are in the order the database committed them; <code>changes</code> is capped at 50, and <code>totalChanges</code> and <code>truncated</code> stay honest about the rest.</li><li><strong>No ordering promise across write requests.</strong> Two writes to one database from separate write requests are two fires, and they carry no order: they may run concurrently or arrive out of order. A function that needs the current state of a row reads it rather than trusting the payload to be the latest.</li><li><strong>No retry, no queue.</strong> A fire that fails, times out, or is refused by the rate ceiling leaves the write&#39;s response exactly as it was and is not retried. Publish, write, or call out if you need a durable record of what it did.</li><li><strong>Bounded fanout, as enforced.</strong> One type may be watched by up to 25 functions — <code>config push</code> refuses the 26th watcher of a type by name — and a fire reaches every watcher the platform accepted; a function may watch up to five types.</li><li><strong>The rows are in the payload.</strong> Every change carries <code>op</code>, <code>modelName</code>, <code>id</code>, <code>data</code> and the pre-image of the write in <code>previousData</code> (<code>null</code> when there was no row before it) — read for you whether or not anyone is subscribed to the database.</li><li><strong>No run row.</strong> Unlike a webhook or cron fire, a database-change fire writes nothing to <code>primitive functions runs</code>. It leaves an invocation metric and, on failure, an application error event.</li><li><strong>Request mode only, and with no caller.</strong> <code>mode = &quot;task&quot;</code> and a database trigger are refused together at push, and <code>ctx.user</code> is <code>null</code> inside the fire — nobody called it, and the code acts as the system as it always does. Hand slow work to a task function with <code>ctx.functions.start</code>.</li></ul><p><code>type</code> must name a database type the app already has; a push naming one it does not have is refused by name. A write to a database with no <code>databaseType</code> set fires nothing — there is no type key to look up.</p><h4 id="loops" tabindex="-1">Loops <a class="header-anchor" href="#loops" aria-label="Permalink to “Loops”">​</a></h4><p>A fire&#39;s own writes fire triggers too, so a function that writes to a type it watches — or two functions that write each other&#39;s types — would re-fire without end. The platform bounds this with an <strong>origin chain</strong>: every database-change fire carries the ids of the functions fired so far in its causal chain, and the fired function&#39;s writes carry that chain to the fires they plan. The planner refuses to fire a target whose id is already in the chain (<code>LOOP_DETECTED</code>), and refuses any fire past a depth of 4 (<code>DEPTH_EXCEEDED</code>). So a function that writes to the type it watches fires itself once and stops; A-writes-B-writes-A stops at the first repeat; and a chain of five watchers each writing the next one&#39;s type fires four of them.</p><p>An HTTP invocation, a webhook, a cron and a <code>workflow.call</code> are roots with an empty chain: a function invoked over HTTP that writes the type it watches is fired once by that write, and that fire&#39;s own write is then refused.</p><p>A refused fire is <strong>not an error</strong>. The write it would have followed commits and answers exactly as before, no application error event is written, and the function&#39;s logs show nothing for it — it never ran. It is <strong>counted</strong> in the function&#39;s invocation metrics as a <code>function.invoke</code> event with <code>status: &quot;refused&quot;</code> and the reason, attributed to the user whose write started the chain, and it is <strong>logged</strong> as one <code>server-function db-change trigger … outcome=refused</code> line per function, reason and minute, carrying the chain, its depth and how many refusals the line stands for. The recommended shape is the example above: write to a type you do not watch, or make the handler idempotent on <code>change.data</code> so the one re-fire it gets is harmless.</p><h3 id="what-a-trigger-fire-records" tabindex="-1">What a trigger fire records <a class="header-anchor" href="#what-a-trigger-fire-records" aria-label="Permalink to “What a trigger fire records”">​</a></h3><p>Unlike an HTTP invocation, <strong>a webhook or cron fire writes a run row</strong> — that is the only record it leaves, since nobody is waiting for an answer. (A database-change fire deliberately writes none; see above.)</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>Runs come back newest first with the status, what fired them, the timings, the failure code and — for a task run — how many times its slice was refreshed (<code>REFRESHES</code>). A trigger fire has no caller, so <code>ctx.user</code> is <code>null</code> inside the function; the run&#39;s <code>executionPrincipal</code> records the app&#39;s system principal.</p><h3 id="editing-and-removing-triggers" tabindex="-1">Editing and removing triggers <a class="header-anchor" href="#editing-and-removing-triggers" aria-label="Permalink to “Editing and removing triggers”">​</a></h3><p>The declaration in the file is the whole truth. Removing the webhook block stops deliveries (the receiver answers <code>410</code>); putting it back resumes them on the same URL, with the delivery log intact. Removing a cron entry cancels its alarm; re-adding it schedules it again.</p><div class="tip custom-block"><p class="custom-block-title">Rotating a webhook secret</p><p><code>primitive webhooks rotate-secret &lt;webhook-id&gt;</code> works on a function&#39;s webhook, and the rotation <strong>survives later pushes</strong>: a push only touches the signing secret when the value written in the TOML itself changed.</p></div><h2 id="recording" tabindex="-1">Recording <a class="header-anchor" href="#recording" aria-label="Permalink to “Recording”">​</a></h2><p>Every HTTP invocation writes one <code>function.invoke</code> analytics event, attributed to the <strong>caller</strong> — the signed-in user who made the request, never the app itself, though the code ran as the app. It carries the function key, the version that ran, the terminal status and the duration. A trigger fire has no caller and writes no per-user event; its record is the run row (webhook, cron) or the invocation metric (database change).</p><p>The event is a COUNT — it carries no message and no error — so it answers &quot;how often&quot; and never &quot;why&quot;. For why, read the invocation records below. A failed invocation&#39;s event does report <code>outcome: &quot;error&quot;</code> through the shared inspection contract, so failures are countable.</p><h2 id="debugging-a-failing-function" tabindex="-1">Debugging a failing function <a class="header-anchor" href="#debugging-a-failing-function" aria-label="Permalink to “Debugging a failing function”">​</a></h2><p>Every invocation the platform dispatched toward the sandbox leaves a record, and <code>console.log</code> is where you read it back:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              # newest first</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --json</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # the shared inspection items</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --follow</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # tail as invocations happen</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --limit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 50</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --cursor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">curso</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">r</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --run</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # one task run&#39;s records</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --invocation</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # one record, by id</span></span></code></pre></div><p><code>--invocation</code> takes the <code>invocationId</code> an invoke answered with, and names one record: it cannot be combined with <code>--run</code>, <code>--follow</code>, <code>--cursor</code> or <code>--limit</code>, which all shape a listing. <code>--run</code> narrows to one run&#39;s records and cannot be combined with <code>--follow</code>, because a run&#39;s records are a bounded set and a tail walks the whole index.</p><p>An id that names nothing, one belonging to another function, and one whose seven days are up all answer the same not-found: the platform does not say whether a record it will not show you ever existed.</p><p>A record carries what the invocation printed (<code>console.log</code>/<code>info</code>/<code>debug</code>/ <code>trace</code> as stdout, <code>warn</code>/<code>error</code> as stderr), the thrown error with its code and a bounded stack, and the correlation keys: the app, the function, the config version that ran, the run id for a trigger fire or a task run, and what triggered it. <strong>Records are kept for seven days.</strong></p><p>Every door writes one — an HTTP invocation, a webhook delivery, a cron fire, a database-change fire, a <code>workflow.call</code> from a DSL workflow, and a task run&#39;s settling slice. A timed-out invocation writes one too, carrying what the function printed <strong>before</strong> it hung, which is usually the whole story.</p><p>A <strong>gate refusal</strong> writes none. A 403 from the access rule, a 404 for an unknown key, a 409 for a version that was never pushed, a 400 from the input schema and a 429 from the rate ceiling are all answered in the HTTP response and debugged from there; they never reached your code, and rows a caller can make on demand are rows a caller controls.</p><p>A function can read its own records too — <code>ctx.api.functions.logs({ functionKey, limit })</code> — and so can the admin API. Both are <strong>owner and admin only</strong>.</p><div class="warning custom-block"><p class="custom-block-title">Secrets are redacted, best-effort</p><p>A value <code>ctx.secret()</code> returned is replaced with <code>[REDACTED:&lt;NAME&gt;]</code> in the captured lines, in the lines forwarded to the live console, and in the error message and stack, on both sides of the sandbox boundary. It is best-effort by nature: a secret your code transformed before printing — sliced, encoded, interpolated into a signature — is not detectable. Do not print credentials.</p></div><p><strong>Task functions have a narrower guarantee.</strong> A task run&#39;s record is written when the slice that SETTLES it finishes. A <code>step.do</code> body that ran in an earlier slice does not re-execute on replay, so what it printed before a <code>step.sleep</code> is not in the record — and the same is true of a fallback yield the platform takes between steps when a slice runs low (see Budgets in a task run), which is a hibernation like any other sleep. The platform has no way to observe a step boundary from outside the sandbox, and a slice that suspends at a sleep never settles. Print what you need in the slice that settles, or write progress as data.</p><p>Three markers are worth knowing. <code>truncated</code> says a line or the whole channel hit its cap (2 KiB per line, 16 KiB and 256 entries per invocation); the invocation itself is never failed for logging too much. <code>logsUnavailable</code> says the platform could not retrieve the buffer at all — an evicted isolate, or a dispatch refused before any code ran — which is different from a function that printed nothing. <code>contentSuppressed</code> says the platform could not load <strong>every</strong> secret the version declares when it came to write the record — the store did not answer, or a declared <code>secret:&lt;NAME&gt;</code> has no value in this environment — so it kept the correlation and the status and dropped the console and the error fields rather than publish them unredacted. A declared secret that is not provisioned suppresses every record of that version, including the <code>FUNCTION_SECRET_NOT_FOUND</code> it would otherwise show you: provision the secret, or drop the declaration, and invoke again.</p><p><strong><code>--follow</code> shows a slow invocation even when a faster one settled first.</strong> An invocation&#39;s id is minted when it STARTS and its record is written when it SETTLES, so a call that began before another and finished after it is inserted into the listing <em>below</em> rows the tail has already printed. The tail therefore looks back a minute past its own high-water mark — the longest a request record can lag its id, the 30 s request ceiling plus the token&#39;s grace — and remembers which ids inside that window it has already shown you, so each row is printed once. Those slow, failed and timed-out invocations are the ones you are tailing for.</p><p>Records <strong>outlive the function</strong>. Archiving a function does not remove its records, and both read surfaces keep answering for it until the seven days are up — which is the point, since a function is usually archived because it was failing.</p><h2 id="operating-a-function" tabindex="-1">Operating a function <a class="header-anchor" href="#operating-a-function" aria-label="Permalink to “Operating a function”">​</a></h2><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # keys, status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # …plus the active version, its capabilities, its manifest and its triggers</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # what its webhook and schedules fired</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> steps</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # one durable run, step by step</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> terminate</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # end a run that will not settle</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # what it printed, and what it threw</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> disable</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> enable</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> archive</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><h3 id="running-one" tabindex="-1">Running one <a class="header-anchor" href="#running-one" aria-label="Permalink to “Running one”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> invoke</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;n&quot;:21}&#39;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # request mode: run it, print the result</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --input</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;n&quot;:21}&#39;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # task mode: start a run, print its id</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --wait</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # …and wait for it</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wait</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # wait for a run already started</span></span></code></pre></div><p><code>invoke</code> and <code>start</code> take the <strong>key</strong> — what you wrote in <code>functions/&lt;key&gt;.toml</code> — because that is what the public route takes. <code>runs</code>, <code>runs wait</code>, <code>runs steps</code>, <code>runs terminate</code> and <code>logs</code> take the <strong>function id</strong>, as they always have; every verb prints the ids it produced with the command that takes each one underneath, so you never have to look one up:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Status  completed</span></span>
<span class="line"><span>Output</span></span>
<span class="line"><span>{</span></span>
<span class="line"><span>  &quot;doubled&quot;: 42</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>Ran as 01K3XV… (owner)</span></span>
<span class="line"><span>Function ID 01K3XW…</span></span>
<span class="line"><span>  → primitive functions logs 01K3XW…</span></span>
<span class="line"><span>Invocation ID 01K4ANRRG0Q7VZ3M8T6XW2H5NB</span></span>
<span class="line"><span>  → primitive functions logs 01K3XW… --invocation 01K4ANRRG0Q7VZ3M8T6XW2H5NB</span></span></code></pre></div><p>The <strong>wrong verb is refused, not converted</strong> — on a LOCK. <code>invoke</code> on a <code>mode = &quot;task&quot;</code> function and <code>start</code> on a <code>mode = &quot;request&quot;</code> one both stop before any request is made, naming the other verb, and if the version is repointed between that check and the call the platform refuses it too, with nothing executed. On a function whose mode is <code>any</code> — the default — both verbs are admitted and each gets the runner it names.</p><p><strong>Exit codes</strong>, so a script can branch on them:</p><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th></tr></thead><tbody><tr><td><code>0</code></td><td>The invocation completed, or the run you waited for completed.</td></tr><tr><td><code>1</code></td><td>It failed, timed out, was terminated, or the platform refused the call.</td></tr><tr><td><code>124</code></td><td><code>runs wait</code> spent its budget with the run still going. It prints the command that resumes the wait.</td></tr><tr><td><code>130</code></td><td>Ctrl-C. Same resume line; a second Ctrl-C exits at once.</td></tr></tbody></table><p><code>--timeout &lt;seconds&gt;</code> is the request budget for <code>invoke</code> (the platform clamps at 30 s) and the WAIT budget for <code>runs wait</code> and <code>start --wait</code>, which defaults to 900.</p><h3 id="who-it-runs-as" tabindex="-1">Who it runs as <a class="header-anchor" href="#who-it-runs-as" aria-label="Permalink to “Who it runs as”">​</a></h3><p>By default, <strong>your own app user</strong> — the one the platform provisions for an admin on first use. The output says so (<code>Ran as &lt;user-id&gt; (&lt;role&gt;)</code>), and the role matters: owners and admins <strong>bypass a function&#39;s <code>access</code> expression</strong>, so your own invocation does not exercise the gate. Use <code>--user</code> when you want it exercised.</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> invoke</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --user</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">user-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # as that app user</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> invoke</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --as</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> system</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # with no caller at all</span></span></code></pre></div><p><code>--user</code> mints a ten-minute API token for that user, makes the call with it, and revokes it afterwards — on every path, including Ctrl-C. The token&#39;s value is never printed. If the revoke itself fails you get a warning naming the token and <code>primitive tokens revoke &lt;id&gt;</code>; the exit code is still the invocation&#39;s. Owner and admin only, enforced by the server.</p><p><code>--as system</code> runs the function the way a <strong>cron, webhook or database-change fire</strong> does: <code>ctx.user</code> is <code>null</code>, <code>ctx.trigger</code> is <code>{ kind: &quot;manual&quot;, userId: &lt;you&gt; }</code>, and the code carries the app&#39;s system authority. It is how you exercise a trigger-fired code path without faking the trigger. Owner and admin only, and a task started this way is keyed to the system exactly as a trigger-fired root is — <code>FIRED BY manual</code>, with you recorded as the initiator.</p><p>The two flags are mutually exclusive, and <code>--as</code> accepts only <code>system</code>.</p><div class="tip custom-block"><p class="custom-block-title">A task start mints your root document</p><p><code>functions start</code> needs a context document, and the app user the platform provisions for an admin has none until something creates one. The CLI asks for it — through the same idempotent route sign-in uses — so <code>start</code> works on a fresh app without you doing anything first. Pass <code>--context-doc-id</code> to name one yourself. <code>--as system</code> uses the synthetic <code>fn:&lt;function-id&gt;</code> context that every trigger-rooted run uses, and asks for nothing.</p></div><h3 id="reading-a-durable-run-step-by-step" tabindex="-1">Reading a durable run step by step <a class="header-anchor" href="#reading-a-durable-run-step-by-step" aria-label="Permalink to “Reading a durable run step by step”">​</a></h3><p><code>functions runs</code> answers at the level of the run: status, start, end, the failure code. For a <code>mode = &quot;task&quot;</code> function that is rarely enough — the handler&#39;s work is in its named steps, and what you want to know is which one it is on, which ones already finished, and where the time went.</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> steps</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>Each step comes back with its name, its kind (<code>function.step</code> for a <code>step.do</code>, <code>function.sleep</code> for a <code>step.sleep</code> or <code>step.sleepUntil</code>, <code>function.event</code> for a <code>step.waitForEvent</code>), its status, the idle GAP before it, and its duration. A step that is <strong>still executing</strong> reads <code>running</code> with the time it has been running so far — including a run that is asleep, whose <code>function.sleep</code> step stays <code>running</code> until it wakes. A step that threw reads <code>failed</code> with its message.</p><p>The rows are written as the run goes, so they are readable while it is still in flight, and they survive the hibernation of a <code>step.sleep</code>. A resumed run replays its completed steps from the engine&#39;s memo rather than re-running them: each step keeps <strong>one</strong> row, with the timing of the slice that actually executed it. <code>--json</code> emits the same envelope <code>primitive workflows runs steps</code> does.</p><div class="tip custom-block"><p class="custom-block-title">Which command for which run</p><p><code>primitive workflows runs steps</code> addresses a <strong>DSL workflow</strong> by its id or key; <code>primitive functions runs steps</code> addresses a <strong>server function</strong>. A function run has no <code>workflowId</code>, so the workflow-scoped verbs cannot see one — passing a function id or key to them answers &quot;Workflow not found&quot;.</p></div><h3 id="ending-a-run-that-will-not-settle" tabindex="-1">Ending a run that will not settle <a class="header-anchor" href="#ending-a-run-that-will-not-settle" aria-label="Permalink to “Ending a run that will not settle”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> functions</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> terminate</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">function-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">run-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>Stops the run where it is and settles its row <code>terminated</code>. Both halves happen, and in that order: stopping the instance without settling the row would leave every reader still reporting <code>running</code>, and settling the row without stopping the instance would leave your code executing — and the next status read reconciles the row against the engine and rolls it forward again.</p><p>A run whose instance is already <strong>gone</strong> — aged out of the engine&#39;s retention, or discarded with the document it was working on — still settles, because that is the run you came here to end. A stop that fails for any other reason is an <strong>error</strong>, and the run is left exactly as it was: the engine is the only thing that knows whether your code is still executing, and a row settled over a live run would both mislead you and remove the retry, since a settled run is reported rather than stopped. Run the command again.</p><p>A run that has <strong>already finished</strong> is reported, not overwritten: the command prints the status it settled at and terminates nothing. Its outcome is the only record of what it actually did — and that is asked of the engine, not only of the run row. A task run settles its row on the first status read after it ends, so a run that finished seconds ago may still be stored <code>running</code>; terminating it records what it really did instead of burying it under <code>terminated</code>.</p><p>Any step still open when the run is terminated is closed with it, however the run was stopped — this command or <code>client.functions.terminate</code> — so the trace of a run stopped mid-<code>step.sleep</code> does not go on reporting that sleep as <code>running</code>. Running the command against a run that is already over closes anything its trace still has open, so a trace left behind by an older stop is repaired by asking again.</p><p>Availability is <strong>server-owned</strong>: <code>status</code> is not a TOML key, <code>config pull</code> never writes it, and <code>config push</code> never sends it. That cuts both ways deliberately — a <code>config push</code> to a <strong>disabled</strong> function updates its code and leaves it out of service, so shipping a fix never puts an endpoint back in service on its own.</p><p><code>archive</code> retires a function without destroying it. The archived row goes on holding its key, pushing code to it is refused, and there is no un-archive. Reclaiming the key means a hard delete: run <code>primitive config pull</code> (which removes the tombstone&#39;s local files), then a confirmed <code>primitive config push --prune</code>, which destroys the function, every version and their stored bundles.</p><h2 id="one-key-namespace-per-app" tabindex="-1">One key namespace per app <a class="header-anchor" href="#one-key-namespace-per-app" aria-label="Permalink to “One key namespace per app”">​</a></h2><p>A function&#39;s key is unique per app, case-insensitively. Pushing a function whose key another object in the app already holds fails naming the holder. Keys must be URL-path-safe, because a function key doubles as the path segment that addresses it.</p><h2 id="migrating-a-workflow-tree-to-functions" tabindex="-1">Migrating a workflow tree to functions <a class="header-anchor" href="#migrating-a-workflow-tree-to-functions" aria-label="Permalink to “Migrating a workflow tree to functions”">​</a></h2><p>A DSL workflow tree moves <strong>leaf-first</strong>. Take the leaves — the workflows that do one thing and call nothing — and rewrite each as a function. Leave the parents alone: a parent&#39;s <code>workflow.call</code> step names a <strong>key</strong>, and a key may name a function, so the parent goes on calling exactly what it called before. The parents move last, when they have nothing left to call but functions.</p><p>That order matters because an app&#39;s parents are its largest workflows. Moving a whole subtree at once is the only alternative, and for a real app it is not a migration anyone can stage.</p><h3 id="calling-a-function-from-a-workflow" tabindex="-1">Calling a function from a workflow <a class="header-anchor" href="#calling-a-function-from-a-workflow" aria-label="Permalink to “Calling a function from a workflow”">​</a></h3><p>Nothing changes in the step. <code>workflow.call</code> resolves its <code>workflowKey</code> like this:</p><ol><li>A <strong>live workflow</strong> holding the key wins. Everything about that call is what it has always been.</li><li>When the key names <strong>no workflow, or an archived one</strong>, a function holding the same key answers instead. Its output becomes the step&#39;s <code>output</code>, so <code>saveAs</code>, templates and downstream steps read through it exactly as they did for the child workflow.</li><li>When neither answers, the step fails with the error it always failed with — <code>Workflow &quot;&lt;key&gt;&quot; not found</code>, or the archived workflow&#39;s own sentence.</li></ol><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">steps</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">id = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;shape-order&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">kind = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;workflow.call&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">workflowKey = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;shape-order&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # a workflow yesterday, a function today</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">steps</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">orderId = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;{{ input.orderId }}&quot;</span></span></code></pre></div><p><strong>Authorization.</strong> A <strong>caller-mode</strong> parent — one a member started — has the callee function&#39;s own <code>access</code> gate evaluated against that member before anything is dispatched, the same way the parent has always had a child workflow&#39;s <code>accessRule</code> evaluated. A refused call fails the step with <code>FUNCTION_ACCESS_DENIED</code> and never reaches the function. App admins and owners pass, as they do at every other gate. A <strong>system</strong> parent (a cron or webhook run) skips the gate, exactly as it skips a child workflow&#39;s rule.</p><p>This is not ceremony. <code>workflowKey</code> is a template, so a dispatcher workflow may carry <code>workflowKey = &quot;{{ input.target }}&quot;</code> and let its caller choose. Without the gate, any member that dispatcher admits could reach any request function — including one whose own gate denies them — with the app&#39;s authority.</p><p><strong>Identity.</strong> The function runs with the app&#39;s system authority, like every function. <code>ctx.user</code> names the member who started the parent run, or <code>null</code> when nothing human did — a cron fire, a webhook delivery, a system workflow. <code>ctx.trigger</code> says how the function was entered:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">orderId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (ctx.trigger.kind </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;workflow&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;called by&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ctx.trigger.workflowKey, ctx.trigger.runId, ctx.trigger.stepId);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { orderId: input.orderId };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><strong>Budget and bounds.</strong> A bridge invocation gets the platform&#39;s full synchronous budget, 30 seconds, because the DSL child it replaces had the parent run&#39;s whole budget and a slow leaf must not break mid-migration. It draws on the function&#39;s ordinary per-minute invocation ceiling, and its declared <code>inputSchema</code> and <code>outputSchema</code> apply at this door as at every other. There is no separate run row: the parent&#39;s own step run is the record, and <code>primitive functions logs</code> shows the invocation.</p><p><strong>Failures.</strong> A function that throws, times out or answers something its <code>outputSchema</code> refuses fails the step carrying the function&#39;s own error code and message, so the parent&#39;s <code>continueOnError</code> and <code>retry</code> settings apply unchanged. An archived function (<code>FUNCTION_ARCHIVED</code> — its tombstone still holds the key, so the step names the function rather than the workflow it replaced), a disabled one (<code>FUNCTION_DISABLED</code>), one with no pushed code (<code>FUNCTION_NOT_PUSHED</code>) and a task-mode one (<code>FUNCTION_MODE_UNAVAILABLE</code> — <code>workflow.call</code> is synchronous and a <code>durable</code> function has no answer to hand back) fail the step without a retry, naming the remedy. Every one of those codes lands on <code>steps.&lt;id&gt;.code</code>, so a later step can branch on which failure it was.</p><p>The bridge is <strong>transitional</strong>. It exists so a tree can move; it retires with the workflow engine in project phase 7, along with <code>workflow.call</code> itself. It goes one way only: a function never calls a workflow.</p><h3 id="taking-the-key-over" tabindex="-1">Taking the key over <a class="header-anchor" href="#taking-the-key-over" aria-label="Permalink to “Taking the key over”">​</a></h3><p>A replacement should not carry a temporary name into its public URL for the life of a migration, so a function may take an <strong>archived</strong> workflow&#39;s key:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> workflows</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> archive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> shape-order</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # the workflow keeps its history</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                     # the function claims the same key</span></span></code></pre></div><p>The reservation moves to the function, and the archived workflow row is untouched — its status, its versions and its runs all still resolve by id. Every place the key resolves now resolves to the function: the HTTP invoke path, a webhook path the function declares, and a parent&#39;s <code>workflow.call</code>.</p><p>If the workflow is <strong>not</strong> archived, the push is refused:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>functionKey already exists (held by a workflow 01J…; archive it with</span></span>
<span class="line"><span>&#39;primitive workflows archive&#39;, or remove its file and run</span></span>
<span class="line"><span>&#39;primitive config push&#39;, then re-push this function to take the key)</span></span></code></pre></div><p>That is the whole difference: archive first, then push. Only a function may take a key this way, and only from an archived workflow — a new workflow, script or webhook still meets the ordinary conflict, and an archived <strong>function</strong> still blocks everything, because a function tombstone keeps its key.</p><h3 id="what-becomes-what" tabindex="-1">What becomes what <a class="header-anchor" href="#what-becomes-what" aria-label="Permalink to “What becomes what”">​</a></h3><p>Three rules cover almost every workflow step and operation an app has:</p><ul><li><strong>An operation that filtered on a <code>$params.userId</code> fed from the user becomes a <code>defineQuery</code> with the <code>$caller</code> marker.</strong> The platform injects the invoking user, a call may not supply one, and the query runs on the app&#39;s authority under the function&#39;s <code>access</code> gate. Threading the id through a parameter was always a rule you had to remember to write; the marker is one the platform keeps.</li><li><strong>A client-callable operation becomes a function.</strong> The gate that decided who could execute the operation becomes the function&#39;s <code>access</code> rule, and the operation&#39;s shape becomes the function&#39;s <code>inputSchema</code> and <code>outputSchema</code>.</li><li><strong>A <code>script</code> step becomes a pure function.</strong> A Rhai transform has no platform access to lose: the same input, the same output, written in TypeScript.</li></ul><h3 id="do-not-reuse-the-old-envelope-as-fixtures" tabindex="-1">Do not reuse the old envelope as fixtures <a class="header-anchor" href="#do-not-reuse-the-old-envelope-as-fixtures" aria-label="Permalink to “Do not reuse the old envelope as fixtures”">​</a></h3><p>Test fixtures captured from the workflow you are replacing <strong>prove nothing about the replacement</strong>. They describe the old envelope, so a port that shapes its answer to them passes its tests while every page reading the new one is wrong. Write the replacement&#39;s tests against what the caller needs now, and check a real caller against the running function before you archive anything.</p>`,400)])])}const g=i(n,[["render",h]]);export{c as __pageData,g as default};
