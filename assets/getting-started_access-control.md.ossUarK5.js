import{_ as s,c as t,o as a,aj as i}from"./chunks/framework.Id7s26SP.js";const k=JSON.parse('{"title":"Access Control","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/access-control.md","filePath":"getting-started/access-control.md"}'),n={name:"getting-started/access-control.md"};function r(o,e,l,h,d,p){return a(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="access-control" tabindex="-1">Access Control <a class="header-anchor" href="#access-control" aria-label="Permalink to “Access Control”">​</a></h1><p>Access rules in Primitive are written in <strong><a href="https://github.com/google/cel-spec" target="_blank" rel="noreferrer">CEL</a></strong> — a one-line expression evaluated against the authenticated caller. CEL has two jobs: a <a href="./server-functions.html">server function</a>&#39;s <code>access</code> gate, which decides who may run the function, and <strong>rule sets</strong>, which govern who may manage the platform&#39;s own resources — groups, <a href="./collections.html">collections</a>, blob buckets, database types and named locks. One expression language, one identity context: learn it once and you can read and write the rules anywhere they appear.</p><h2 id="cel-in-sixty-seconds" tabindex="-1">CEL in Sixty Seconds <a class="header-anchor" href="#cel-in-sixty-seconds" aria-label="Permalink to “CEL in Sixty Seconds”">​</a></h2><p>A CEL expression is a one-line condition that evaluates to true or false:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user.userId != &#39;&#39;&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                  # any authenticated user</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hasRole(&#39;admin&#39;) || hasRole(&#39;owner&#39;)&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               # app admins</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;isMemberOf(&#39;team&#39;, &#39;core&#39;)&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                         # members of one group</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;size(memberGroups(&#39;team&#39;)) &gt; 0&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                     # member of any team</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">edit = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user.userId == group.createdBy&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                       # a rule-set rule: the group&#39;s creator</span></span></code></pre></div><p>Standard operators work as you&#39;d expect: <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&amp;&amp;</code>, <code>||</code>, <code>!</code>, <code>in</code>, plus functions like <code>size(...)</code>. String literals use single quotes inside TOML double-quoted strings.</p><h2 id="the-identity-context" tabindex="-1">The Identity Context <a class="header-anchor" href="#the-identity-context" aria-label="Permalink to “The Identity Context”">​</a></h2><p>Every CEL evaluation sees the authenticated caller:</p><table tabindex="0"><thead><tr><th>Variable / function</th><th>Meaning</th></tr></thead><tbody><tr><td><code>user.userId</code></td><td>The caller&#39;s user ID (empty string when unauthenticated)</td></tr><tr><td><code>user.role</code></td><td>The caller&#39;s app role</td></tr><tr><td><code>isAnonymous()</code></td><td>True when the caller has no account at all (an unauthenticated request). Write <code>!isAnonymous()</code> to require any signed-in member; relevant where anonymous access is possible, such as a <code>public</code> <a href="./blobs-and-files.html#access-presets">blob bucket</a></td></tr><tr><td><code>hasRole(role)</code></td><td>True if the caller&#39;s app role is <code>&quot;owner&quot;</code>, <code>&quot;admin&quot;</code>, or <code>&quot;member&quot;</code> as given (app-level role — distinct from the document <code>owner</code> <a href="./working-with-documents.html#sharing-documents">permission</a>)</td></tr><tr><td><code>isMemberOf(groupType, groupId)</code></td><td>True if the caller belongs to that exact group</td></tr><tr><td><code>memberGroups(groupType)</code></td><td>The list of group IDs of that type the caller belongs to</td></tr></tbody></table><p>Group membership is the workhorse: model teams, roles, and relationships as <a href="./users-and-groups.html">groups</a>, then write rules against membership instead of hard-coding user IDs. <code>isMemberOf(&#39;team&#39;, &#39;core&#39;)</code> stays correct as people join and leave; <code>user.userId == &#39;01ABC...&#39;</code> doesn&#39;t.</p><p>A function&#39;s <code>access</code> gate sees exactly this context. Each rule-set surface adds its own on top — the <code>group.*</code> a group rule set manages, the <code>collection.*</code> a collection rule set manages, the <code>record.key</code> a lock rule matches. The feature pages document their specific variables. A rule at a metadata-capable site also gains <code>md.self.*</code> — reading <code>md.self.&lt;category&gt;</code> needs no declaration (it&#39;s inferred), and <code>md.&lt;path&gt;.*</code> / <code>md.caller.*</code> are available where declared — see <a href="./resource-metadata.html">Resource Metadata</a>.</p><h3 id="a-rule-sees-identity-not-stored-data" tabindex="-1">A Rule Sees Identity, Not Stored Data <a class="header-anchor" href="#a-rule-sees-identity-not-stored-data" aria-label="Permalink to “A Rule Sees Identity, Not Stored Data”">​</a></h3><p>Every function above answers a question about the <em>caller</em> — their role, their group memberships. A rule has no way to read a database record. So an entitlement you want the server to enforce — a paid feature, a role gate — can&#39;t live as a flag on a row: storing <code>isSubscribed = true</code> and checking it in a rule doesn&#39;t work, because the rule can&#39;t read the row.</p><p>Model the entitlement as <strong>group membership</strong> instead, which a rule <em>can</em> check, and put the check in the <code>access</code> gate of every function behind the paywall:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># primitive/dev/functions/export-report.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;export-report&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entry = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;functions/export-report/index.ts&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">access = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;isMemberOf(&#39;subscription&#39;, &#39;trialing&#39;) || isMemberOf(&#39;subscription&#39;, &#39;active&#39;)&quot;</span></span></code></pre></div><p>Maintain that membership from your billing source — a function fired by the provider&#39;s <a href="./inbound-webhooks.html">webhook</a> adds a member when a subscription starts and removes them when it lapses (<code>ctx.api.groups.addMember</code>, <code>ctx.api.groups.removeMember</code>) — and every paid feature becomes a one-line membership check. Give that webhook function <code>access = &quot;false&quot;</code>: a webhook fire skips the gate, so a gate no member passes keeps members from calling it over HTTP to grant themselves the entitlement, while the provider&#39;s verified deliveries still run it.</p><h2 id="where-rules-appear" tabindex="-1">Where Rules Appear <a class="header-anchor" href="#where-rules-appear" aria-label="Permalink to “Where Rules Appear”">​</a></h2><p>Every surface a signed-in member can reach that runs app-defined server-side code, manages a platform resource, or spends the app&#39;s credentials, and what happens on each when <strong>no rule is set</strong>. App owners and admins bypass every rule below.</p><table tabindex="0"><thead><tr><th>Surface</th><th>What the rule gates</th><th>No rule set</th><th>Details</th></tr></thead><tbody><tr><td>Server functions (<code>access</code>)</td><td>Who may invoke or start a function — the rule is evaluated on every call, and it is the whole authorization: inside, the code acts with the app&#39;s authority</td><td><strong>Denied</strong> — push refuses code for a function with no gate, and a function without one denies every caller with <code>FUNCTION_ACCESS_DENIED</code></td><td><a href="./server-functions.html#the-gate-runs-on-every-call">Server Functions</a></td></tr><tr><td>Groups and collections (rule sets)</td><td>Who can perform <strong>management operations</strong> on groups and collections</td><td>Permissive defaults — any member creates; the creator manages what they created</td><td>below</td></tr><tr><td>Blob buckets (<code>preset</code> / <code>ruleSetId</code>)</td><td>Member-level access to a bucket&#39;s blobs, per operation</td><td>Cannot happen — a bucket carries a preset or a rule set</td><td><a href="./blobs-and-files.html#access-presets">Blobs and Files</a></td></tr><tr><td>Database types (rule set, <code>ruleSetName</code>)</td><td>Who may edit or delete a database type&#39;s configuration</td><td>Denied for non-admins</td><td><a href="./working-with-databases.html">Working with Databases</a></td></tr><tr><td>Named locks (<code>locks/acquire</code>, <code>renew</code>)</td><td>Which keys a caller may acquire or renew — the requested key is matched as <code>record.key</code> by the app&#39;s single <code>lock</code> rule set (<code>release</code> is authorized by the acquire handle and is never rule-gated)</td><td><strong>Open</strong> until a <code>lock</code> rule set is installed: any member may take any key. Once one is installed, each operation it does not define is denied for everyone but app admins and owners</td><td><a href="./locks.html#access-control">Locks</a></td></tr><tr><td>Metadata categories (<code>readRule</code> + <code>writeRule</code>)</td><td>Who can read or write one category of a resource&#39;s metadata</td><td>Denied for non-admins</td><td><a href="./resource-metadata.html">Resource Metadata</a></td></tr><tr><td>Server-stamped fields (trigger <code>when</code> conditions)</td><td>Whether a computed field applies to this write</td><td>Not a caller gate — the field simply always applies</td><td><a href="./working-with-databases.html#server-stamped-fields">Server-Stamped Fields</a></td></tr><tr><td>Documents</td><td>Who may read, write or share a document</td><td>Not CEL — per-document permission grants (<code>owner</code>, read-write, reader) to users, emails and groups</td><td><a href="./working-with-documents.html#sharing-documents">Sharing Documents</a></td></tr><tr><td>Notification send, run reads, admin routes</td><td>Sending a notification, reading a function run, and the app&#39;s administrative routes</td><td>Not a per-resource rule — a run read is gated on being the run&#39;s initiator, and the send and admin routes on the app admin role</td><td><a href="./notifications.html">Notifications</a></td></tr><tr><td>Direct model routes (<code>[app] directLlmEnabled</code>)</td><td>Whether the app allows the direct LLM / Gemini routes at all</td><td>No per-resource rule — the surface is <strong>off by default</strong> for every caller, app admins and owners included; <code>directLlmEnabled = true</code> in <code>app.toml</code> opts in</td><td><a href="./prompts.html#direct-model-routes-are-off-by-default">Prompts</a></td></tr></tbody></table><p>A prompt and an integration carry no rule of their own: nothing but a server function reaches them, so the calling function&#39;s <code>access</code> gate is their authorization (plus the function&#39;s <code>integration:&lt;key&gt;</code> capability for an integration).</p><h2 id="rule-sets-governing-management-operations" tabindex="-1">Rule Sets: Governing Management Operations <a class="header-anchor" href="#rule-sets-governing-management-operations" aria-label="Permalink to “Rule Sets: Governing Management Operations”">​</a></h2><p>A function&#39;s <code>access</code> gate decides who may run its code. <strong>Rule sets</strong> answer a different question: who may <em>manage</em> a platform resource directly — create groups of a type, add or remove members, edit or delete a collection, read or write a bucket&#39;s blobs, take a lock. A rule set is a named bundle of CEL rules per management operation:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># primitive/dev/rule-sets/team-management.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ruleSet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;team-management&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">resourceType = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;group&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">group</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">create = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;true&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">edit = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user.userId == group.createdBy&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">delete = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user.userId == group.createdBy&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">member</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">create = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;isMemberOf(group.groupType, group.groupId)&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">edit = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user.userId == group.createdBy&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">delete = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user.userId == group.createdBy&quot;</span></span></code></pre></div><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --only</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rule-set/team-management</span></span></code></pre></div><p>Bind a rule set to a <strong>group type</strong> or <strong>collection type</strong> via its type config — in TOML (<code>primitive/&lt;env&gt;/group-type-configs/&lt;type&gt;.toml</code>, <code>primitive/&lt;env&gt;/collection-type-configs/&lt;type&gt;.toml</code>) or from the client (<code>client.groupTypeConfigs.create({ groupType, ruleSetId })</code>). A <strong>blob bucket</strong> attaches one directly via its <code>ruleSetId</code>, where it governs member access per operation — see <a href="./blobs-and-files.html#access-presets">Blobs and Files</a>.</p><p>Two behaviors to know:</p><ul><li><strong>App owners and admins bypass rule-set evaluation</strong> — rules govern regular members.</li><li><strong>Types without a config row use permissive defaults</strong> (any member can create; the creator manages what they created). To deny an operation for everyone except admins, attach a rule set with that operation set to <code>&quot;false&quot;</code>.</li></ul><h3 id="rules-that-follow-a-group-s-own-memberships" tabindex="-1">Rules That Follow a Group&#39;s Own Memberships <a class="header-anchor" href="#rules-that-follow-a-group-s-own-memberships" aria-label="Permalink to “Rules That Follow a Group&#39;s Own Memberships”">​</a></h3><p>The functions above answer questions about the <em>caller</em>. In a group rule set you can also ask about the <strong>group being managed</strong> — the groups it itself belongs to — with <code>memberGroupsOf(group.groupId, &#39;&lt;groupType&gt;&#39;)</code>, which returns the list of group IDs of that type the group is a member of. This lets a rule follow a relationship graph: for example, grant a teacher access to a group when the group belongs to a class the teacher teaches.</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Allow a caller who teaches any class this group is enrolled in.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">get = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;memberGroupsOf(group.groupId, &#39;class-students&#39;).exists(c, isMemberOf(&#39;class-teachers&#39;, c))&quot;</span></span></code></pre></div><p>Both arguments are checked when the rule is saved: argument 1 must be the literal path <code>group.groupId</code> (any other expression is rejected), and argument 2 must be a literal group type. Because the group&#39;s identity is only trusted once the group exists, <code>memberGroupsOf</code> is available on existing-group operations (<code>get</code>, <code>edit</code>, <code>delete</code>, and the <code>member</code> operations) but <strong>not</strong> on <code>group.create</code>. The same function works in <a href="./resource-metadata.html">metadata category rules</a>, where the subject id is a loaded <code>md.self.*</code> value instead.</p><h2 id="testing-rules" tabindex="-1">Testing Rules <a class="header-anchor" href="#testing-rules" aria-label="Permalink to “Testing Rules”">​</a></h2><p>Rule sets are versioned and testable — evaluate rules against simulated requests before deploying:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Dry-run a rule set against a simulated scenario</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rule-sets</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">rule-set-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --scenario</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;user&quot;: {&quot;userId&quot;: &quot;u-1&quot;}}&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Trace an evaluation using real user and group data</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rule-sets</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> debug</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --user</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">userI</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --group-type</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> team</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --category</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> member</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --operation</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> create</span></span></code></pre></div><p>The same is available from the client as <code>client.ruleSets.test()</code> and <code>client.ruleSets.debug()</code>. For a function&#39;s <code>access</code> gate, the fastest loop is invoking it as a specific app user — <code>primitive functions invoke &lt;key&gt; --user &lt;user-id&gt;</code> — or signing in as different <a href="./authentication.html#test-user-sign-in">test users</a>.</p><p><strong>Test gated states as a plain member.</strong> App owners and admins bypass function <code>access</code> gates, rule sets, metadata read/write rules, and bucket presets, so you can&#39;t exercise a locked or entitlement-gated state while signed in as one: a paywalled feature reads as open to an admin even when the gate is correct. That includes <code>primitive functions invoke</code> without <code>--user</code>, which runs as your own admin app user. Verify that a gate actually denies using a <code>member</code> test user.</p><h2 id="trusting-external-identifiers" tabindex="-1">Trusting External Identifiers <a class="header-anchor" href="#trusting-external-identifiers" aria-label="Permalink to “Trusting External Identifiers”">​</a></h2><p>When the server acts on an identifier — opening a billing portal from a payment <code>customer_id</code>, calling a provider API on a user&#39;s behalf — it must not trust a value the client supplied. A caller could substitute someone else&#39;s id and act on their account.</p><p>Keep the authoritative user-to-external-id mapping in a database only your functions write, and resolve it inside the function from the <strong>authenticated</strong> caller — <code>ctx.user</code> — never from the function&#39;s input:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineFunction } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;primitive-functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">billingDatabaseId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // The caller is who the platform authenticated, not an id the client sent.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> profiles</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">db</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(input.billingDatabaseId, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;billing&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;BillingProfile&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> mine</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> profiles.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ filter: { userId: ctx.user</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.userId } });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> customerId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mine.items[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]?.customerId;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">customerId) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;no billing profile&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // …call the provider with \`customerId\` through an integration…</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { hasProfile: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>A function reads every row of the app on the app&#39;s own authority, so the scoping is the code&#39;s job: the filter on <code>ctx.user</code> is what keeps one caller from reaching another&#39;s row. The same rule holds for ids the app minted itself — a <code>householdId</code>, an <code>itemId</code> — once they arrive as function input rather than as something the server derived. &quot;External&quot; describes where the id came from into this request, not who defined it.</p><h2 id="patterns-that-hold-up" tabindex="-1">Patterns That Hold Up <a class="header-anchor" href="#patterns-that-hold-up" aria-label="Permalink to “Patterns That Hold Up”">​</a></h2><ol><li><strong>Default-deny, then open up.</strong> Start restrictive (<code>hasRole(&#39;admin&#39;)</code>) and widen deliberately. A function with no <code>access</code> gate is denied to every caller.</li><li><strong>One group type per concept.</strong> Separate <code>team</code>, <code>org</code>, and role-like types keep expressions readable: <code>size(memberGroups(&#39;team&#39;)) &gt; 0 || isMemberOf(&#39;org&#39;, &#39;staff&#39;)</code>.</li><li><strong>Membership over IDs.</strong> Manage <em>who&#39;s in the group</em>, not the rules — rules referencing groups rarely need to change.</li><li><strong>Gate the function, scope in the code.</strong> The <code>access</code> gate says who may run a function; which rows or documents that caller may touch is the function&#39;s code, filtering on <code>ctx.user</code>.</li></ol><h2 id="next-steps" tabindex="-1">Next Steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to “Next Steps”">​</a></h2><ul><li><strong><a href="./users-and-groups.html">Users and Groups</a></strong> — The groups your rules check membership against</li><li><strong><a href="./server-functions.html">Server Functions</a></strong> — The <code>access</code> gate in practice</li></ul>`,45)])])}const u=s(n,[["render",r]]);export{k as __pageData,u as default};
