import{_ as t,c as i,o as s,aj as o}from"./chunks/framework.Id7s26SP.js";const g=JSON.parse('{"title":"Configuring Primitive Services","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/configuring-primitive-services.md","filePath":"getting-started/configuring-primitive-services.md"}'),a={name:"getting-started/configuring-primitive-services.md"};function n(r,e,d,c,l,h){return s(),i("div",null,[...e[0]||(e[0]=[o(`<h1 id="configuring-primitive-services" tabindex="-1">Configuring Primitive Services <a class="header-anchor" href="#configuring-primitive-services" aria-label="Permalink to “Configuring Primitive Services”">​</a></h1><p>Everything Primitive does for your app — which sign-in methods are enabled, what server functions exist and who may call them, which external APIs they can reach, how your databases are shaped — is <strong>configuration of Primitive&#39;s services, stored on the Primitive servers</strong>. Your client code doesn&#39;t define that behavior; it invokes what configuration declares.</p><p>There are two equivalent ways to manage that configuration:</p><ol><li><strong>The <a href="https://admin.primitiveapi.com" target="_blank" rel="noreferrer">Admin Console</a></strong> — interactive and visual. Best for exploring, testing, and one-off changes.</li><li><strong>TOML files in your repo, synced via the CLI</strong> — configuration as code. Best for anything you want versioned, reviewed, reproduced across environments, or managed by an AI coding agent.</li></ol><p>Both edit the same underlying configuration. Most teams explore in the console and commit the result as TOML.</p><h2 id="the-sync-loop" tabindex="-1">The Sync Loop <a class="header-anchor" href="#the-sync-loop" aria-label="Permalink to “The Sync Loop”">​</a></h2><p><code>primitive config</code> round-trips your app&#39;s configuration between the server and a directory of TOML files:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # create the directory structure</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # download current server config as TOML</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ... edit files locally ...</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> diff</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # see what would change</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # apply your changes to the server</span></span></code></pre></div><p>The config directory is <code>primitive/&lt;env&gt;/</code> — one isolated slot per environment, and the only directory any <code>config</code> command reads or writes. The lookup walks up from where you run the command, so in an app with <a href="./multi-client.html">several clients</a> the configuration lives once at the repository root and every client directory resolves it.</p><p>The files live in your repo, so configuration changes ride the same workflow as code: branches, diffs, reviews, rollbacks. <code>config push</code> runs a local preflight over the tree before it applies anything — an error there aborts the push with no changes applied — and reports the file and entity behind any failure. See <a href="#what-a-push-guarantees">What a push guarantees</a> for what that preflight checks and what it leaves to the server.</p><p><strong>Preview before you push.</strong> <code>primitive config diff</code> shows which entities would be created, changed, or removed; <code>primitive config push --dry-run</code> walks the full push and reports the same outcome without writing anything. Both answer from the comparison push itself acts on, so a clean <code>diff</code> means <code>push</code> has nothing to apply; <a href="#what-diff-compares">What diff compares</a> covers how each difference is reported.</p><p>Every <code>config pull</code> snapshots the config directory before writing, so a pull that overwrites local edits is recoverable:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> revert</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --list</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           # list available snapshots</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> revert</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                  # restore the most recent</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> revert</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --snapshot</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # restore a specific one</span></span></code></pre></div><p>Snapshots are local CLI state, kept out of version control and pruned after 28 days. <code>revert</code> replaces the config directory with the snapshot — run <code>primitive config diff</code> afterwards to see where the restored files stand relative to the server.</p><h2 id="toml-is-the-only-write-path" tabindex="-1">TOML Is the Only Write Path <a class="header-anchor" href="#toml-is-the-only-write-path" aria-label="Permalink to “TOML Is the Only Write Path”">​</a></h2><p>Server-side configuration is authored in TOML and applied with <code>primitive config push</code>. That is the only way the CLI writes it: there are no <code>primitive &lt;thing&gt; create</code> / <code>update</code> / <code>delete</code> commands and no flags that set a configuration field. Whatever you want to change — a function&#39;s triggers, an integration&#39;s base URL, a prompt&#39;s model config, a cron schedule, an app setting, a config var — you edit the file that owns it and push.</p><p>Four commands cover authoring without leaving the terminal:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fields</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> function</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    # the type&#39;s TOML keys, types, required, defaults</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> create</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> function</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> order-intake</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # scaffold functions/order-intake.toml from defaults</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> function/order-intake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> function.description=&quot;Order intake&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # edit one scalar in place</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --only</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> function/order-intake</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # apply just that object</span></span></code></pre></div><ul><li><strong><code>config fields &lt;type&gt;</code></strong> lists a configuration type&#39;s TOML keys with their types, whether they&#39;re required, and their defaults — the way to discover the field surface. <code>--json</code> prints it for tooling, and <code>primitive config fields --help</code> lists every type it accepts.</li><li><strong><code>config create &lt;type&gt; &lt;key&gt;</code></strong> writes <code>&lt;key&gt;.toml</code> in that type&#39;s directory, filled in from the type&#39;s defaults. It is local only — nothing reaches the server until you push.</li><li><strong><code>config set &lt;object&gt; &lt;path&gt;=&lt;value&gt;...</code></strong> sets one or more scalars in an existing file, preserving comments and key order. <code>&lt;object&gt;</code> is <code>&lt;type&gt;/&lt;key&gt;</code>, <code>app</code>, or <code>vars</code>. Also local only. Tables and arrays are edited in the file directly.</li><li><strong><code>config push --only &lt;selector&gt;[,...]</code></strong> applies just the named objects instead of the whole tree. Selectors are <code>&lt;type&gt;/&lt;key&gt;</code>, <code>app</code>, or <code>var/&lt;key&gt;</code>. Everything else about push — validation, conflict detection, <code>--dry-run</code>, <code>--force</code>, <code>--prune</code> — behaves the same.</li></ul><p><code>set</code> plus <code>push --only</code> is the scripting path: two commands, no here-document, no temporary file.</p><p><strong>Deleting</strong> a configuration object means deleting its file and running <code>primitive config push --prune</code> — every type, no exceptions. One caveat is specific to metadata categories: deleting the definition does <strong>not</strong> delete stored metadata values, which become unreachable, so delete the values first (<code>primitive metadata delete</code>) if you need them gone. The prune plan says so before you confirm.</p><p><strong>Structured values are native TOML tables.</strong> Where a config surface takes a structured value — a function&#39;s or prompt&#39;s <code>inputSchema</code> and <code>outputSchema</code>, a prompt&#39;s <code>providerConfig</code>, a database type&#39;s <code>autoPopulatedFields</code>, a metadata category&#39;s <code>schema</code> — write it as nested TOML tables. The same fields also accept single-line JSON text, which the server treats identically, and <code>config pull</code> keeps whichever form a file already uses.</p><p><strong>What is not configuration stays interactive.</strong> Secrets (<code>primitive secrets set</code>) never go in a file. Resources whose creation mints a server id — <code>primitive apps create</code>, <code>databases create</code>, <code>collections create</code>, <code>groups create</code> — are created with a command, then configured in TOML. Data operations (<code>databases records</code>, <code>documents records</code>, <code>metadata set</code>) and every read or test command (<code>list</code>, <code>get</code>, <code>diff</code>, <code>test</code>, <code>execute</code>) are unaffected. Availability and retirement are the two recurring cases of this, covered next.</p><h3 id="status-is-server-owned" tabindex="-1">Availability (<code>status</code>) Is Server-Owned <a class="header-anchor" href="#status-is-server-owned" aria-label="Permalink to “Availability (status) Is Server-Owned”">​</a></h3><p>Three nouns carry an availability switch — <code>primitive functions</code>, <code>integrations</code> and <code>prompts</code> — and on every one of them <code>status</code> (<code>active</code> / <code>inactive</code>) is runtime state, not configuration. It is deliberately off the TOML surface entirely:</p><ul><li><strong>It has no TOML key.</strong> None of the three types&#39; config tables declare <code>status</code>; the CLI does not accept it on any file the three nouns own.</li><li><strong><code>disable</code>/<code>enable</code> are its only writers</strong> — <code>primitive functions disable &lt;function-id&gt;</code>, <code>primitive prompts enable &lt;prompt-id&gt;</code>, and so on. Taking an object out of service is reversible and immediate; <code>enable</code> puts it back.</li><li><strong><code>config pull</code> never writes it.</strong> A file a pull produces states the object&#39;s configuration, not whether it&#39;s currently serving — pull the same file whether the object is active or disabled.</li><li><strong>A file that still carries the key fails.</strong> A hand-authored or stale <code>status</code> line is an unrecognized key: <code>config push</code> refuses it, naming the <code>enable</code>/<code>disable</code> verbs instead, and <code>config diff</code>&#39;s identical pre-flight validation reports the identical rejection — so it is never silently applied or converged, and pushing a file pulled before an operator disabled the object can never re-enable it as a side effect.</li><li><strong><code>config diff</code> still surfaces it, distinctly from drift.</strong> When an object is out of service, diff appends &quot;inactive; configuration unchanged&quot; (or, for an archived row, &quot;archived; <code>config push --prune</code> reclaims it&quot;) to its line instead of reporting a difference to reconcile — the file and the server agree on configuration; only the operational switch differs.</li></ul><p>A prompt&#39;s per-config <code>status</code> (<code>active | archived</code> on a <code>[[configs]]</code> entry) is a different key answering a different question — which named config is retired — and it stays TOML-owned; see <a href="./prompts.html#prompt-configurations">Prompt Configurations</a>.</p><h3 id="archive-vs-prune" tabindex="-1">Retiring an Object: Archive vs Prune <a class="header-anchor" href="#archive-vs-prune" aria-label="Permalink to “Retiring an Object: Archive vs Prune”">​</a></h3><p>The same three nouns — functions, integrations, prompts — carry a third verb, <code>archive</code> (<code>primitive functions archive</code>, <code>primitive integrations archive</code>, <code>primitive prompts archive</code>). It answers a different question than availability: not &quot;is it serving right now&quot; but &quot;does this row still exist.&quot;</p><ul><li><strong><code>archive</code> is a soft delete</strong> — the CLI spelling of the API&#39;s plain <code>DELETE</code>. The row is retired but kept, so anything that references it keeps resolving, and it goes on holding its key. <code>enable</code> refuses an archived object, and there is no un-archive.</li><li><strong><code>config push --prune</code> is the hard delete</strong>, and the only one in the CLI: delete the object&#39;s TOML file and run a confirmed prune. The row is destroyed outright, and its key comes back for reuse.</li><li><strong>Recovering from an archive means the hard delete, not a second push.</strong> <code>config push</code> resolves an existing row by key and cannot clear a server-owned <code>archived</code> state, so undoing an archive is: prune the archived row away, then add the file back and push it as new.</li><li><strong>Every archive and prune confirms first.</strong> <code>--yes</code>/<code>-y</code> skips the prompt; on a pipe or in CI without it, the command exits <code>1</code> and asks you to re-run with <code>--yes</code> rather than acting unasked.</li></ul><p><code>users</code> and <code>admins</code> carry <code>enable</code>/<code>disable</code> but no <code>archive</code> — they are people, not configuration objects, and their model has two states by design.</p><h2 id="what-lives-in-the-config-directory" tabindex="-1">What Lives in the Config Directory <a class="header-anchor" href="#what-lives-in-the-config-directory" aria-label="Permalink to “What Lives in the Config Directory”">​</a></h2><p>One subdirectory per kind of configuration:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>primitive/&lt;env&gt;/</span></span>
<span class="line"><span>  app.toml                        # App settings</span></span>
<span class="line"><span>  vars.toml                       # Config vars (non-secret key/value store)</span></span>
<span class="line"><span>  prompts/*.toml                  # Managed prompts</span></span>
<span class="line"><span>  prompts/{key}.tests/*.toml      # Prompt test cases</span></span>
<span class="line"><span>  integrations/*.toml             # External API integrations</span></span>
<span class="line"><span>  integrations/{key}.tests/*.toml # Integration test cases</span></span>
<span class="line"><span>  functions/*.toml                # Server function definitions</span></span>
<span class="line"><span>  functions/{key}/**              # Function sources (the entry its TOML names)</span></span>
<span class="line"><span>  functions/primitive-db-types.d.ts  # Generated: this app&#39;s database types</span></span>
<span class="line"><span>  functions/primitive-functions.d.ts # Generated: the function SDK&#39;s own types</span></span>
<span class="line"><span>  functions/tsconfig.json         # Scaffolded: wires both into your editor</span></span>
<span class="line"><span>  database-type-configs/*.toml    # Database types: schemas, rule set, triggers</span></span>
<span class="line"><span>  blob-buckets/*.toml             # Blob bucket definitions</span></span>
<span class="line"><span>  email-templates/*.toml          # Email template overrides</span></span>
<span class="line"><span>  rule-sets/*.toml                # Access rule sets</span></span>
<span class="line"><span>  group-type-configs/*.toml       # Group type configuration</span></span>
<span class="line"><span>  collection-type-configs/*.toml  # Collection type configuration</span></span>
<span class="line"><span>  metadata-category-configs/*.toml # Resource metadata category configs</span></span></code></pre></div><p>Every feature page in these docs that shows a TOML block — <a href="./server-functions.html">Server Functions</a>, <a href="./prompts.html">Prompts</a>, <a href="./api-integrations.html">API Integrations</a>, <a href="./working-with-databases.html">Working with Databases</a>, <a href="./blobs-and-files.html">Blobs and Files</a>, <a href="./resource-metadata.html">Resource Metadata</a> — is describing one of these files. Define the entity in TOML, <code>primitive config push</code>, and it&#39;s live.</p><p><code>vars.toml</code> round-trips your app&#39;s <a href="./app-secrets.html#config-vars">config vars</a> the same way <code>app.toml</code> round-trips app settings — a flat table of non-secret key/value pairs, written by <code>config pull</code> and applied by <code>config push</code>.</p><p>Credentials never go in these files: config that needs an API key references it as <code>{{secrets.KEY}}</code>, with the value stored server-side. See <a href="./app-secrets.html">App Secrets</a>.</p><h3 id="app-settings" tabindex="-1">App Settings (<code>app.toml</code>) <a class="header-anchor" href="#app-settings" aria-label="Permalink to “App Settings (app.toml)”">​</a></h3><p><code>app.toml</code> holds the app-level settings. <code>primitive config push</code> applies them along with everything else, and <code>primitive config push --only app</code> applies just them. <code>primitive config pull --only app</code> writes the current server settings into <code>app.toml</code>, <code>primitive config diff --only app</code> shows per-field differences, and <code>primitive apps get</code> renders the current server-effective settings without touching any file. All of them read and write the same <code>app.toml</code>. The TOML-syncable settings are:</p><ul><li><code>[app]</code> — <code>name</code>, <code>mode</code>, <code>baseUrl</code>, <code>waitlistEnabled</code>, <code>waitlistNotifyAdmins</code>, <code>directLlmEnabled</code> (turns on the direct model routes — off by default, see <a href="./prompts.html#direct-model-routes-are-off-by-default">Direct Model Routes Are Off by Default</a>), <code>allowedDomains</code>, <code>testAccountBaseEmails</code>, <code>largeDocumentWindowDays</code> (how many days this app&#39;s <a href="./working-with-documents.html#large-documents">large documents</a> may be written offline before a client goes read-only — 1–14; omit it to take the deployment&#39;s window, 7 days unless the deployment says otherwise)</li><li><code>[auth]</code> — <code>googleOAuthEnabled</code>, <code>emailSignInEnabled</code>, <code>passkeyEnabled</code>, <code>passkeyUserVerification</code>, <code>appleSignInEnabled</code>, <code>appleAudiences</code>, <code>emailRedirectUris</code>, <code>[auth.google.clients.&lt;type&gt;]</code> Google client entries, and <code>[auth.passkeys]</code> relying-party config</li><li><code>[cors]</code> — <code>mode</code>, <code>allowedOrigins</code>, <code>allowCredentials</code>, <code>allowedMethods</code>, <code>allowedHeaders</code>, <code>exposedHeaders</code>, <code>maxAge</code></li><li><code>[invitations]</code> — <code>enabled</code>, <code>limit</code> (whether members may send invitations and the per-member cap)</li></ul><p><strong><code>app.toml</code> is the whole truth.</strong> <code>config push</code> sends a value for every setting listed above, not just the keys the file happens to carry, so the file states the app&#39;s configuration rather than patching it: delete a line and the next push <strong>clears</strong> that setting, or <strong>resets it to its declared default</strong> where the server has one (<code>emailSignInEnabled</code> and <code>waitlistNotifyAdmins</code> go back to <code>true</code> — so a file that never stated <code>emailSignInEnabled</code> turns email sign-in back <strong>on</strong>, and an app that wants it off keeps the line <code>emailSignInEnabled = false</code> in the file; <code>[cors] mode</code> to <code>&quot;universal&quot;</code>; the invitation limit to <code>5</code>; <code>waitlistEnabled</code>, <code>passkeyEnabled</code>, <code>directLlmEnabled</code>, <code>[invitations] enabled</code> and <code>allowCredentials</code> to <code>false</code>; everything else is cleared). This is the same source-of-truth rule every other configuration file follows (see <a href="#deletions-and-cleared-fields">Deletions, Cleared Fields, and Out-of-Band Edits</a>).</p><p><code>[app].name</code> and <code>[app].mode</code> are <strong>required</strong>: they have no default to reset to, and a deleted <code>mode</code> line silently returning an invite-only app to <code>public</code> is not a failure worth allowing, so their absence is a validation error from both <code>config push</code> and <code>config diff</code>. A key <code>config pull</code> omitted is a setting the server does not hold, so a pull followed by a push is a no-op. An unrecognized key (or a mistyped table header) is rejected by name before anything is applied, the same as for every other configuration file.</p><p>The <code>[auth]</code> fields — the Google client map, the email sign-in allow-list, and passkeys — follow the same TOML rules as everything above; see <a href="./authentication.html">Authentication</a> for what each field does and its error codes.</p><p>Change these settings by editing <code>app.toml</code> and pushing it — there is no command that sets an app setting directly (see <a href="#toml-is-the-only-write-path">TOML Is the Only Write Path</a>):</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.mode=invite-only</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --only</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app</span></span></code></pre></div><p><code>app.toml</code> accepts only the keys above. <code>googleClientId</code>, <code>googleClientSecret</code>, <code>redirectUris</code>, <code>passkeyRpId</code>, <code>passkeyRpName</code>, <code>magicLinkEnabled</code> and <code>otpEnabled</code> are rejected by name, and the error names the key that holds the setting: <code>[auth.google.clients.&lt;type&gt;]</code> for the Google id, secret and callbacks; <code>emailRedirectUris</code> for the email sign-in allow-list (a <code>redirectUris</code> list may mix Google callbacks with sign-in-link targets — split it); <code>[auth.passkeys]</code> for the passkey relying-party pair; and <code>emailSignInEnabled</code>, the one switch for email sign-in.</p><h3 id="functions-directory" tabindex="-1">The Functions Directory <a class="header-anchor" href="#functions-directory" aria-label="Permalink to “The Functions Directory”">​</a></h3><p><code>functions/</code> is the one directory whose contents are not all yours. A function is authored as <code>functions/&lt;key&gt;.toml</code> plus the TypeScript entry the TOML&#39;s <code>entry</code> key names, and those you write. The other files are put there by <code>primitive config push</code> so your editor and a bare <code>tsc</code> type your functions against your own schemas with nothing to wire up:</p><ul><li><strong><code>functions/primitive-db-types.d.ts</code></strong> — this app&#39;s database types, model by model, rendered from the <code>database-type-configs/*.toml</code> schemas in this same tree. It is what gives <code>ctx.db(databaseId, &quot;&lt;type&gt;&quot;).model(&quot;&lt;Model&gt;&quot;)</code> its model names and field types.</li><li><strong><code>functions/primitive-functions.d.ts</code></strong> — the types of the <code>primitive-functions</code> module every function imports.</li><li><strong><code>functions/primitive-function-types.d.ts</code></strong> — <code>&lt;Key&gt;Input</code> and <code>&lt;Key&gt;Output</code> for every function in the tree, from its <code>inputSchema</code> and <code>outputSchema</code>, and the <code>FunctionSchemas</code> augmentation that types the keyed <code>defineFunction(&quot;&lt;key&gt;&quot;, handler)</code>.</li><li><strong><code>functions/primitive-document-types.d.ts</code></strong> — the project&#39;s document models from <code>models/models.toml</code>, and the <code>DocumentSchemas</code> augmentation that types <code>ctx.doc(documentId).model(&quot;&lt;Model&gt;&quot;)</code>; open when the project has no schema file.</li><li><strong><code>functions/primitive-prompt-types.d.ts</code></strong> — <code>&lt;Key&gt;PromptOutput</code> for every <code>prompts/*.toml</code> in this tree that declares a <code>[prompt.outputSchema]</code>, and the <code>PromptSchemas</code> augmentation that types <code>ctx.prompts.run(&quot;&lt;key&gt;&quot;)</code>. A prompt that declares no output schema is listed with an empty entry, so its <code>parsed</code> stays <code>unknown</code>; a tree with no <code>prompts/</code> directory gets the open form.</li><li><strong><code>functions/primitive-document-schema.generated.toml</code></strong> — the project&#39;s <code>models/models.toml</code>, copied into the tree. <code>config push</code> carries the document schema inside each function&#39;s version so a function&#39;s first write to a declared model can seed that model&#39;s metadata in the document; <code>config pull</code> writes the active version&#39;s copy here, and removes a stale one when the active version carries none. Push and diff resolve the project file first and this copy only as the fallback, so a tree pulled into a project that has no <code>models/models.toml</code> still rebuilds the same version — and a push from a project that HAS one regenerates the copy from it.</li><li><strong><code>functions/generated/&lt;key&gt;.generated.ts</code></strong> — one typed client invoker per function, for the app&#39;s client code (it imports <code>js-bao-wss-client</code>): both verbs, because the caller picks the runtime at each call; <code>primitive functions codegen -o &lt;dir&gt;</code> writes these somewhere else.</li><li><strong><code>functions/tsconfig.json</code></strong> — the wiring that puts the sources and the declarations in one program, points <code>primitive-functions</code> at the declaration beside it, and excludes <code>generated/**</code>.</li></ul><p>The declarations and the invokers are <strong>generated by <code>primitive config push</code> and rewritten on every push</strong>: edit them and your next push discards the change, so treat them as build output (commit them or ignore them, as you prefer — <code>config diff</code> reports one that has gone stale). The <code>tsconfig.json</code> is <strong>scaffolded once and then left alone</strong>, because it is the file you have a legitimate reason to adjust; the only parts push repairs are the ones that make the program load at all — the <code>primitive-functions</code> path mapping, the declarations being included, and the <code>generated/**</code> exclusion — so an edited or stale tsconfig cannot leave push reporting success over a program no compiler can read. None of them is sent to the server: they are regenerated from the tree, never round-tripped.</p><p><code>primitive functions codegen</code> writes the same files without pushing, and <code>primitive functions codegen --check</code> exits non-zero when they are out of date or the tsconfig no longer loads them — the form a CI job can gate on.</p><p>The <code>[function]</code> table&#39;s own keys are covered with the functions themselves. It says nothing about how a function runs — the caller picks the runtime at each call, and code that must not run under one says so with <code>assertRuntime</code> (see <a href="./server-functions-runtimes.html#runtimes">Runtimes</a>). Its <code>capabilities</code> declare integrations (<code>integration:&lt;integrationKey&gt;</code>), secrets (<code>secret:&lt;NAME&gt;</code>) and a few high-blast operations (see <a href="./server-functions-context.html#capabilities">Capabilities</a>); its trigger blocks add a webhook, whose deliveries use the request runtime, and cron schedules, where every cron fire starts a task run (see <a href="./server-functions-invoking.html#triggers">Triggers</a>). Integrations are applied before functions, so a tree that adds an integration and a function that declares it lands in one push.</p><h3 id="test-case-identity" tabindex="-1">Test Case Identity <a class="header-anchor" href="#test-case-identity" aria-label="Permalink to “Test Case Identity”">​</a></h3><p><strong>A test case&#39;s file name is its identity.</strong> The basename of a <code>&lt;key&gt;.tests/&lt;case&gt;.toml</code> file is the case&#39;s key on the server — unique per block, case-insensitively. That is what makes the checked-in tree self-sufficient: a case&#39;s <code>.sync-state.json</code> entry exists only once it has been pushed or pulled at least once, so without a key derived from the tree itself, a newly authored case would create a duplicate of every case whose filename differed from its <code>[test] name</code> the first time anyone pushed it. <code>config pull</code> writes a case back under its own name rather than renaming it, and renaming the file renames the case on the next push — the same test case, with its history, not a second one. Keep the name path-safe (no <code>/</code> or <code>\\</code>, no leading or trailing spaces, no trailing dot, at most 200 characters); <code>config push</code> names any file that is not and refuses it — before it sends anything for that case, so it is never created without its identity — while the rest of the push proceeds. Cases created outside the CLI carry no key until a push stamps one, and where push cannot tell which server case an unkeyed local file belongs to it says so and creates nothing — <code>config pull</code> first, or push after pulling the commit that recorded its <code>.sync-state.json</code> entry.</p><p><strong>Writing the file is not enough to run it.</strong> A <code>.tests/&lt;case&gt;.toml</code> file is local until a <code>config push</code> registers it — <code>tests run-all</code> and <code>tests list</code> (on prompts and integrations alike) run the <strong>registered</strong> set on the server, not whatever is in the directory. A newly authored case is silently absent from <code>run-all</code> until you push, and editing an already-registered case&#39;s file doesn&#39;t change what runs until the next push sends it. <code>config diff</code> is where this becomes visible, with four counters per case:</p><ul><li><strong>Test Cases (local only)</strong> — a file with no registered case yet; <code>run-all</code> won&#39;t run it until you push.</li><li><strong>Test Cases (remote only)</strong> — a registered case whose file is gone; it keeps running until <code>config push --prune</code> removes it — deleting the file alone does not stop it.</li><li><strong>Test Cases (modified)</strong> — both exist but differ; <code>run-all</code> still runs the last-pushed version, not the file on disk.</li><li><strong>Test Cases (synced)</strong> — file and registered case match; the only state where what you wrote is what runs.</li><li><strong>Test Cases (push would refuse)</strong> — a case file that fails validation, shown only when there is one. It is a separate status, not a subset of &quot;local only&quot;: a malformed new case is counted here and nowhere else, so a &quot;local only&quot; count of zero doesn&#39;t mean every file reached the server.</li></ul><h2 id="email-templates" tabindex="-1">Email Templates <a class="header-anchor" href="#email-templates" aria-label="Permalink to “Email Templates”">​</a></h2><p>Primitive sends transactional email on your app&#39;s behalf — the sign-in email, document and collection share notices, waitlist and invitation messages. Each kind has a built-in default, and you can override any of them with your own subject and body. Your app can also register custom types to send app-specific mail such as order confirmations or receipts.</p><p>The built-in types are <code>email-sign-in</code>, <code>document-share</code>, <code>document-share-deferred</code>, <code>collection-share</code>, <code>collection-share-deferred</code>, <code>waitlist-invite</code>, <code>waitlist-signup-notification</code>, <code>admin-invite</code>, <code>app-invite</code>, <code>access-request-created</code>, and <code>access-request-resolved</code>. A server function&#39;s <code>ctx.api.email.send</code> can send any of these, or a custom kebab-case type you register.</p><p>Each type exposes template variables — <code>{{code}}</code>, <code>{{expiryMinutes}}</code> and the optional <code>{{magicLink}}</code> for <code>email-sign-in</code>, and so on — that Primitive substitutes at send time. A custom subject and/or HTML and text body overrides the built-in default; deleting the override restores it. Overrides live in the config directory as <code>email-templates/*.toml</code> and are authored and applied like any other configuration:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> create</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> email-template</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> email-sign-in</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # scaffold email-templates/email-sign-in.toml</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ... edit subject, html, text ...</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primitive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --only</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> email-template/email-sign-in</span></span></code></pre></div><p>The sign-in email is the one type <code>email-sign-in</code>: its code block carries <code>{{code}}</code> and <code>{{expiryMinutes}}</code>, and the optional link sits inside <code>{{#if magicLink}}…{{/if}}</code> as <code>{{{magicLink}}}</code>. <code>magic-link</code> and <code>otp</code> are not sign-in types: an override stored under either name is listed as retired and never rendered, and every write to it except deletion is refused with a message naming the block of <code>email-sign-in</code> its content belongs in.</p><p>See <a href="./authentication.html#email-sign-in-one-email-one-or-two-credentials">Email Sign-In</a> for which parts of the <code>email-sign-in</code> override are removable (the link block) and which aren&#39;t (<code>{{code}}</code>).</p><p>Delete the file and run <code>primitive config push --prune</code> to drop the override and go back to the built-in default. See <a href="./primitive-cli.html#email-templates">Email Templates</a> for the read commands — listing types and override status, viewing a type&#39;s current template and variables, and sending a test email.</p><h2 id="environments" tabindex="-1">Environments <a class="header-anchor" href="#environments" aria-label="Permalink to “Environments”">​</a></h2><p>A project usually targets more than one app — a development app for day-to-day coding, a production app for your customers. The CLI&#39;s named environments bind a server and app together so every command (including <code>config pull</code> and <code>config push</code>) knows which app it&#39;s talking to, and each environment gets its own isolated config directory. See <a href="./primitive-cli.html#project-configuration-and-environments">Project Configuration and Environments</a>.</p><p>In a web app, which Primitive environment a build resolves is independent of its Vite mode (the <code>.env.&lt;mode&gt;</code> file of app-behavior keys). When a mode&#39;s keys are only correct against one backend, pin the pair with <code>VITE_EXPECTED_PRIMITIVE_ENV=&lt;name&gt;</code> in that mode&#39;s <code>.env</code> file: a run that resolves a different environment then fails at startup instead of using the wrong app. See <a href="./deploying-to-production.html#pinning-a-mode-to-a-primitive-environment">Pinning a mode to a Primitive environment</a>.</p><h2 id="console-or-code" tabindex="-1">Console or Code? <a class="header-anchor" href="#console-or-code" aria-label="Permalink to “Console or Code?”">​</a></h2><table tabindex="0"><thead><tr><th>Task</th><th>Console</th><th>TOML + CLI</th></tr></thead><tbody><tr><td>Exploring and testing</td><td>Preferred — visual, interactive</td><td>Works but less convenient</td></tr><tr><td>Version-controlled config</td><td>—</td><td>Preferred</td></tr><tr><td>Quick one-off changes</td><td>Convenient</td><td>Convenient</td></tr><tr><td>AI agent automation</td><td>—</td><td>Preferred — agents speak CLI</td></tr><tr><td>CI/CD pipelines</td><td>—</td><td>Preferred — scriptable</td></tr></tbody></table><h2 id="push-and-diff-in-detail" tabindex="-1">Push and Diff in Detail <a class="header-anchor" href="#push-and-diff-in-detail" aria-label="Permalink to “Push and Diff in Detail”">​</a></h2><p>The sync loop above is all most changes need. This section is the precise contract behind it — what <code>diff</code> compares, which failures stop a push, and how deletions and out-of-band edits reconcile — for when a push surprises you.</p><h3 id="what-diff-compares" tabindex="-1">What diff compares <a class="header-anchor" href="#what-diff-compares" aria-label="Permalink to “What diff compares”">​</a></h3><p><strong><code>config diff</code> and <code>config push --dry-run</code> answer &quot;has this changed?&quot; from one source.</strong> <code>diff</code> compares every resource type <code>config push</code> handles — every per-entity type, its test-case sidecars, and the app settings in <code>app.toml</code>, which are compared per field — for CONTENT against the state the server currently holds, and push decides what to apply from that same comparison. There is no per-type exception list to memorize: a clean <code>diff</code> means <code>push</code> has nothing to apply, and a comment-only or formatting-only edit is a change to neither command. A <strong>Modified</strong> row names the field it differs in — for a database type, the model field inside the <code>[models.*]</code> schema — ahead of the <code>config pull</code> remedy, so a difference you cannot see in the file is diagnosable from the row itself. A difference <code>diff</code> reports is one push acts on: it applies the edit, or it names what it will not overwrite — <strong>DRIFT</strong> when the server moved and your file did not (run <code>config pull</code>, or <code>--force</code> to overwrite), a <strong>CONFLICT</strong> when both moved or the direction cannot be established, or an immutable-field difference with the remedy (a blob bucket&#39;s <code>ttlTier</code> cannot be changed by an update, so push says so instead of reporting a success that changed nothing). A file <code>push</code> would REJECT is not one <code>diff</code> calls in sync: an unrecognized or retired key, or a value whose spelling is not its declared type, is reported by both commands with the identical message, under <strong>Invalid</strong> in the diff. If it can&#39;t fetch a type&#39;s current state from the server, that type is named explicitly under a <strong>not compared</strong> line instead of being silently left out, and the fallback fails closed — with no last-sync content hash to gate on, the resource is not written and the run tells you to <code>config pull</code> or <code>--force</code> rather than overwriting server state it never managed to read.</p><p>A local file with no matching server entity falls into one of two groups, reported separately: a file you authored and never pulled is <strong>new</strong> — push will create it — while a file a prior pull wrote whose entity is now absent from the server was deleted server-side, so push would <strong>re-create</strong> it unless you remove the file.</p><p>Both run the same local preflight a real push runs — the identical checks, reported in the identical words — and both add the comparisons against live server state. What a preview cannot reach is what only applying can: a server function&#39;s build, and a server refusal that depends on your app&#39;s state. Those surface when the push runs. <a href="#what-a-push-guarantees">What a push guarantees</a> draws the line. And a blocked entity records no sync state, so it stays visible as pending on the next <code>diff</code> instead of quietly reading as &quot;in sync.&quot;</p><h3 id="what-a-push-guarantees" tabindex="-1">What a push guarantees <a class="header-anchor" href="#what-a-push-guarantees" aria-label="Permalink to “What a push guarantees”">​</a></h3><p>A push has two stages, and only the first is all-or-nothing.</p><p><strong>Preflight — the local tree, and nothing else.</strong> Before the first mutating call, <code>config push</code> checks the files, and any error among them aborts the whole run with nothing applied. The two stages are a fixed classification, not a loose description: every cross-field rule and every field&#39;s own validation is declared as either <code>preflight</code> or <code>apply</code>. <code>preflight</code> means decidable from your files alone and enforced before the first mutating call — the CLI runs the server&#39;s own check rather than a local imitation of it, so a preflight refusal is the sentence the server would have given you. <code>apply</code> means the rule needs your app&#39;s state, or has path semantics only the server can decide. Unrecognized keys and declared types are preflight on every config type, and so is almost everything about a server function&#39;s file (the <code>function</code> type): its <code>entry</code>, its capability grammar, the trigger block shapes, a webhook trigger&#39;s signing-secret requirement and reference shape, and a cron entry&#39;s expression and timezone. A constraint that spans two fields of one object is decidable here because the state a push writes comes from the file: a function&#39;s version carries the whole authored TOML.</p><p><strong>Apply — incremental, convergent, and without rollback.</strong> Once the preflight passes, entities are applied one after another. A rejection may stop the remaining work, depending on the type — a rule set&#39;s or an integration&#39;s server error ends the run — while a refused function is recorded and the push continues, so the rest of the tree still lands. Nothing already applied is undone. The exit status is non-zero, and re-running converges: every applied change is idempotent.</p><p><strong>What the preflight cannot answer.</strong> Whether a <code>{{secrets.KEY}}</code> reference names a secret that exists, whether a webhook scheme&#39;s <code>config</code> is valid under this environment&#39;s JWKS policy, per-app caps, key collisions, archived entities — each of those is a question about the app&#39;s state, not about your file, so each is <code>apply</code>, surfacing during apply rather than preflight. Nothing you could work out from the file alone is on that list; guessing at these locally would mean a preflight that gives a false all-clear. Which stage a refusal came from tells you whether re-running could ever help without touching the file: an <code>apply</code> refusal may depend on your app&#39;s state, so fixing that state — creating the missing secret, freeing the cap — and re-running can turn it into a success; a <code>preflight</code> refusal cannot, because it is decided by the tree in front of you and nothing server-side changes the answer. A function whose webhook trigger references a secret you have not created yet is the plain example: the file is well formed, the push starts, that one function fails naming the secret, and its siblings land.</p><p><strong>An unrecognized key is an error, not a shrug.</strong> <code>config push</code> rejects any key the CLI does not recognize — in a server function&#39;s <code>[function]</code> table, a prompt&#39;s <code>[prompt]</code> or <code>[[configs]]</code> blocks, or an integration, blob bucket, email template, database type, rule set, group- or collection-type config or metadata-category config&#39;s table, in <code>app.toml</code>&#39;s <code>[app]</code> / <code>[auth]</code> / <code>[cors]</code> / <code>[invitations]</code> sections, and in a <code>&lt;key&gt;.tests/</code> test-case file&#39;s <code>[test]</code> table — naming the file and the key, so a typo fails locally instead of being silently dropped on the way to the server. The same goes for a whole table: a mistyped header (<code>[integraton]</code>) or a repeated table written with single brackets (<code>[configs]</code> where <code>[[configs]]</code> is meant) is named too, because the file would otherwise push as though the table it meant to write were empty — clearing the fields inside it. The check runs in pre-flight, so a bad key or table aborts before anything is applied, and <code>config diff</code> runs the identical check and reports the identical message — a file push will refuse is never one diff calls in sync. <strong>The declared type is part of the contract</strong>: a value whose spelling is not its field&#39;s type — a quoted number for <code>timeoutMs</code>, a table where JSON text is expected — is a validation error both commands name by field, not something either converts for you. (A handful of fields genuinely accept two spellings and are recorded as such: a prompt&#39;s <code>temperature</code> and <code>topP</code> are stored as strings and returned parsed, so <code>&quot;0.2&quot;</code> and <code>0.2</code> are one value, and a JSON field may be a native table or JSON text.) The mirror case is a CLI older than the server: <code>config pull</code> names every key the server returned that this CLI version does not know and leaves it out of the file rather than writing a key the next push would reject. Nothing is dropped without a message, and the round trip stays safe — push only sends the fields it knows, so the server keeps its stored values for the rest. Upgrade the CLI (<code>npm i -g primitive-admin@latest</code>) to manage the new keys.</p><h3 id="deletions-and-cleared-fields" tabindex="-1">Deletions, Cleared Fields, and Out-of-Band Edits <a class="header-anchor" href="#deletions-and-cleared-fields" aria-label="Permalink to “Deletions, Cleared Fields, and Out-of-Band Edits”">​</a></h3><p><strong>Removing a managed field clears it.</strong> For the fields the CLI owns on a synced entity, the local file is the source of truth: delete a line and the next <code>push</code> clears that field on the server rather than leaving the old value in place. Remove a database type&#39;s <code>ruleSetName</code>, or an integration&#39;s or prompt&#39;s <code>description</code>, and push sets it back to empty — for database types it reports each one as <code>Cleared &lt;field&gt; on &lt;type&gt;</code>. Server functions are the strongest form of this rule: a push that changes a function creates a new version from the whole authored file, so a removed line — a trigger, a capability, a limit — is simply absent from the version that runs. <code>status</code> is never on the list: availability is <a href="#status-is-server-owned">server-owned</a>. This is field-level and separate from removing a whole file: a default push never deletes an entity, so a file you delete simply isn&#39;t pushed and its server entity stays put (tree-level deletions are handled by the pull-side reconciliation and <code>push --prune</code> below). App settings follow this rule too, in its strongest form: <code>config push</code> sends the whole of <code>app.toml</code>, so a deleted line clears the setting or resets it to its declared default (see <a href="#what-lives-in-the-config-directory">What Lives in the Config Directory</a>).</p><p><strong><code>config pull</code> mirrors deletions.</strong> A pull doesn&#39;t only write files — after downloading the current configuration it also removes local files whose entity a prior pull wrote but the server no longer returns (an entity you deleted in the console or with a delete command). This keeps the config directory a faithful mirror: because a default <code>config push</code> treats every local TOML as something to create or update, a stale file left behind would re-create the entity you just deleted. Files you authored by hand and never pulled are left untouched — a <code>diff</code> lists them as <em>new</em> — and a file with uncommitted git changes is kept rather than removed, so an in-progress edit is never lost. Pass <code>--no-prune</code> to keep every local file regardless; and because the snapshot below is taken first, <code>config revert</code> restores anything a pull removed unexpectedly.</p><p><strong><code>config push --prune</code> mirrors deletions the other way.</strong> By default a push only creates and updates — delete a local file and its server entity lives on. Pass <code>--prune</code> and push also deletes: after the create/update pass, it removes managed entities — the ones a prior pull recorded in sync state — whose local TOML you&#39;ve since deleted. Entities authored in the console and never pulled are never touched. <code>config diff</code> shows every server entity that has no local file under <strong>Remote only</strong>, split into <em>managed</em> (which <code>--prune</code> deletes) and <em>unmanaged</em> (left alone). Each deletion is confirmed by a fresh read of the entity and skipped if it changed on the server since your last sync — <code>--force</code> overrides that check — and an entity still referenced by another is reported and kept while the rest proceed. Push confirms once before deleting; <code>--yes</code> skips the prompt for CI, and <code>--dry-run</code> reports the plan without deleting anything.</p><p><strong>Make the sync tree the single source of truth.</strong> Once you manage configuration as TOML, create entities and land changes by editing files and pushing — including throwaway test or debugging functions — rather than with console edits. The CLI records each pushed entity&#39;s server identity in <code>.sync-state.json</code>, the committed baseline at the root of the environment&#39;s config directory, and uses it to decide whether a push creates or updates each entity; changes made outside the loop leave that state out of date, and the two directions behave differently. For an entity <strong>created</strong> outside the loop (a console edit), push adopts by key: a create that hits a conflict matches the existing entity and updates it in place instead of failing, so re-running a push that hit a conflict succeeds. An entity <strong>deleted</strong> outside the loop leaves a stale entry behind: the next push of an edited version of its TOML updates an entity that no longer exists and fails with a &quot;not found&quot; error. When a push fails that way, remove that entity&#39;s entry from <code>.sync-state.json</code> and push again — the entity is created fresh.</p><h2 id="next-steps" tabindex="-1">Next Steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to “Next Steps”">​</a></h2><ul><li><strong><a href="./primitive-cli.html">Primitive CLI</a></strong> — Full CLI reference, environments, and sync details</li><li><strong><a href="./admin-console.html">Admin Console</a></strong> — The interactive view of the same configuration</li><li><strong><a href="./server-functions.html">Server Functions</a></strong> — Server-side logic, authored and pushed from the same tree</li></ul>`,88)])])}const u=t(a,[["render",n]]);export{g as __pageData,u as default};
